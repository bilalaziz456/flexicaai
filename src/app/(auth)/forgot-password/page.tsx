import { ForgotPasswordForm } from "./forgot-password-form";

/** Forgot-password page (public). Enumeration-safe; the action always responds the same. */
export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
