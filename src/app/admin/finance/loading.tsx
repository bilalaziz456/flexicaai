import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the company P&L. The PanelShell persists around it. */
export default function AdminFinanceLoading() {
  return <ReportSkeleton />;
}
