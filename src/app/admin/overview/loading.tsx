import { DashboardSkeleton } from "@/core/ui/panel-skeleton";

/** The owner overview. The PanelShell persists around it. */
export default function AdminOverviewLoading() {
  return <DashboardSkeleton />;
}
