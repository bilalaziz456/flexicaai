import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the revenue-share report. The PanelShell persists around it. */
export default function ClinicSharesLoading() {
  return <ReportSkeleton />;
}
