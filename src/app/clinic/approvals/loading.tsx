import { PanelSkeleton } from "@/core/ui/panel-skeleton";

/** Discount approvals: a policy card, then the pending list. The PanelShell persists around it. */
export default function ClinicApprovalsLoading() {
  return <PanelSkeleton />;
}
