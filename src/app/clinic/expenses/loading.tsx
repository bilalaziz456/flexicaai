import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the expenses report. The PanelShell persists around it. */
export default function ClinicExpensesLoading() {
  return <ReportSkeleton />;
}
