import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the sales report. The PanelShell persists around it. */
export default function ClinicSalesLoading() {
  return <ReportSkeleton />;
}
