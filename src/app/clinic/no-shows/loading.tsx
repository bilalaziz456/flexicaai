import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the no-show report. The PanelShell persists around it. */
export default function ClinicNoShowsLoading() {
  return <ReportSkeleton />;
}
