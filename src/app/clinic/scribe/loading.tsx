import { PanelSkeleton } from "@/core/ui/panel-skeleton";

/** The scribe workspace. The PanelShell persists around it. */
export default function ClinicScribeLoading() {
  return <PanelSkeleton />;
}
