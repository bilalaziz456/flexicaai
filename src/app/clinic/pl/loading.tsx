import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the profit & loss report. The PanelShell persists around it. */
export default function ClinicPlLoading() {
  return <ReportSkeleton />;
}
