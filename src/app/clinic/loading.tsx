import { DashboardSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the clinic dashboard. The PanelShell persists around it. */
export default function ClinicLoading() {
  return <DashboardSkeleton />;
}
