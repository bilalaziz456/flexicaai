import { FormSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the clinic settings form. The PanelShell persists around it. */
export default function ClinicSettingsLoading() {
  return <FormSkeleton />;
}
