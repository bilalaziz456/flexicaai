import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** The practice overview, which is charts throughout. The PanelShell persists around it. */
export default function ClinicReportsOverviewLoading() {
  return <ReportSkeleton />;
}
