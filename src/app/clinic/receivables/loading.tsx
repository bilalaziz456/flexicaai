import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the receivables report. The PanelShell persists around it. */
export default function ClinicReceivablesLoading() {
  return <ReportSkeleton />;
}
