import { ReportSkeleton } from "@/core/ui/panel-skeleton";

/** Loading shape for the discounts report. The PanelShell persists around it. */
export default function ClinicDiscountsLoading() {
  return <ReportSkeleton />;
}
