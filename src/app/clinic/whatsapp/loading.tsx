import { PanelSkeleton } from "@/core/ui/panel-skeleton";

/** The WhatsApp queue. The PanelShell persists around it. */
export default function ClinicWhatsappLoading() {
  return <PanelSkeleton />;
}
