import { PanelLoader } from "@/core/ui/panel-loader";

/** Loading boundary for /doctor — spinner in the content area during nav. */
export default function DoctorLoading() {
  return <PanelLoader />;
}
