# PROGRESS.md — FlexicaAI Implementation Tracker

> This file tracks what has been implemented and what remains. It is updated after
> every meaningful change. The build order follows CLAUDE.md §11. Do not jump ahead.
>
> **Legend:** ✅ done · 🔨 in progress · ⬜ not started · ⚠️ blocked/needs attention

_Last updated: 2026-07-30_

---

## Product decisions

- **No public signup — accounts are invite/admin-provisioned only** (decided 2026-07-06,
  supersedes an earlier trial-signup idea). There is no `/signup` page.
  - **Super Admin** (seeded directly) creates **clinics + each clinic's Clinic Admin**,
    and selects that clinic's specialties → `clinics.modules_enabled` (Step 5).
  - **Clinic Admin** creates their own staff: doctors, receptionists (Step 6).
  - **No account = no login.** Everyone provisioned this way uses the single `/login`.
  - The **specialty checkboxes** (Dental now; Derma/Hair "coming soon") therefore live on
    the Super Admin "create clinic" screen (Step 5), rendered from the module registry so
    new modules appear with zero UI changes.
- **Login = single shared page** for all roles; the proxy routes each role to its panel.
- **Stack change: Supabase → local PostgreSQL + Drizzle + custom auth** (decided 2026-07-06,
  supersedes CLAUDE.md's original Supabase stack; §2/§5/§10 updated). Owner wants data on
  own Postgres + fastest local queries. Implications:
  - DB access is 100% server-side (no browser DB client). Multi-tenancy enforced by
    filtering every query by `clinic_id` in the query layer (Postgres RLS later, optional).
  - Auth is home-grown: `users` (bcrypt) + `sessions` (opaque cookie token, SHA-256 in DB).
  - **Drizzle vs raw SQL policy:** default Drizzle; raw SQL via `db.execute(sql\`…\`)` on the
    same pool for heavy analytics / hand-tuned queries. Full policy in `src/core/db/index.ts`.
  - File storage → local filesystem for now (audio/PDF/photos), S3-compatible later.

---

## Environment

- Node: v24.18.0 ✅
- npm: 11.16.0 ✅
- git: **2.55.0** ✅ (repo initialized; commit identity set locally as "Bilal Aziz" &lt;bilalaziz456@gmail.com&gt;)
- Next.js: **16.2.10** (spec said "14+"; scaffold gave latest — App Router, satisfies "14+")
- React: 19.2.4 · Tailwind: **v4** · TypeScript: strict ✅
- **PostgreSQL 17** ✅ (local service `postgresql-x64-17` running) · **Drizzle ORM** + `pg`
  · **bcryptjs** · drizzle-kit / tsx / dotenv (dev)
- Note: an `AGENTS.md` (added by create-next-app) warns Next 16 has API changes vs
  older docs — check `node_modules/next/dist/docs/` when unsure. Imported from CLAUDE.md.

---

## Build order (CLAUDE.md §11)

### 1. Project setup ✅  _(reworked to local Postgres 2026-07-06)_
Next.js 16 (App Router) · TypeScript strict · Tailwind v4 · shadcn/ui · **Drizzle + local Postgres**
- [x] Next.js app scaffolded (App Router, TS strict)
- [x] Tailwind configured (v4)
- [x] shadcn/ui initialized — output realigned to `/core/ui` + `/core/lib` per §3
- [x] Folder structure created per CLAUDE.md §3 (`/core`, `/modules`, `/config`)
- [x] DB layer wired: `core/db/index.ts` (single `pg` pool + Drizzle + raw escape hatch), `core/db/schema.ts`
- [x] Zod-validated env (`core/lib/env.ts`): server-only `DATABASE_URL` (no public vars now)
- [x] `.env.example` + `.env.local` updated to `DATABASE_URL` + seed vars
- [x] `drizzle.config.ts` + npm scripts (`db:generate/migrate/push/studio/seed`)
- [x] Typecheck clean + production build passes
- **Left for you:** set your Postgres password in `.env.local` `DATABASE_URL`, create the `flexicaai`
  database, then `npm run db:migrate` (see "How to run the DB" below).

### 2. Auth ✅  _(reworked to custom session auth 2026-07-06)_
- [x] Username/password login (`(auth)/login` + `signIn`/`signOut` in `core/auth/actions.ts`); no public signup
- [x] Roles: super_admin, clinic_admin, doctor, receptionist (`core/types/auth.ts` + `user_role` pg enum)
- [x] Custom sessions: `core/auth/session.ts` (opaque cookie token, SHA-256 hash in `sessions`), `password.ts` (bcrypt)
- [x] Server guards `requireUser` / `requireRole` (`core/auth/user.ts`) — the REAL auth gate; `signOut` + button
- [x] Edge-safe proxy (`src/proxy.ts`): coarse cookie-presence check only (no DB in Edge); role enforced in pages
- [x] Placeholder panel pages at `/admin`, `/clinic`, `/doctor`, `/reception` (role-gated)
- [x] Seed script `scripts/seed.ts` (`npm run db:seed`) creates the first super_admin
- [x] Typecheck + build green
- **Left for you:** after migrating, `npm run db:seed` to create your super admin, then log in.

### 3. Core DB schema ✅
- [x] `clinics` (with `modules_enabled` text[])
- [x] `users` (role, clinic_id, bcrypt hash, is_active) + `sessions`
- [x] `patients` (clinic-scoped; phone/WhatsApp, DOB, data-consent flag)
- [x] `appointments` (`module` text tag, status enum, scheduled_at, doctor)
- [x] `visits` (`module` tag, `status` draft/approved enum, transcript, JSONB note, approved_by)
- [x] `recalls` (`module` tag, due_at, status enum, source_visit link) — recall engine reads these
- [x] `module` kept as free-text tag (NOT enum) so new specialties need no schema change
- [x] Indexes on every `clinic_id` + common lookups (name/phone/date/status)
- [x] Tenant-scoping helper `byClinic()` (`core/db/tenant.ts`) — the multi-tenancy boundary
- [x] Migrations `0000` + `0001` applied; production build green
- **Note:** specialty clinical data (e.g. dental tooth-chart rows) will live in module tables
  linked to `visits`, never as columns on these core tables (CLAUDE.md §5).

### 4. Module registry + dental skeleton ✅
- [x] `ModuleDefinition` contract + specialty-catalog types (`core/types/module.ts`)
- [x] Dental module: `config.ts`, `prompts/scribe.ts`, `recall-rules.ts`, `drug-formulary.ts` (Pakistan brands)
- [x] `/config/modules.ts` registry: `MODULES`, helpers (`getModule`, `loadModules`, `getClinicWorkspace`)
- [x] `SPECIALTY_CATALOG` for Step 5 checkboxes: Dental=available, Derma/Hair=coming_soon (architected, not built)
- [x] Verified golden rule: `/core` imports from neither `/modules` nor `/config` (grep-clean)
- [x] Typecheck + build green
- **Adding a specialty later:** implement `/modules/<id>/config.ts`, register in `/config/modules.ts`,
  flip catalog entry to "available" — zero `/core` changes.

### 5. Super Admin panel (`/admin`) ✅ (billing deferred)
- [x] `/admin` layout guards super_admin; header + sign out
- [x] Clinics list (`/admin`) with specialty badges
- [x] Create clinic + **specialty checkboxes** (from `SPECIALTY_CATALOG`) + its Clinic Admin,
      in one transaction (`/admin/clinics/new`) — no service-role client needed (local Postgres)
- [x] Clinic detail (`/admin/clinics/[id]`): toggle modules + staff list (uses `byClinic()`)
- [x] Reusable `SpecialtyCheckboxes` (dental selectable; derma/hair disabled "coming soon")
- [x] Verified: `/admin` + `/admin/clinics/new` render 200 with a real super_admin session
- [x] **Force password change on first login** — `must_change_password` col (migration 0002),
      `/change-password` flow, enforced in `requireRole`; created clinic admins start with the flag.
      Verified: flag on → `/admin` 307 → `/change-password` 200.
- [x] **Edit a staff member's name + username** (unique-checked) from the clinic detail page
- [x] **Reset a staff password** — issues a temp password + sets the flag + revokes their sessions
- [x] **Rename clinic**; **suspend/reactivate** account (`is_active`, revokes sessions on suspend);
      **delete clinic** (danger zone, confirm by typing the name; cascades all its data)
- [ ] View billing / usage (deferred — not MVP-critical)
- [x] **Search** clinics by name on `/admin` (URL-driven `?q=`, debounced, case-insensitive)
- **Still deferred (revisit later):** billing & usage · audit log of admin actions (§10) ·
  pagination on clinics · clinic-level suspend. Not premature to skip now.

### 6. Clinic Admin panel (`/clinic`) ✅
- [x] Layout guards `clinic_admin` (via `requireClinicAdmin`, guarantees non-null clinicId); shows clinic name; nav
- [x] Dashboard: staff + patient counts (clinic-scoped, index-backed COUNTs)
- [x] Staff: add doctor/receptionist (temp password → must-change), list, suspend/reactivate, reset password
- [x] Patients: add (name, phone/WhatsApp, DOB, gender, address, consent), list, **search by name/phone**
- [x] All writes/reads clinic-scoped via `byClinic()`; staff mgmt also filters `clinic_id` so cross-clinic ids match 0 rows
- [x] Trigram (pg_trgm GIN) indexes on `patients.full_name` + `patients.phone` for fast ILIKE (migration 0006)
- [x] Verified: /clinic, /clinic/staff, /clinic/patients render 200 as clinic001; role isolation (clinic_admin → /admin 307)
- **Note:** clinic admin can only create `doctor`/`receptionist` — never admins.

### 7. Doctor panel — voice scribe (`/doctor`) ✅ _(needs API keys to run live)_
- [x] Voice recorder (client, browser MediaRecorder) — `scribe-workspace.tsx`
- [x] Audio → local filesystem storage (`core/integrations/storage`, clinic-namespaced; swap to S3 later)
- [x] `/api/ai/scribe`: Whisper (OpenAI, separate) → Claude (`claude-sonnet-4-6`) → structured note, using the CLINIC'S MODULE prompt (generic engine in `core/ai/scribe-engine` + `prompt-runner`; JSON via prompt + safe parse — Sonnet 4.6 has no `output_config.format`)
- [x] Draft → review/edit (generic `NoteEditor`, specialty-agnostic) → approve → save to `visits`
- [x] Flywheel: `visits.ai_draft` + `audio_key` (migration 0007) freeze the original AI output for edit-diffing
- [x] Drug validation against the module formulary (warns, doesn't block)

### 8. Prescription generator 🟨 _(PDF done; WhatsApp delivery deferred to Step 9)_
- [x] Dental drug formulary validation (drugs checked against the module formulary; scribe already warns at draft time)
- [x] PDF generation — generic CORE `core/lib/prescription-pdf.ts` (pdf-lib, built-in Helvetica; specialty-agnostic, renders {drug,dosage,duration}); `GET /api/prescriptions/[visitId]` serves it from the APPROVED visit note, clinic-scoped; doctor "Prescription" link on approved visits
- [ ] WhatsApp delivery → Step 9

> Lib note: used **pdf-lib** (not react-pdf/pdfkit from CLAUDE.md §2). Reason: both
> pdfkit and @react-pdf/renderer have font-file/asset bundling issues under Turbopack;
> pdf-lib embeds standard fonts with zero config and renders reliably in a Next route.

### 9. WhatsApp integration 🟨 _(infra done; needs AiSensy account + approved template to run live)_
- [x] CORE AiSensy client `core/integrations/whatsapp` (v2 Campaign/template API; specialty-agnostic; config-gated)
- [x] `whatsapp_messages` log table + enums (migration 0008) — records every send + inbound; source for the Step 11 queue
- [x] Notification channel `core/notifications/whatsapp` — records FIRST (queued), then sends, then updates status (nothing lost if unconfigured)
- [x] Inbound webhook `POST /api/whatsapp/webhook` (shared-token secured; logs incoming messages, best-effort patient match by phone; advances outbound status on delivery/read receipts)
- [x] Prescription delivery: signed public link (`core/lib/signed-link`, HMAC+expiry) → `GET /p/rx/[token]` serves the PDF with no session; doctor "WhatsApp" button (`sendPrescriptionToWhatsApp`) delivers it
- [x] Verified: webhook 401/200 + inbound row logged + status receipt advances; public link 200 PDF, tampered token 404. Typecheck green.
- **Config to go live:** `AISENSY_API_KEY` + an approved `AISENSY_RX_CAMPAIGN` template (with a document header), `WHATSAPP_WEBHOOK_TOKEN`, `LINK_SIGNING_SECRET`, `APP_URL`. Map the template body params in `sendPrescriptionToWhatsApp`.
- Deferred: per-clinic WhatsApp numbers (currently one platform-level AiSensy account); text/session messages (template-only for now).

### 10. Recall engine ✅ _(reminder send needs WhatsApp live; booking = Step 11)_
- [x] CORE engine `core/recall` — `scheduleRecall` (capture) + `processDueRecalls` (send due reminders via the WhatsApp channel), specialty-agnostic
- [x] Capture: approving a visit schedules a recall from the note's `nextVisit {reason, afterDays}` (`approveVisit`)
- [x] Cron endpoint `GET /api/cron/recalls` (CRON_SECRET-secured; Bearer or ?token) + `vercel.json` daily schedule (09:00)
- [x] Success → recall `sent`+sentAt; provider failure leaves it `pending` to retry (attempt still logged in `whatsapp_messages`); no phone → skipped
- [x] Clinic `Recalls` page + nav item (read-only list, responsive table/cards)
- [x] Verified: cron 401 without secret; authorized run picks only DUE recalls (`{processed:1,sent:0,skipped:1}`), leaves pending + logs a failed WhatsApp row when unconfigured; recalls page renders. Typecheck green.
- **To go live:** WhatsApp configured (Step 9) + an approved `AISENSY_RECALL_CAMPAIGN` template; set `CRON_SECRET` on Vercel.
- Booking (recall `sent` → `booked`) lands with the reception panel / inbound WhatsApp reply — Step 11.

### 11. Receptionist panel (`/reception`) 🟨 _(appointments + WhatsApp queue done; payments deferred)_
- [x] Appointments: list (`/reception`, newest first, patient+doctor, status badges), schedule (`/reception/new` — patient picker + doctor + datetime + reason), status actions (confirm/complete/cancel/no-show, icon-only on mobile); mobile `+` FAB; all clinic-scoped
- [x] WhatsApp queue (`/reception/whatsapp`): inbound + outbound messages for the clinic, newest first, patient-matched (reads the Step 9 `whatsapp_messages` log)
- [x] Reception nav = Appointments + WhatsApp (shared PanelShell); responsive table→cards throughout
- [x] Actions `createAppointment` / `setAppointmentStatus` / `searchClinicPatients` (tenant-scoped)
- [x] Verified with a seeded receptionist: all three pages 200, appointment + inbound message render, Confirm action + nav present. Typecheck green.
- [ ] Payments (JazzCash/Easypaisa/Raast/Stripe) — DEFERRED (needs provider accounts; like billing). Record-a-payment can be a light follow-up.
- Booking a recall → appointment from an inbound reply: natural next connection (recall `sent` → `booked` + create appointment).

### 12. Owner dashboard ✅ — MVP finale 🎉
- [x] **"Revenue Recovered" hero metric** on the clinic-admin dashboard (`/clinic`): return
      visits driven by recall reminders × the clinic's average visit value (PKR).
- [x] "Recovered" = a recall in status `sent`/`booked`/`completed` whose patient then had a
      `completed` appointment on/after the reminder — correlated `EXISTS` (raw SQL on the same
      pool per the db/index.ts analytics policy), all clinic-scoped.
- [x] Owner setting `clinics.avg_visit_value` (migration 0009, default 3000) editable inline via
      `AvgVisitValueForm` → `updateClinicSettings` (validated, clinic-scoped).
- [x] Supporting stat cards: Return visits · Recalls sent · Upcoming appts · Patients · Staff
      (each index-backed COUNT; the last three deep-link to their pages).
- [x] Verified with a seeded clinic_admin + a recall→completed-appointment scenario: dashboard
      200, hero renders **Rs 5,000** (1 return visit × 5,000 avg), settings form present; test data
      cleaned up. Typecheck green.

---

## 🎉 MVP complete (CLAUDE.md §11 steps 1–12)

All twelve build-order steps are done. Per CLAUDE.md §11, **stop here** — do NOT build derma,
hair, mobile apps, or advanced analytics until instructed. Config left to go live (owner's task):
`ANTHROPIC_API_KEY` + `OPENAI_API_KEY` (scribe), an AiSensy account + approved
`AISENSY_RX_CAMPAIGN`/`AISENSY_RECALL_CAMPAIGN` templates + `WHATSAPP_WEBHOOK_TOKEN` +
`LINK_SIGNING_SECRET` (WhatsApp/recalls), `CRON_SECRET` (Vercel cron), and the deploy-time
scaling fixes in "Deployment & scaling". Deferred within-MVP items: billing/usage (Steps 5–6),
payments (Step 11), audit log.

---

## Deployment & scaling (DEFERRED — handle at server deploy, not now)

Current settings are deliberate MVP/local defaults. Under load (~1000 concurrent
requests) the failure points are known and must be addressed **before production**:

- **DB connection pool** — `core/db/index.ts` uses a single pool `max: 10`. On
  Vercel serverless each instance opens its own pool → concurrent instances can
  exhaust Postgres `max_connections` (~100) → "too many connections" errors.
  **Fix:** put a pooler in front (PgBouncer / Neon / Supabase pooler / Vercel
  Postgres) and tune `max` to `pooler_limit ÷ expected_instances`.
- **Login CPU / event-loop block** — `auth/password.ts` uses **bcryptjs (pure JS,
  cost 12)**, chosen for no native build on Windows. Pure-JS hashing runs *on*
  the Node event loop (~250ms each), so a burst of logins serializes and stalls
  all other requests. **Fix (prod):** native `bcrypt`/`argon2` (thread pool) or a
  worker thread, and **rate-limit `/login`** per IP.
- **No rate limiting** anywhere — add per-IP throttling (login + general) so a
  burst/abuse can't take the app down.
- **Session validation hits the DB every request** (`sessions ⋈ users`). Fine for
  now; consider a short-TTL cache if it becomes hot.
- **Verify with a real load test** against a **production build** (`npm run build
  && npm start`), not `next dev` — e.g. `autocannon` or `k6`. Dev numbers are
  meaningless for capacity.

> Reads (indexed, tenant-scoped) hold up for bursts; the real risks are login
> CPU and serverless connection exhaustion. Owner's call: tackle at deploy time.

---

## How to run the DB (local Postgres)

1. In `.env.local`, set `DATABASE_URL` — replace `YOUR_PASSWORD` with your Postgres password.
2. Create the database (once):
   `psql -U postgres -c "CREATE DATABASE flexicaai;"`
3. Apply migrations: `npm run db:migrate`
4. Seed the first super admin: `npm run db:seed` (set `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` first).
5. `npm run dev`, open http://localhost:3000, log in with the seeded admin → lands on `/admin`.

Other DB commands: `npm run db:generate` (new migration after schema change) ·
`npm run db:push` (dev-only quick sync) · `npm run db:studio` (browse data in a GUI).

> **This machine:** local Postgres runs on **port 5433** (not the default 5432); `.env.local`
> reflects that. Migration `0000` applied and a super admin is seeded — login works.

---

## Change log

| Date | Change |
|------|--------|
| 2026-07-05 | Created PROGRESS.md tracker. Confirmed empty project; at Step 1. |
| 2026-07-05 | Scaffolded Next.js 16 (TS strict, Tailwind v4, App Router, `src/`, `@/*` alias). |
| 2026-07-05 | Restored CLAUDE.md — create-next-app had clobbered it to a single `@AGENTS.md` line; full guardrails recovered, import kept. |
| 2026-07-05 | Built §3 folder tree (`/core`, `/modules/dental`, `/config`) with `.gitkeep`s. |
| 2026-07-05 | Installed `@supabase/supabase-js`, `@supabase/ssr`, `zod`. Wired browser+server Supabase clients and validated env. |
| 2026-07-05 | Initialized shadcn/ui; relocated `button.tsx`→`core/ui`, `utils.ts`→`core/lib` to honor §3. |
| 2026-07-05 | **Step 1 complete** — typecheck + `next build` green. Next: Step 2 (Auth). |
| 2026-07-05 | Git 2.55 installed; repo initialized (`main`). Added `.gitattributes` (LF normalization) and `.idea` ignore. |
| 2026-07-05 | Pushed to GitHub: https://github.com/bilalaziz456/klenic (origin/main). |
| 2026-07-06 | **Step 2 (Auth) complete** — roles, login/signup, session proxy, route protection, role guards, placeholder panels. Module-agnostic (authorizes by role, never specialty). Typecheck + build green. |
| 2026-07-06 | Next 16 learning: `middleware.ts` convention deprecated → renamed to `src/proxy.ts` (`export function proxy`), per bundled docs. |
| 2026-07-06 | Fixed Grammarly hydration warning (`suppressHydrationWarning`); set FlexicaAI metadata; `/` now redirects to `/login`. |
| 2026-07-06 | **Decision revised:** removed public signup entirely — accounts are admin-provisioned only (Super Admin → clinics + clinic admins; clinic admin → staff). Deleted `/signup` page/form/action + login link; specialty checkboxes move to Step 5. Build green. |
| 2026-07-06 | **Major rework: Supabase → local PostgreSQL + Drizzle + custom session auth.** Removed `@supabase/*`; added drizzle-orm/pg/bcryptjs/drizzle-kit/tsx/dotenv. New DB layer (`core/db/index.ts` + `schema.ts`), custom auth (`session.ts`/`password.ts`/`constants.ts`), Edge-safe proxy, seed script. Generated migration `0000_*`. Updated CLAUDE.md §2/§5/§8/§10/§11. Typecheck + build green. |
| 2026-07-06 | **DB live end-to-end.** Postgres on port 5433; created `klenic` DB, applied migration `0000`, seeded super admin. Verified: `/login` 200, `/admin` 307→login, form renders. Login works in browser. |
| 2026-07-06 | **Step 3 (Core DB schema) complete.** Added `patients`, `appointments`, `visits`, `recalls` (module-tagged, status enums, JSONB note, indexed by clinic_id). Added `byClinic()` tenant helper. Migration `0001` applied → 7 tables. Typecheck + build green. |
| 2026-07-06 | **Step 4 (Module registry) complete.** `ModuleDefinition` contract + dental module (scribe prompt, recall rules, PK drug formulary) + `/config/modules.ts` registry with specialty catalog (dental available; derma/hair coming_soon). Golden rule verified: /core imports no module. Build green. |
| 2026-07-06 | **Step 5 (Super Admin panel) complete.** `/admin` clinics list, create-clinic-with-admin (transaction) + specialty checkboxes from registry, clinic detail with module toggles + staff. Added shadcn checkbox/table/badge. Verified authed pages render 200 (Dental selectable, derma/hair coming soon). Build green. Billing deferred. |
| 2026-07-06 | **Super Admin management tier complete.** `must_change_password` (migration 0002) + forced `/change-password` flow; reset staff password (revokes sessions); rename clinic; suspend/reactivate accounts; delete clinic (confirm-by-name, cascades). Verified force-change redirect (flag on → /admin 307 → /change-password 200). Build green. Auth bits reused by Step 6. |
| 2026-07-06 | **Login switched from email → username.** Added `users.username` (unique, migration 0003, backfilled admin@klenic.local → `admin`); email now optional contact. Updated login/create-clinic forms, signIn, seed (`SEED_ADMIN_USERNAME`), displays. Verified: login form shows Username, credential check passes (admin/change-me-now). Login now: **admin** / change-me-now. |
| 2026-07-06 | **UI polish.** Reusable `PasswordInput` with show/hide eye (login, change-password, create-clinic). Font → Plus Jakarta Sans (the `--font-sans` var was previously unwired → browser default). Teal brand palette in light+dark (primary/ring/accent/charts). Admin top bar: removed duplicate "Admin" on the left (now "FlexicaAI" wordmark), username pill on right. Colored auth backgrounds. Verified via curl. |
| 2026-07-06 | **Edit clinic staff.** Super admin can edit a staff member's full name + username (unique-checked, `updateStaffProfile`) from the clinic detail page, alongside the existing rename-clinic. Verified staff row + Edit control render (200). Build green. |
| 2026-07-06 | **Per-account dark/light/system theme.** `users.theme` enum (migration 0004, default system) + mirrored `klenic_theme` cookie; no-flash inline script applies `.dark` before paint and follows OS for "system". `ThemeToggle` (System/Light/Dark) in headers persists via `setThemePreference` action + cookie; login seeds the cookie from the account's saved theme. Verified: dark→html.dark, light→none, script present. Root layout now dynamic (reads cookie). |
| 2026-07-06 | **UI/UX fixes.** Keyed inputs to kill Base UI defaultValue warning (staff/rename edits); Chrome autofill kept on-theme in dark (unlayered box-shadow); unified solid input surface (`--input-bg`) so inputs look identical everywhere; clearer dark borders; create-clinic now redirects to the list. |
| 2026-07-06 | **Clinics search + perf rule.** Name search on `/admin` (URL `?q=`, debounced, case-insensitive). Saved standing memory: build every data op for fastest response. Added pg_trgm GIN index on `clinics.name` (migration 0005). |
| 2026-07-06 | **Step 6 (Clinic Admin panel) complete.** `/clinic` dashboard (counts) + staff (add doctor/receptionist, suspend/reset) + patients (add, search by name/phone). All clinic-scoped via `byClinic()`. Trigram indexes on patients name/phone (migration 0006). Verified 200s + role isolation. |
| 2026-07-07 | **UI: brand logo + panel chrome.** Vector-traced `logo2.png` → theme-aware SVGs (`logo.svg`/`logo-dark.svg`) with refined Plus Jakarta Sans tagline; icon-only transparent favicon set (`app/icon.svg`/`favicon.ico`/`apple-icon.png`). Locked exact logo colors into theme tokens (`--brand-teal #0FB4BB`, `--brand-blue #069FC5`, `--brand-navy #082957`; primary/ring/charts + `bg-brand-gradient`). Responsive shells: desktop sidebar + mobile hamburger drawer (animated slide/fade) for `/clinic` + `/admin`, icon+text nav, sign-out icon-only on mobile. Added `loading.tsx` boundaries (spinner) so nav doesn't linger on the old page. Mobile FAB for "New clinic". |
| 2026-07-07 | **Scaling review (deferred to deploy).** Analyzed ~1000-request behavior; documented deploy-time fixes in "Deployment & scaling" section: pool `max:10` → needs a connection pooler on serverless; bcryptjs (pure-JS) login blocks the event loop → native bcrypt/argon2 + login rate-limit; no rate limiting yet; load-test on a prod build. Owner: handle at server deploy. |
| 2026-07-07 | **Step 7 (voice scribe) complete.** Generic CORE scribe: `core/integrations/storage` (local fs, clinic-namespaced), `core/ai/prompt-runner` (Anthropic SDK, `claude-sonnet-4-6`, safe JSON parse — Sonnet 4.6 lacks `output_config.format`), `core/ai/scribe-engine` (Whisper transcribe + note-gen). `POST /api/ai/scribe` (doctor-guarded, tenant-scoped) uses the clinic's MODULE prompt → saves a DRAFT visit. Doctor UI: `/doctor` shell + `ScribeWorkspace` (MediaRecorder → review → approve) + generic `NoteEditor`. Migration 0007: `visits.ai_draft`+`audio_key` (flywheel). Drug-formulary warnings. Verified end-to-end (auth→patient→module→storage→AI-key gate) via a minted doctor session; typecheck green. **Needs `ANTHROPIC_API_KEY` + `OPENAI_API_KEY` in `.env.local` to run live.** Added `@anthropic-ai/sdk`. |
| 2026-07-07 | **Nav unified + delete/suspend UX + Step 8 (prescription PDF).** All four panels use one shared `PanelShell` (sidebar/hamburger); doctor+reception got the shell; deleted per-panel shells. Clinic staff/patients pages match the admin flow (list-first + search + `/new` page + mobile `+` FAB; table→cards on mobile with icon actions); same responsive cards+icons on the super-admin clinic-detail staff table. Delete flows: reusable `ConfirmDeleteDialog` (portal modal, step-up password re-auth via `core/auth/reauth`, autofill-proof text field with eye toggle) on staff+clinic deletes. Suspend/reactivate a clinic_admin cascades to all clinic staff (bidirectional, sessions revoked on suspend). Login now shows a distinct "suspended" message after the password verifies. Fixed duplicate-username crash (`isUniqueViolation` walks the drizzle `.cause` chain). **Step 8:** `core/lib/prescription-pdf.ts` (pdf-lib) + `GET /api/prescriptions/[visitId]` → PDF from the approved visit note, clinic-scoped, formulary-validated; doctor "Prescription" link. Verified: PDF text decoded + checked (patient/doctor/diagnosis/Rx/advice/footer). Added `pdf-lib`. Typecheck green. |
| 2026-07-07 | **Step 9 (WhatsApp) — infra.** CORE AiSensy client (`core/integrations/whatsapp`, template Campaign API, config-gated), `whatsapp_messages` log (migration 0008), notification channel (`core/notifications/whatsapp`, record-then-send), inbound webhook (`/api/whatsapp/webhook`, token-secured, logs inbound + advances outbound status on receipts, best-effort patient match). Prescription delivery: HMAC signed public link (`core/lib/signed-link`) → `/p/rx/[token]` serves the PDF session-free; doctor "WhatsApp" button. Env: APP_URL, AISENSY_*, WHATSAPP_WEBHOOK_TOKEN, LINK_SIGNING_SECRET (dev values added to .env.local). Verified end-to-end (webhook 401/200 + logged, status receipt → delivered, public link 200 PDF, tampered → 404). Needs an AiSensy account + approved template to send live. Typecheck green. |
| 2026-07-07 | **Step 10 (recall engine).** CORE `core/recall`: `scheduleRecall` (capture) + `processDueRecalls` (sends due reminders via the WhatsApp channel, specialty-agnostic). Capture wired into `approveVisit` from the note's `nextVisit`. Cron `GET /api/cron/recalls` (CRON_SECRET, Bearer/?token) + `vercel.json` daily 09:00. Success → recall `sent`; failure → stays `pending` (retry) with the attempt logged; no phone → skipped. Clinic `Recalls` page + nav (responsive table/cards). Verified: cron 401 unauthorized, authorized run `{processed:1,sent:0,skipped:1}` on a seeded due recall, pending retained + failed WhatsApp row logged when unconfigured; recalls page 200. Env: AISENSY_RECALL_CAMPAIGN, CRON_SECRET. Typecheck green. |
| 2026-07-07 | **Step 11 (receptionist panel) — appointments + WhatsApp queue.** `/reception` appointments list (patient+doctor, status badges, confirm/complete/cancel/no-show icon actions, responsive table→cards, mobile + FAB); `/reception/new` schedule form (patient picker + doctor select + datetime + reason); `/reception/whatsapp` queue (inbound+outbound from the Step 9 log, patient-matched). Reception nav = Appointments + WhatsApp. Actions: createAppointment / setAppointmentStatus / searchClinicPatients (tenant-scoped). Verified with a seeded receptionist: all pages 200, appointment + inbound message render, actions + nav present. Payments DEFERRED (provider accounts). Typecheck green. |
| 2026-07-09 | **Scheduling form adapts to the doctor's hours.** The new-appointment form (shared by reception + clinic admin) now splits Date and Time: for a doctor with set hours (not flexible), the Time field is a **dropdown of valid slots** within that doctor's working window for the chosen date (30-min steps, past slots on today filtered out); for a flexible doctor or "Any doctor", it's a **free time picker** (hour/min/AM-PM). Off-day/leave shows "Doctor doesn't work that day". `doctorDayAvailability` now returns `flexible` + the day's `window`; Submit is disabled until a valid slot is chosen. Extracted `TimeSelect` to `core/ui/time-select.tsx` (shared by the schedule editor + booking form). Suite 47/47; both modes verified visually at mobile width. |
| 2026-07-10 | **Specific-hours doctor → time is radio buttons of the visiting windows** (not a slot dropdown). When creating an appointment, a non-flexible doctor shows one radio per working window for the day ("9:00 AM – 12:00 PM", "4:00 PM – 7:00 PM"); the appointment books at the chosen window's start. A single window shows one (pre-selected) radio. The free hour/min/AM-PM dropdown now shows ONLY for flexible / "Any" doctors. Past windows are dropped for today. Suite 47/47. |
| 2026-07-10 | **Appointment status is a dropdown (any status → supports undo).** Replaced the per-status action buttons with a single status `<select>` (Scheduled / Confirmed / Completed / Cancelled / No-show) on the lists + detail page — so a status can be changed to anything, including **undo** (e.g. Confirmed → Scheduled). `setAppointmentStatus(id, status)` is now a direct action (no form), applied via `useTransition`; it no-ops when the status is unchanged, and patient notices (cancel / whatsapp-confirm) fire only on a real transition (no duplicates). E2E updated to assert the dropdown + full status set: 50/50. |
| 2026-07-10 | **Appointments open to a detail page (edit + delete).** Each appointment row (clinic admin + reception) now has **Open** → a detail page (`/clinic/appointments/[id]` and `/reception/appointments/[id]`, shared `AppointmentDetail` body). There the clinic admin/receptionist can change the **doctor / date / time / duration / reason** (`updateAppointment`, enforced by `checkDoctorSlot` — out-of-hours rejected), run the **status actions** (confirm/complete/cancel/no-show), and **delete** the appointment (`deleteAppointment`, step-up password). Time uses the free picker; the server enforces the doctor's hours. Clinic-scoped (no cross-tenant leak). E2E: both lists open, detail renders for both roles, edit persists, tenant isolation — 50/50. |
| 2026-07-10 | **Patients list opens to a full detail page (edit + delete).** Each patient row now has **Open** → `/clinic/patients/[id]`, matching the staff/clinic "open to manage" pattern. The detail page lets the clinic admin **edit** all fields (`updatePatient`) and **delete** the patient (`deletePatient`, step-up password; cascades appointments/visits/recalls), and shows the patient's recent appointments (read-only). Clinic-scoped via `byClinic` — a foreign patient's data isn't shown (verified: no cross-tenant leak). E2E: list→Open, detail renders, edit persists, tenant isolation — 49/49. |
| 2026-07-10 | **Reverted the date dropdown → native date input; allow past dates.** The appointment Date field is the native `<input type="date">` again (no upcoming-dates dropdown, no min), so staff can record appointments on **previous dates** (e.g. logging a walk-in that already happened). Backend already allowed it (`createAppointment` only rejects invalid dates). Removed the today-only window filtering. Verified 4/4 (past-date appointment created + stored; native date input). |
| 2026-07-10 | **Multiple time windows per day (split shifts).** A weekday can now have several working windows (e.g. Mon 09:00–12:00 AND 16:00–19:00). Core `isDoctorAvailableAt` checks ALL windows for the weekday; `windowsForWeekday` + `describeAvailability` handle multiples; removed the "one window per weekday" validation. The schedule editor lets you **add/remove multiple time ranges per day** ("+ Add another time"). The appointment slot list generates slots across all windows (and skips the gap); booking is blocked in the gap. `doctorDayAvailability` now returns `windows[]`. Verified 9/9 (two windows enforced incl. the gap; edit removes a window; slots span both). **Fix:** invalid ranges (end ≤ start) are now validated **client-side** with an inline error + disabled submit, so a bad submit no longer round-trips the server and wipes the form (the React-19 form-reset that made it look like saved data was lost). |
| 2026-07-09 | **Scheduling form: strict specific/flexible split.** The doctor's `flexible_hours` is now passed with the doctor list, so the form knows up front (before a date is picked) which picker to show: a **flexible/"Any" doctor gets the free time picker**; a **specific-hours doctor NEVER shows the free picker** — only the visiting-hours slot list (with "Pick a date to see available times" before a date is chosen). Fixes the free picker briefly showing for a specific doctor pre-date. |
| 2026-07-09 | **Per-doctor "Flexible hours" toggle** (`users.flexible_hours`, migration 0016). When ON, the doctor can be booked at ANY time — working hours are not enforced (leave and the daily cap still apply); ON is how "no restriction" is expressed now. When OFF, appointments (staff, WhatsApp self-book, reschedule) may only be made during the doctor's visiting hours, and a doctor with no hours set + not flexible is not bookable ("no visiting hours set"). Checkbox in the doctor schedule editor (`DoctorScheduleFields`); enforced in the shared `checkDoctorSlot`; reflected in the "slots left" hint. Verified 8/8 (flexible books any time incl. off-days/3am; strict blocks off-day/after-hours/no-hours; leave still overrides flexible; createStaff persists the flag). Suite 47/47. |
| 2026-07-09 | **Reschedule wording/status fix (light).** A WhatsApp reschedule now (a) **preserves the appointment's status** — a `confirmed` appointment stays confirmed after a move (it's not a new request needing re-approval), instead of being downgraded to `scheduled`; and (b) sends an accurate **"Your appointment has been rescheduled to \<time\> with Dr X. Fee: Rs …"** message via the generic reschedule reply, instead of the previous "Appointment confirmed …" (which was misleading). `checkDoctorSlot` now also returns the fee. Decided NOT to make reschedule pending-until-confirm (unlike new bookings) — a move to an already-validated slot shouldn't add staff re-approval. Verified 6/6 (confirmed stays confirmed, scheduled stays scheduled, correct message, no booked-notice). Suite 47/47. |
| 2026-07-09 | **WhatsApp booking is pending until staff confirm.** A WhatsApp self-booking is now a *request*: the appointment is tagged `appointments.source='whatsapp'` (migration 0015) and the patient gets a "booking request received — the clinic will confirm shortly" ack (not a premature "confirmed"). When the receptionist/clinic admin **Confirms** it, `setAppointmentStatus` sends the patient the confirmation with slot/timing/doctor/fee (`notifyAppointmentBooked`). Staff-created bookings (`source='staff'`) still confirm immediately at booking and are NOT re-notified on confirm (gated by source + prior status). Verified 7/7: whatsapp booking → pending ack + no confirmation; staff confirm → confirmation with Dr + fee; staff booking → immediate + no double. Suite 47/47. |
| 2026-07-09 | **Patient self-booking via WhatsApp** (respects doctor visiting hours). A matched patient can text e.g. "book with Dr Khan monday 3pm"; the webhook runs `handleBookingReply` (`core/appointments/booking.ts`) after the reschedule check: resolves the doctor (named / the clinic's only doctor / else asks which, listing each doctor's hours), parses date+time, validates against the doctor's hours/leave/daily cap (`checkDoctorSlot`), creates the appointment (status `scheduled`), and confirms — or replies with the hours / the reason. Requires an explicit date AND time; "cancel" messages are excluded. New env `AISENSY_BOOKING_REPLY_CAMPAIGN`. Verified end-to-end (webhook): valid slot creates the appt, off-day refused with hours, no-date → guidance-with-hours, two-doctor selection by name, cancel-not-booked — 6/6; suite 47/47. |
| 2026-07-09 | **Reschedule: weekday parsing.** `parse-when.ts` now resolves weekday names (full + abbreviations, optional "next"/"this") to the next upcoming occurrence — e.g. "reschedule next monday 3pm". Word-boundary matching so "next month" is NOT read as Monday. Verified 9/9 (all weekdays, no-time-keeps-existing, natural phrasing, negative case). |
| 2026-07-09 | **Reschedule: broadened intent.** `isRescheduleIntent` now also fires on "postpone" and move/change/shift/rebook when the message names an appointment/booking/slot/visit (and NOT on unrelated "I'll move to Lahore"). Verified across 15 date-format + phrasing cases. |
| 2026-07-09 | **UI: consistent time picker.** Replaced the native `<input type="time">` (inconsistent clock icon / clipped AM-PM across devices) with hour/minute/AM-PM `<select>` dropdowns in the doctor schedule editor; stored value stays "HH:MM" 24h. |
| 2026-07-08 | **UI polish: mobile schedule editor + dashboard links.** Doctor working-days rows stack cleanly on mobile; avg-visit-value Save button aligned + status centred; keyed the input to kill the Base UI defaultValue warning; dashboard "Upcoming appts" → `/clinic/appointments`, "Recalls sent" → `/clinic/recalls`. |
| 2026-07-09 | **Patient self-service reschedule via WhatsApp reply.** A matched patient can reply e.g. "reschedule 12 Jul 3pm"; the inbound webhook runs `handleRescheduleReply` (core/appointments): finds their next upcoming appointment, parses the date/time (`parse-when.ts`, heuristic — ISO / DD-MM / "12 Jul" / today|tomorrow + am-pm|24h), validates the new slot against the doctor's leave/hours/daily cap, moves the appointment (resets the reminder), and confirms with full details — or replies with clear guidance / the reason it couldn't. Extracted the booking slot-validation into shared `core/appointments/availability.ts` (`checkDoctorSlot`) so booking AND reschedule enforce identical rules (createAppointment now uses it too). New env `AISENSY_RESCHEDULE_CAMPAIGN`. Verified end-to-end (webhook): valid slot moves the appt + confirmation, off-day slot refused with reason, no-date → guidance — 8/8; suite 46/46. |
| 2026-07-09 | **Day-before appointment reminder (WhatsApp, daily cron).** `sendDueAppointmentReminders(now)` in `core/notifications/appointment.ts` scans active appointments scheduled TOMORROW that aren't reminded yet (patient has phone) and sends a WhatsApp reminder with doctor + time. Idempotent via new `appointments.reminder_sent_at` (migration 0014; stamped on success, left null to retry on failure). Cron `GET /api/cron/reminders` (CRON_SECRET, Bearer/?token — same shape as recalls) + `vercel.json` daily 18:00. New env `AISENSY_REMINDER_CAMPAIGN` (default `appointment_reminder`; params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} clinic). Verified end-to-end: only tomorrow's appt reminded ("Reminder: your appointment with Dr X is on Fri 10 Jul, 10:00."); today / day-after / already-reminded / phone-less all skipped — 7/7; suite 45/45. |
| 2026-07-09 | **WhatsApp booking confirmation to patients** (doctor + working hours + fee + time). `notifyAppointmentBooked(clinicId, apptId)` in `core/notifications/appointment.ts`, wired into `createAppointment` after insert. Best-effort/clinic-scoped/phone-only, same as the cancel notice. New env `AISENSY_BOOKING_CAMPAIGN` (default `appointment_booked`; params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} hours/{{5}} fee/{{6}} clinic). Body e.g. "Appointment confirmed with Dr Book on Mon 13 Jul, 10:00. Working hours: Mon 09:00–17:00…. Fee: Rs 2,000. — Clinic". Verified 6/6; suite 43/43. |
| 2026-07-09 | **WhatsApp notice to patients when an appointment is cancelled** (with doctor name + time). New CORE `core/notifications/appointment.ts` `notifyAppointmentsCancelled(clinicId, ids)` — best-effort, clinic-scoped, only messages patients with a phone, never throws (a notify failure can't undo the cancellation). Wired into BOTH cancellation paths: manual `setAppointmentStatus(...,'cancelled')` and bulk cancel from `addDoctorLeave`. Uses the standard WhatsApp channel (logs every attempt, no-ops gracefully when unconfigured); new env `AISENSY_CANCEL_CAMPAIGN` (default `appointment_cancelled`, params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} clinic). Verified end-to-end: manual + leave cancels each create an outbound notice ("Your appointment with Dr X on Mon 13 Jul, 10:00 has been cancelled."), phone-less patients get none — 6/6; suite 43/43. |
| 2026-07-08 | **Doctor leave / vacation days.** New `doctor_leaves` table (clinic-scoped date range per doctor, migration 0013). Receptionist (`/reception/doctors`) and clinic admin (`/clinic/staff/[id]`) set leave via the shared `DoctorLeaves` component → `addDoctorLeave`/`removeDoctorLeave` (both roles). Adding a leave **cancels** the doctor's active (scheduled/confirmed) appointments in the range (reports the count); booking is **hard-blocked** on leave days (checked in `createAppointment` before hours/limit), and the schedule form shows "on leave that day". Verified end-to-end with real actions: add-leave cancels the appt, booking blocked on leave day / allowed on non-leave day, remove-leave unblocks — 9/9; permanent suite 43/43. |
| 2026-07-08 | **Per-doctor consultation fee + staff "Open" → full management page.** Added `users.consultation_fee` (int PKR, migration 0012), set/edited alongside the doctor schedule (`DoctorScheduleFields` fee input; parsed in `createStaff`/`updateDoctorSchedule`). Reworked the clinic staff list: each row now shows a single **Open** button (no inline action cluster) → `/clinic/staff/[id]`, which became a full management page for any staff — edit profile (name/username), doctors' schedule + daily cap + fee, reset password, suspend/reactivate, and delete (delete now redirects to the list). Removed the old inline `staff-actions.tsx`. Verified end-to-end with real server actions: create-with-fee, edit fee, edit profile, suspend, reset password (9/9); permanent suite 43/43. |
| 2026-07-08 | **Doctor scheduling: working hours + daily appointment cap, enforced on booking.** New core `core/lib/availability.ts` (per-weekday `DayAvailability`, `isDoctorAvailableAt`, `describeAvailability`, `dayBounds`; specialty-agnostic). Schema: `users.availability` jsonb + `users.daily_appointment_limit` int (migration 0011). Clinic admin sets custom per-day hours + daily cap when **creating** a doctor (`DoctorScheduleFields` in add-staff) and edits them at `/clinic/staff/[id]` (`updateDoctorSchedule`). Booking (`createAppointment`, shared by receptionist + clinic admin) **hard-blocks** off-day / out-of-hours / over-cap bookings (0 = unlimited); the schedule form shows live "N of M appointments left" via `doctorDayAvailability`. Receptionist adjusts caps at `/reception/doctors` (`setDoctorDailyLimit`, both roles). Verified end-to-end (real server actions): create-with-schedule, off-day/out-of-hours/over-limit blocks, limit→0 unblocks — 10/10; permanent suite render checks 43/43. |
| 2026-07-08 | **Clinic Admin can now MANAGE appointments (not just view).** Generalized the appointment actions (`createAppointment`/`setAppointmentStatus`/`searchClinicPatients`) to accept `receptionist` OR `clinic_admin` via a shared `requireAppointmentsAccess()` that routes revalidate/redirect back to the caller's own panel (clinic admin → `/clinic/appointments`, receptionist → `/reception`). `/clinic/appointments` upgraded to the full manager (all appts newest-first, confirm/complete/cancel/no-show, New button + mobile FAB) reusing the reception `AppointmentActions` + `NewAppointmentForm`; added `/clinic/appointments/new`. Tenant scoping unchanged (`byClinic`). E2E: manage controls render + schedule page + clinic-B isolation + reception regression, 39/39. |
| 2026-07-08 | **Clinic Admin: read-only upcoming-appointments view.** New `/clinic/appointments` (clinic-scoped via `byClinic()`, lists scheduled/confirmed appts from now on, soonest first, responsive table→cards, no scheduling actions — reception still owns those). Added "Appointments" to the clinic nav; the dashboard "Upcoming appts" card now links here (previously pointed at `/reception`, which the role guard bounced clinic admins away from → looked like a refresh). E2E: lists upcoming appt + tenant-scoped, 38/38. |
| 2026-07-08 | **Revenue dashboard = super-admin-gated, specialty-agnostic feature.** New core `clinics.features_enabled` text[] (migration 0010) + `core/lib/features.ts` (`CLINIC_FEATURES`, `clinicHasFeature`) — optional platform features, off by default, working across dental/derma/hair alike (core never hardcodes a specialty). Super admin toggles "Revenue dashboard" per clinic on the clinic detail page (`FeaturesForm` → `updateClinicFeatures`); the clinic dashboard shows the Revenue Recovered hero + avg-visit-value setting (and the "Return visits" stat) ONLY when enabled, and skips the recovered analytics query when off (perf-first). E2E extended to cover feature ON (shown) + OFF (hidden) + the admin toggle: 36/36. |
| 2026-07-08 | **E2E smoke test harness** (`scripts/e2e.mjs`, `npm run test:e2e`). Seeds a throwaway two-clinic world + real SHA-256 sessions, exercises every panel + API route over HTTP (auth, role isolation, tenant scoping, Revenue Recovered, prescription PDF + signed link, WhatsApp webhook, recall cron, scribe unconfigured/tenant, bcrypt round-trip), then cleans up (incl. orphaned audio). Full pass **35/35**. Note: the recurring Turbopack "Jest worker" dev 500 on cold route compile is environmental — clear `.next` + restart, not a code bug. |
| 2026-07-07 | **Step 12 (owner dashboard) — MVP finale.** `/clinic` dashboard rebuilt around a "Revenue Recovered" hero: return visits from recall reminders × `clinics.avg_visit_value` (migration 0009, default 3000, editable via `AvgVisitValueForm` → `updateClinicSettings`). "Recovered" = a `sent`/`booked`/`completed` recall whose patient later had a `completed` appointment on/after the reminder (correlated `EXISTS`, raw SQL on the same pool, clinic-scoped). Supporting stat cards (Return visits, Recalls sent, Upcoming appts, Patients, Staff). Verified with a seeded clinic_admin + recall→completed-appt scenario: dashboard 200, hero = Rs 5,000 (1×5000), settings form present; data cleaned up. Typecheck green. **All 12 MVP steps complete — stop per CLAUDE.md §11.** |
| 2026-07-10 | **Appointment edit form now uses the create form's adaptive visiting-hours UI.** Generalized `NewAppointmentForm` to serve both create and edit (props `appointmentId`/`fixedPatient`/`initial`); in edit mode it prefills fields, fixes the patient, and saves via `updateAppointment`. Opening an appointment (`/clinic|/reception/appointments/[id]`) now shows the same doctor-aware time picker as creation — radio buttons of the doctor's visiting-hours window(s) for a specific-hours doctor, a free time picker only for flexible/"Any" doctors — with the doctor prefilled and hours enforced on save. `edit-appointment-form.tsx` trimmed to just the delete button. Verified end-to-end (real server actions): create still books, edit page renders the prefilled radio form, edit saves time/reason/duration, out-of-hours edit rejected — 4/4; permanent suite 50/50. |
| 2026-07-10 | **Fix: appointment edit radio/fields reverting after Save.** Selecting a second visiting-hours window (e.g. 4–7 PM), saving, then seeing the radio snap back to the first window (9–12) was React 19's automatic post-action `<form>` reset: native `form.reset()` unchecks the controlled radios and clears inputs, and React skips re-writing the DOM (props unchanged from the prior render), desyncing the selection. `NewAppointmentForm` now remounts its field group (`key={fieldsKey}`, bumped once each submit settles) so the DOM is rebuilt from live state, and duration/reason are controlled (were `defaultValue`, so they were silently reverting too). Typecheck green; suite 50/50. |
| 2026-07-10 | **Fix: brief flash of the wrong window on Save (supersedes the remount hack).** The prior remount corrected the desync a frame *after* React 19's post-action form reset, so the first window flashed selected before snapping to the chosen one. Root cause removed instead: the visiting-hours selector is now **buttons** (`role="radio"`, React-state only) rather than `<input type="radio">`, so native `form.reset()` has nothing to reset and the selection can't flash — the value is still carried by the hidden `scheduledAt`. Dropped the `fieldsKey` remount; duration/reason stay controlled (immune to the reset). Typecheck green; suite 50/50. |
| 2026-07-10 | **Theme-aware native controls (date picker etc.).** The appointment date field (native `<input type="date">`) — and every other native control — ignored the app theme: in dark mode the calendar icon was dark-on-dark and the picker popup rendered light. Set `color-scheme` on `:root` (light) / `.dark` (dark) so native date pickers, `<select>` popups, spinners and scrollbars follow the theme, and `accent-color: var(--primary)` so the picker's selected-day highlight uses the brand teal. Class-based (`.dark` on `<html>`), so the "system" preference is covered too. Global, so DOB / doctor-leave date fields and all selects match as well. Suite 50/50. |
| 2026-07-10 | **Themed date picker for appointment create/edit.** The native `<input type="date">` popup is drawn by the OS/browser and can't be themed (background/font/accent), so it clashed with the app UI. Added `core/ui/date-picker.tsx` — a calendar popover built on Base UI `Popover` + a hand-rolled month grid, styled entirely with app tokens (`bg-popover`, `border-border`, `bg-primary` selected day, `ring-primary/40` today, muted adjacent-month days), with prev/next month, Today and Clear. Controlled, value stays `YYYY-MM-DD`, past dates still allowed. Swapped into `NewAppointmentForm` (create + edit). Typecheck green; suite 50/50. (DOB / doctor-leave date fields still use native inputs — can adopt this component later for full consistency.) |
| 2026-07-13 | **Unified permission-driven workspace (supersedes the per-panel model).** Delivers the rule "grant any module → it appears in that user's nav and works", for EVERY user — fixing that a doctor granted appointments/patients previously had nowhere to use them. All clinic staff now share ONE workspace at `/clinic`: nav is built from `accessibleResourceIds(user)` and each page is gated by a new `requireWorkspace(resource, action)` guard (any clinic staff + the per-resource permission). The three modules that lived only in the old panels were mounted into `/clinic` via shared reused components — **Voice scribe** (`/clinic/scribe`, `ScribePanel`), **WhatsApp** (`/clinic/whatsapp`, `WhatsappQueue`), **Doctors/leave** (`/clinic/doctors`, `DoctorsPanel`) — joining the already-shared appointments/patients/procedures/sales/recalls (the clinic pages became thin wrappers → big de-dup). `ROLE_HOME_ROUTE` + `ROUTE_ROLE_ACCESS` route every clinic role to `/clinic`; the old `/doctor` and `/reception` layouts **redirect into** `/clinic` (folded, old links still land), and the shared action `home`s point at `/clinic/*`. Result: **bilal** (doctor, granted appts+patients) logs in and sees Voice scribe + Appointments + Patients + WhatsApp and uses them all; a default doctor gets scribe + view-only appts/patients; a clinic admin sees everything. Staff + settings management stays clinic-admin-only for now (privilege-escalation nuance; removed from manager defaults). Verified live (bilal/admin) + e2e updated to workspace routes → 52/52; tsc clean. |
| 2026-07-13 | **ACL cross-over: Patients in the doctor panel.** Same per-panel approach as appointments. Extracted the clinic patient list/detail/new into shared `PatientsList` / `PatientDetail` / `NewPatientPanel` (clinic pages are now thin wrappers). New `/doctor/patients`, `/doctor/patients/[id]`, `/doctor/patients/new`, each gated by the doctor's `patients` permission (view→list/detail, create→new + Add button, edit→edit form (read-only otherwise), delete→Danger zone). The patient actions (`createPatient`/`updatePatient`/`deletePatient`) are now permission-based via a shared `requirePatientAccess(action)` guard with a role-aware `home` (doctor → /doctor/patients). Doctor-panel nav shows **Patients** only when accessible. Verified 8/8 (bilal manages patients; default doctor bounced; clinic-admin pages intact) + suite 50/50; tsc clean. **bilal (doctor) now has full Appointments + Patients from his own panel** — the original request works. |
| 2026-07-13 | **ACL cross-over: Appointments in the doctor panel (per-panel model).** Fixes "granted bilal (a doctor) appointment access but he can't see it" — because panels were role-siloed (the doctor panel only had the scribe). Chose the per-panel cross-over approach: extracted the reception appointments list into a shared, path-parameterised `AppointmentsList` (+ shared `NewAppointmentPanel`); the reception page is now a thin wrapper. New `/doctor/appointments` (list), `/doctor/appointments/[id]` (reuses `AppointmentDetail`), `/doctor/appointments/new` — each gated by the doctor's `appointments` permission (view→list/detail, create→new + New button, edit→status/edit, delete→delete). `requireAppointmentsAccess` now accepts `doctor` with the correct `home` (`/doctor/appointments`). Doctor-panel nav shows **Appointments** only when the doctor can access it. Verified 8/8 (bilal sees + uses appointments; a default doctor has no nav + is bounced) + suite 50/50; tsc clean. Patients cross-over next. |
| 2026-07-13 | **ACL enforcement — procedures, leave, WhatsApp (reception panel; enforcement complete).** `procedures` gated end-to-end: the shared `requireProcedureAccess(action)` guard checks `can(user,"procedures",<action>)` for create/edit/delete/import; the `ProceduresPanel` denies the view without `procedures:view` and passes `{create,edit,delete}` to the manager so the Add form, Save, Delete, and row inputs hide/disable per permission. `leave`: `addDoctorLeave`→create, `removeDoctorLeave`→delete, `setDoctorDailyLimit`→edit; the reception Doctors page requires `leave:view`. `whatsapp`: the reception queue page (read-only) requires `whatsapp:view`. Verified 9/9 (full manager manages everything; a procedures-view-only manager sees the catalog but no Add form; an appointments-only manager is denied procedures view + bounced from whatsapp/doctors) + suite 50/50; tsc clean. **The per-user CRUD ACL is now enforced across every resource that a non-admin can reach** (appointments, clinical, prescriptions, procedures, leave, whatsapp). `patients`/`recalls`/`sales`/`settings`/`staff` live only in the clinic-admin panel (short-circuits to all-access), so their grants are set-able but inert until managers are given clinic-panel access — a future scope decision. |
| 2026-07-13 | **ACL enforcement — doctor panel (clinical + prescriptions).** `clinical` and `prescriptions` permissions now enforced. **Server**: `approveVisit` / `discardDraft` and the scribe route (`/api/ai/scribe`) require `clinical:create`; `sendPrescriptionToWhatsApp` requires `prescriptions:create`; the authenticated PDF route (`/api/prescriptions/[visitId]`) is now permission-driven (`prescriptions:view`) instead of a role list. **UI** (`/doctor`): the scribe workspace is replaced with a "no permission" notice without `clinical:create`; Recent notes hide without `clinical:view`; the Prescription link hides without `prescriptions:view`; the Send-Rx button hides without `prescriptions:create`. Receptionist defaults gained `prescriptions:view` to preserve their existing PDF access. Verified across 3 doctor profiles (full / view-only-clinical / clinical-but-no-rx) — 12/12 (UI gating + scribe 403 + PDF 403) + suite 50/50; tsc clean. Sales/settings/staff (the other "sensitive" resources) live in the clinic-admin panel which short-circuits all, so there's nothing to enforce there until managers get clinic-panel access. |
| 2026-07-13 | **ACL enforcement — nav gating + appointment permissions (Phase 2 start).** Permissions now actually *take effect*. **Nav gating**: `PanelShell` nav items carry a `resource` tag; the reception + doctor layouts pass `accessibleResourceIds(user)` so a user only sees nav for resources they can access (super/clinic admin unaffected — short-circuit all). **Manager panel**: all reception pages open to managers (`requireRole(["receptionist","manager"])`). **Appointments enforcement** (the worked example): `createAppointment`/`updateAppointment`/`setAppointmentStatus`/`deleteAppointment` check `can(user,"appointments",<action>)` server-side; the reception list hides "New appointment" without create, `/reception/new` redirects viewers, and the appointment-detail Status/Edit/Danger-zone cards are hidden without the matching permission. Verified with a full-access vs view-only manager (New button, nav items, detail cards all gated; server actions blocked) + suite 50/50; tsc clean. Note: discovered this modified Next does a **soft redirect** (in-component `redirect()` → 200 with the target's content, not 307) — recorded in memory. Remaining enforcement (doctor clinical/prescriptions; patients/whatsapp/recalls/procedures/leave) is the next slice. |
| 2026-07-13 | **Access-control lists — foundation + manager role + permissions grid (Phase 1–2 of the ACL build).** Two-tier model: super-admin→clinic (existing `modules/features/log_access`) + **clinic-admin→staff per-user permissions**. New `core/auth/permissions.ts` (PURE/isomorphic): a `resource:action` catalog (View/Create/Edit/Delete; each resource declares which apply — reports are view-only, WhatsApp is view+send, etc.), `ROLE_DEFAULTS` for every role, and `can()`/`canAccess()`/`resourcesForClinic()`. New **`manager`** role (migration 0026 adds the enum value + `users.permissions text[]`) — an operations role that runs the reception panel (`ROLE_HOME_ROUTE`/`ROUTE_ROLE_ACCESS`/reception layout now accept it) with broad defaults (appointments/patients/recalls VCED, procedures VCE, sales/clinical/prescriptions/staff view, settings view). Clinic admins can now create + manage managers (shared `CLINIC_STAFF_ROLES`, staff list/detail/actions extended). New **permissions grid** on the staff detail page (`permissions-grid.tsx`) — a V/C/E/D matrix with greyed-out N/A cells and a View-is-prerequisite rule (button-checkboxes + hidden inputs, reset-proof), saved via `updateStaffPermissions` (sanitised, clinic-scoped, audit-logged) with a "reset to role defaults" action. The same matrix (`permission-matrix.tsx`, shared/controlled) also appears on the **create-staff form** — pick a role and the grid prefills that role's defaults (resets on role change), adjustable before creating; `createStaff` saves the chosen slugs. `users.permissions` NULL = follow role defaults; a stored array overrides. Verified 12/12 (defaults render, override replaces them, manager reaches /reception, blocked from /admin) + 19/19 catalog unit tests + suite 50/50; tsc clean. **Enforcement (making `can()` gate pages/actions/nav) is the next phase** — permissions are stored + editable but not yet enforced beyond role guards. |
| 2026-07-13 | **Consistent list "Open" button (admin clinics list).** The super-admin clinics list (`/admin`) used a plain underlined "Open" text link, unlike every clinic/reception list which uses the outline button + `ChevronRight` ("Open ›"). Standardised it to the same `buttonVariants({variant:"outline",size:"sm"})` + chevron, so the open affordance matches across the whole app. Verified render (admin list 200, no plain-underline Open left); suite 50/50; tsc clean. |
| 2026-07-13 | **Dropdown arrows: consistent gap from the right edge (app-wide).** The select chevrons sat too close to the right border. Base UI Select triggers (all filter bars — period/doctor/status/employee/action/clinic, page-size, appointment status) now use `pl-2.5 pr-3.5` so the chevron insets from the edge. Native `<select>`s (doctor + discount-type on the booking form, patient gender, staff role, patient-admin) get a new **`.select-chevron`** utility (globals.css) — `appearance-none` + a themed double-chevron SVG (matching lucide ChevronsUpDown, light/dark variants) positioned `right 0.85rem center` with `pr-8` text spacing — replacing the OS arrow so their gap is controlled and consistent too. Compact specialised selects (time picker h/m/AM-PM, date-picker month/year) left as-is to avoid distortion. Verified classes render on the real filter + form pages; suite 50/50; tsc clean. |
| 2026-07-13 | **Per-procedure discounts (layered with the appointment discount).** Each procedure line can now carry its OWN discount (Rs or %), applied to that line before the appointment-level discount. New `appointment_procedures.discount_type` / `discount_value` (migration 0025). Bill math is layered — `lineNet = gross − line discount`; `subtotal = fee + Σ lineNet`; `total = subtotal − appointment discount` — centralised in `core/appointments/fee.ts` (`computeProcedureLine` / `computeBill`, + `computeSaleAmounts` so the sales ledger snapshots the FULL discount = Σ line + appointment, keeping gross−discount=net). The per-row net is expressed once in SQL (`procedures.ts#procedureRowNetSql`, mirroring the JS) with correlated `appointmentProceduresNetSql`/`GrossSql` helpers, so the appointment lists, WhatsApp confirmation, reschedule quote, ledger and the report's per-procedure breakdown all stay a single fast aggregate (no N+1) and can't drift from the JS. Booking form: each chosen procedure row gets a Rs/% + value discount control next to the qty stepper, with the discounted line net (original struck). Detail bill card shows each line's discount + a subtotal → appointment-discount → total split. Encoded field is now `"<id>:<qty>:<type>:<value>"` (old 2-part still parses). Verified 14/14 (fee 2000 + X-Ray 1000×2 −10% + Cleaning 3000 −Rs500, appt −10% → net 5,670; list/bill/report + ledger snapshot 7,000/1,330/5,670 all agree) + suite 50/50; tsc clean. Completes the Sales feature's discount model. |
| 2026-07-13 | **Net-sales card on the clinic dashboard.** The clinic-admin dashboard (`/clinic`) now shows a **"Net sales (30 days)"** stat card — the net figure + completed-visit count, linking to the full `/clinic/sales` report — gated by the `sales` feature (hidden when off). Backed by a lightweight `getSalesSummary` (core/sales/report.ts): a single indexed `sum(net_amount)`+count over the (`clinic_id`,`occurred_at`) range, run in parallel with the other dashboard stats. Fixes "I don't see any sales summary on the dashboard" — Sales previously lived only on its own sidebar page. Verified 5/5 (card title, net total, completed count, link) + suite 50/50; tsc clean. |
| 2026-07-13 | **Activity-log Action filter.** Added an **Action** dropdown to both log filter bars (`core/ui/log-filters.tsx`) — filter entries by category (Logins / Views / Creates / Updates / Status changes / Deletes), alongside the existing date/employee(/clinic) filters. Options are **scoped to the viewer**: the clinic admin only sees the categories the super admin granted (`clinics.log_access`), and an out-of-scope `?action=` is ignored server-side so it can never widen access (falls back to all granted categories); the super admin gets all six. `parseLogFilters` now returns `action`; both pages apply `eq(activityLogs.action, …)` when in scope and pass the option list. Verified 5/5 (create/update narrowing, out-of-scope delete ignored for clinic, admin delete/login filtering) + suite 50/50; tsc clean. |
| 2026-07-13 | **Optional consultation fee (procedure-only visits).** New `appointments.charge_consultation` bool (migration 0024, default true). The booking form (create + edit) shows a **"Charge consultation fee (Rs X)"** checkbox whenever the chosen doctor has a fee — uncheck it for a patient who comes only for a procedure, and the consultation drops out of the live bill. Threaded through everywhere the fee is computed: both appointment lists, the WhatsApp booking confirmation, the WhatsApp reschedule quote, the appointment-detail bill card, and the **sales ledger** (`recordSaleForAppointment` + backfill), so a waived fee is reflected in revenue. The bill preview reads cleanly with no fee ("Rs X procedures = …" instead of "Rs 0 fee + …"). Also clarified for the owner: the Sales **graph needs completed appointments** — it's realised revenue, so it stays on the empty state until an appointment is marked Completed (Clinic001 currently has 7 scheduled, 0 completed). Verified 7/7 (procedure-only vs normal bill, checkbox prefill on/off) + suite 50/50; tsc clean. |
| 2026-07-12 | **Sales feature — deferred items completed (procedure quantity + bill card).** Closes the three items deferred during the phased build. (1) **Per-procedure quantity**: the booking form's procedure picker now shows a −/qty/+ stepper + line total for each chosen procedure; the form submits `procedure="<id>:<qty>"` hidden fields, parsed by `parseProcedureSelections` and saved via `saveAppointmentProcedures` (now takes `{procedureId, quantity}[]`, merges dupes, clamps 1–99, snapshots price×qty). Lists / WhatsApp / reschedule / ledger already summed `unit_price×quantity`, so they need no change. (2) **Read-only bill card** on the appointment detail page — consultation fee + each procedure line (`name × qty @ unit`) + subtotal + discount + **total** (via `computeAppointmentTotal`; new `getAppointmentProcedureItems` returns the snapshots). (3) **WhatsApp self-booking procedures**: staff attach them by opening the booking and using the same picker (no separate flow needed) — verified the edit path flows into the bill + sales. Verified 8/8 (bill line items, qty prefill, discounted total) + suite 50/50; tsc clean. Sales feature fully complete, no deferrals remaining. |
| 2026-07-12 | **Sales feature — phase 3: revenue ledger + report (feature complete).** New `sales` table (migration 0023; UNIQUE per appointment) — a realised-revenue ledger written by `core/sales/ledger.ts`: `recordSaleForAppointment` snapshots gross/discount/net (`computeAppointmentTotal` = fee + Σ procedures − discount) + doctor name + `occurred_at`, upserted when an appointment is marked **completed** (`setAppointmentStatus`) and re-snapshotted when a completed appointment is edited; `voidSaleForAppointment` deletes it if it leaves "completed"; `backfillClinicSales` (idempotent) runs when the super admin first enables the `sales` feature (`admin/actions.ts#updateClinic`), so the report shows history immediately. All best-effort — a ledger hiccup never blocks the status change. New **Sales** page (`/clinic/sales`, clinic-admin + `sales`-gated, nav via `PanelShell`): period presets (Today / 30d / quarter / 6mo / year) + custom range + per-doctor filter; summary cards (net, completed visits, discounts, avg/visit); a single-series **net-sales-over-time** bar chart (`sales-chart.tsx` — SVG, `--chart-1` teal, auto hour/day/week/month buckets, hover tooltips, theme-aware; built per the dataviz skill); and per-doctor + per-procedure breakdown tables. Query layer `core/sales/report.ts` (one range-scan powers summary/buckets/by-doctor in local time; a join gives the procedure mix). Verified end-to-end (11/11: gating 404, net/discount/count totals, doctor filter narrowing, empty period, tenant isolation, backfill detection) + suite 50/50; typecheck green. Sales feature (all 3 phases) done. |
| 2026-07-12 | **Sales feature — phase 2: procedures on appointments + totals.** `appointment_procedures` line-items table (migration 0022; snapshots name+price+qty). The booking form (`NewAppointmentForm`, create + edit) gains a **Procedures** multi-select (chips with prices) with a live bill preview; `createAppointment`/`updateAppointment` save/replace the line items (`saveAppointmentProcedures`, snapshotting prices). The appointment **total is now consultation fee + Σ procedures, with the discount applied to that total** — new `computeAppointmentTotal` (core/appointments/fee.ts), used by the form, both appointment lists (net via a `sum(unit_price×qty)` subquery), and the WhatsApp confirmation. Picker/prices only appear when the clinic has the `sales` feature (`getBookingProcedures`). Verified: fee 1,000 + Cleaning 3,000 + RCT 15,000 − 2,000 discount = net Rs 17,000 (gross 19,000 struck) on the list, edit prefills the two procedures, picker shows on new/edit; suite 50/50. Also fixed the WhatsApp **reschedule** reply to quote the full net total (fee + procedures − discount) instead of just the consultation fee. Deferred by agreement: WhatsApp self-booking doesn't attach procedures (staff attach on confirm), quantity fixed at 1. Next: phase 3 (sales ledger + report). |
| 2026-07-12 | **Sales feature — phase 1: procedures catalog.** New `sales` feature in `core/lib/features.ts` (super admin toggles it per clinic in Settings → Features). `procedures` table (migration 0021: clinic_id, name, price PKR, module, is_active). Full CRUD by **clinic admin AND receptionist** (`app/reception/procedure-actions.ts`, shared role guard + `sales`-feature gate, audit-logged) — add / edit (name/price/active) / delete (two-click confirm) — on a shared **Procedures** page at `/clinic/procedures` + `/reception/procedures` (`ProceduresManager` + `ProceduresPanel`). "Import suggested procedures" pulls the enabled specialty's defaults (`ModuleDefinition.procedureTemplates` → dental starter list; `config/modules.ts#procedureTemplatesFor`, deduped, re-runnable). Nav item gated in both panels via `PanelShell.salesEnabled`; direct URL 404s when the feature is off. Phase 1 of the 3-part plan (2: attach procedures to appointments + totals; 3: sales ledger + report). Verified feature on/off gating + render + dental import list; typecheck green; suite 50/50. |
| 2026-07-12 | **Pagination bar moved above the list.** The "Showing X–Y of Z" + page-size dropdown (+ Prev/Next) now render **before** the list on all nine pages (after the header/filters/search) instead of below it. Verified the bar precedes the first row; suite 50/50. |
| 2026-07-12 | **Appointment filter-bar alignment.** Applied the same fix as the log bar to `AppointmentFilters` (From/To/Status/Search/Today): identical `flex flex-col gap-1.5` field wrappers, uniform `text-xs` labels, **w-44** for From/To/Status (were w-40 dates + w-40 select, ragged), Search flex-1, and the Today button wrapped with an invisible `Label` so it bottom-aligns with the inputs. Renders 200; typecheck green. Added a "N / page" selector to the pagination bar — a themed Base UI Select (`core/ui/page-size-select.tsx`), URL param `size` (`PAGE_SIZE_OPTIONS=[20,50,100]`, `parsePageSize`), changing it resets to page 1 and preserves filters. The pagination bar now always renders when there's data (so the dropdown shows even on a single page of <20 rows); Prev/Next still appear only when there's more than one page. Threaded `pageSize` through all nine list pages (limit/offset + count). Verified: dropdown present with 5 rows, `?size=50` shows all 30 on one page ("Showing 1–30 of 30 · 50 / page"); suite 50/50. |
| 2026-07-12 | **Pagination on every list screen.** Added a shared `core/lib/pagination.ts` (`parsePage`/`pageOffset`/`DEFAULT_PAGE_SIZE=20`) + server component `core/ui/pagination.tsx` — "Showing X–Y of Z" + Prev/Next that preserve current filters (all query params except `page` and flash flags). Wired into all nine lists: admin clinics, clinic staff, clinic patients, clinic + reception appointments, clinic + admin activity logs, clinic recalls, reception WhatsApp. Each page now runs a `count()` alongside the `limit/offset` slice (count re-joins only the tables its filter needs) and shows the true total in the header. Verified: 25 patients → page 1 = 20, page 2 = 5, "Showing 1–20 of 23 · Page 1 of 2", Next link + filter (`q`) preserved; typecheck green; suite 50/50. |
| 2026-07-12 | **Clinic manage page merged to one Save.** The super-admin clinic detail page had four separate settings saves — Clinic name, Specialties, Features, Activity-log access. Merged into a single **Settings** card + one **Save changes** (`ClinicSettingsForm` with name input + specialty/feature/log-access checkbox groups, each emitting its own hidden inputs). One new action `updateClinic` saves name + `modulesEnabled` + `featuresEnabled` + `logAccess` together and redirects to `/admin?updated=1` ("Clinic updated." toast); removed `updateClinicName`/`updateClinicModules`/`updateClinicFeatures`/`updateClinicLogAccess` and the four form files. Staff list + Danger-zone delete stay separate. Verified: one Save, all groups present, no old buttons, Staff/Danger intact; e2e still sees the Features toggle + ON/OFF behavior; suite 50/50. |
| 2026-07-12 | **Staff edit merged to one Save.** The staff detail page had two record-edit buttons — "Save changes" (profile) and "Save schedule" (doctor hours/cap/fee). Merged into a single **Details** card + one **Save changes** that updates name, username AND (for doctors) the schedule together — mirroring the create form. `updateStaffProfile` now also parses/saves the doctor schedule in the same call (a bad schedule blocks the whole save, like create); removed `updateDoctorSchedule` + `edit-schedule-form.tsx`; `EditProfileForm` → `EditStaffForm` (renders `DoctorScheduleFields` for doctors, submit gated on schedule validity). The separate, distinct actions stay separate (Leave, Password reset, Suspend/Reactivate, Delete). Verified: doctor page shows one Save + inline schedule and no "Save schedule"; receptionist shows just name/username; other action cards intact; suite 50/50. |
| 2026-07-12 | **View logs de-duplicated (30-min window).** Opening/refreshing the same record repeatedly no longer spams the log: `core/audit/log.ts#logView` now skips the insert when the SAME user already has a `view` of the SAME record within `VIEW_DEDUPE_MINUTES` (30). The `logView` server action (ViewLogger) routes through it. Best-effort, non-transactional (a rare concurrent double is harmless). Verified the dedupe query: same user+record recent → skip; 35-min-old / different record / different user → log; suite 50/50. |
| 2026-07-12 | **Log filter bar alignment.** Reworked `core/ui/log-filters.tsx` so every control (From, To, Clinic, Employee, Today) is an identical `flex flex-col gap-1.5` field with a uniform `text-xs` label and the **same width (w-44)** — previously dates were w-40 and selects w-48 (ragged), and the Today button used a plain `<span>` spacer (16px) vs the `Label` component's leading-none (~12px), so heights didn't match. Today now uses a real invisible `Label` so its column height matches and the button bottom-aligns with the inputs. Pages render 200; suite 50/50. |
| 2026-07-12 | **Log employee filter lists the clinic's staff + Today-button alignment.** The employee dropdown was built from *distinct actor names already in the logs* (further constrained by granted categories), so it was empty when a clinic had no matching activity. It now lists the clinic's **actual staff from the `users` table** (roles in `CLINIC_LOG_ROLES`), so everyone appears even before they've logged anything; the filter matches on `actor_user_id` (robust to renames, shows full names). Applies to both `/clinic/logs` and the super-admin `/admin/logs` (once a clinic is picked). Also fixed the **Today** button alignment in the log filter bar (wrapped it with a spacer label so it bottom-aligns with the labelled date/clinic/employee controls). Verified: dropdown lists Dr One/Rec Two/Admin A, filtering by an employee narrows to their rows on both pages; suite 50/50. |
| 2026-07-12 | **Log leak fix: clinic admin no longer sees super-admin actions.** Super-admin actions on a clinic (settings/module/feature/rename/log-access changes) are tagged with that clinic's `clinic_id`, so they were showing in the clinic admin's log. The clinic log page now filters to `actor_role IN {clinic_admin,doctor,receptionist}` (`CLINIC_LOG_ROLES` in `core/audit/access.ts`) — the admin sees only their own + their staff's actions. The super-admin employee dropdown (once a clinic is picked) likewise lists that clinic's staff, not the super admin. Super-admin `/admin/logs` still shows everything (incl. super-admin actions). Verified: clinic admin sees admin+doctor rows but NOT the super-admin action / username; super admin sees all + staff-only employee list; suite 50/50. |
| 2026-07-12 | **Super-admin log filter: clinic → then employee.** On `/admin/logs` the Employee filter now depends on the Clinic filter: with no clinic selected it's disabled and shows "Select a clinic first" (and a stray `actor` param is ignored); once a clinic is chosen, the employee dropdown lists ONLY that clinic's staff and filters within it. Clinic page unchanged (employee always active for its own staff). Verified: no-clinic disables employee + lists none; clinic A lists only A's staff and rows; suite 50/50. |
| 2026-07-12 | **Activity log: permission-based access (replaces the 5-day auto-hide) + today-default + clinic/employee filters.** Dropped the time-based `visible` model — removed `activity_logs.visible`, `hideOldLogs`, `LOG_VISIBLE_DAYS`, the `/api/cron/log-visibility` cron and its `vercel.json` entry (migration 0020). Instead the **super admin grants each clinic a set of visible ACTION categories** (`clinics.log_access` text[] — logins/views/creates/updates/status/deletes; `core/audit/access.ts`), configured via a new **Activity-log access** card on the clinic detail page (`updateClinicLogAccess`). The clinic admin's `/clinic/logs` shows only the granted categories for their clinic, and **no log page/nav at all when `log_access` is empty** (PanelShell `logsEnabled` gate from the clinic layout). Both log pages now **default to today** and gained filters: date range + **employee**; the super-admin page also has a **clinic** filter (and its employee list scopes to the chosen clinic). Verified end-to-end: no-access message, granted-categories + today filtering, employee/clinic/date filters, super admin sees all incl. non-granted categories; typecheck green; suite 50/50. |
| 2026-07-11 | **Activity-log filters: date range + actor.** Both `/clinic/logs` and `/admin/logs` got a filter bar — From/To themed date pickers + an **Actor** dropdown (themed Base UI Select, populated from the distinct actors in scope) + Clear. URL params `from`/`to`/`actor`, parsed by `core/audit/log-filters.ts` (dates DON'T default — empty = no constraint, unlike the appointment list). Verified: actor filter and today-range each narrow correctly on both pages; suite 50/50. |
| 2026-07-11 | **Activity / audit log with 5-day clinic-admin visibility.** New `activity_logs` table (migration 0019) with a `visible` bool (default true). Logs **all clinic-staff actions** (create/update/delete of staff, patients, appointments, settings, leave, status), **logins**, and **record views** (patient/appointment/staff detail, via a client `ViewLogger` so link prefetch doesn't create phantom views). CORE `core/audit/log.ts` — best-effort `logActivity`(current user)/`logActivityAs`(explicit, for login), never throws/blocks; actor identity is snapshotted so rows survive user deletion. A daily cron `/api/cron/log-visibility` (`hideOldLogs`, CRON_SECRET, in `vercel.json` at 00:30) flips `visible=false` once a row is older than **5 days**. **Clinic admin** `/clinic/logs` sees only its own clinic's `visible=true` rows; **super admin** `/admin/logs` sees ALL rows across clinics incl. hidden ones (with Clinic + Visible columns). Nav items added to both panels; shared `core/ui/activity-log.tsx`. Super-admin actions log against the affected clinic (except clinic-delete → clinic_id NULL, since the FK cascades away). Verified end-to-end: cron 401-without-token / hides the 6-day-old row / keeps the recent one; clinic view hides the hidden row, super-admin view shows it; typecheck green; suite 50/50. |
| 2026-07-11 | **Search icon on the appointment patient picker.** The "Search patients by name or phone…" input in the create/edit appointment form now shows a leading `Search` (magnifier) icon, matching the list search inputs (patients/staff/clinics) — `relative` wrapper + absolutely-positioned icon + `pl-8`. Verified it renders on the new-appointment page; typecheck green. |
| 2026-07-11 | **Patients entered by age + whole-row click on every list.** (1) The patient DOB date-picker is replaced with an **Age** number field: `core/lib/age.ts` (`ageFromDob`/`dobFromAge`/`dobFromAgeField`) stores the derived birth date (still `patients.date_of_birth`, no schema change) so age never goes stale — recomputed from the stored date each view. Add/edit forms use the Age input; the patient list shows an **Age** column (and "N yrs" on mobile). (2) New `core/ui/row-link.tsx` (`RowLink`) makes a whole table row / mobile card navigate to its detail on click (hover highlight, keyboard Enter/Space, `role="link"`), ignoring clicks on genuinely interactive controls (links, buttons, the status `Select`, inputs) so those keep working. Applied to all entity lists: patients, staff, admin clinics, and both appointment lists (the "Open" button stays as an affordance). Verified: age 30 round-trips through create/edit/list, every list renders clickable rows; suite 50/50. |
| 2026-07-11 | **Same toast+redirect for patient & clinic create/edit/delete.** Extended the redirect-to-list + `FlashToast` + form error-toast pattern beyond staff/appointments: **patients** — `createPatient`→`/clinic/patients?created=1`, `updatePatient`→`?updated=1`, `deletePatient`→`?deleted=1` (list shows "Patient added/updated/deleted."), add + edit forms drop inline saved/error text for a red error toast; **super-admin clinics** — `createClinicWithAdmin`→`/admin?created=1`, `deleteClinic`→`/admin?deleted=1` ("Clinic created/deleted."), create-clinic form gets the error toast. Left in-place (correct UX — no list to bounce to): the clinic-detail inline settings (modules/features/name/avg-visit-value), admin staff-profile/reset-password dialogs, doctor daily-limit, and doctor leave — these keep their on-page feedback. Verified all pages render incl. flash params; suite 50/50. |
| 2026-07-11 | **Create/edit staff → success/error toast + back to the staff list.** `createStaff` now redirects to `/clinic/staff?created=1`; `updateStaffProfile` and `updateDoctorSchedule` now redirect to `/clinic/staff?updated=1` (were returning `{saved:true}` and staying on the detail page). The staff list renders a `FlashToast` ("Staff member added." / "Staff member updated."). Each form (add-staff, edit-profile, edit-schedule) drops its inline saved/error text and pops a red **error toast** (via the shared `Toast` + per-attempt nonce) when the action fails — matching the appointment create/edit UX. Reset-password / suspend / delete are unchanged (they stay on the detail page). Verified staff pages render incl. flash params; suite 50/50. |
| 2026-07-11 | **Click a queue card → filter the list to that queue.** The "Today's queue" cards on `/clinic/appointments` and `/reception` are now clickable: clicking one sets `?session=<queueSession>` and the list shows ONLY that queue (a doctor's single visiting window), ordered by token number; the selected card is highlighted and a banner ("Showing queue: Dr X · 9:00 AM–12:00 PM" + "Show all appointments") clears it. In session mode the date/status/search filter bar steps aside (the session already pins doctor+window+day). `QueueSummary` gained `pathname`/`activeSession` props (cards render as `<Link>` only when a pathname is passed, so the doctor panel's cards stay non-clickable). Verified: w0 card shows only its patients, w1 only its patient, banner + clear present; suite 50/50. |
| 2026-07-11 | **Themed DatePicker everywhere (DOB, doctor leave) + month/year quick-nav.** Replaced the remaining native `<input type="date">` fields — patient **date of birth** (add + edit) and **doctor leave** start/end — with the shared themed `core/ui/date-picker.tsx` (controlled + hidden input carrying the value for the server action; leave "Add" disabled until both dates set). Enhanced the DatePicker header with **Month + Year dropdowns** (years span today−100 … today+10, descending) so far-back dates like DOB are reachable in one click instead of hundreds of month-chevron presses — benefits appointment/filter pickers too. No native date inputs remain in `src/`. Verified all three pages render the themed trigger with no native date field; suite 50/50. |
| 2026-07-11 | **Appointment list: status filter.** Added a **status** dropdown to the filter bar (All / Scheduled / Confirmed / Completed / Cancelled / No-show) — a themed Base UI Select (theme accent on hover, matching the row status control), URL param `status`. Server-side `parseListFilters` validates it against the enum (invalid → ignored) and both lists add `eq(status, …)` to the query; the header label reflects the active status. Verified: each status narrows correctly, bogus values ignored; suite 50/50. |
| 2026-07-11 | **Appointment list: date-range filter (defaults to today) + search.** Both `/clinic/appointments` and `/reception` now default to showing **today's** appointments, with a filter bar: **From**/**To** date pickers (themed `DatePicker`) + a debounced **search** box (patient name/phone) + a **Today** reset. Filters are URL query params (`from`/`to`/`q`) read server-side — shared parser `core/appointments/list-filters.ts` (defaults both bounds to today, tolerates a reversed range, half-open `[start, endExclusive)`), shared client bar `reception/appointment-filters.tsx`. List now orders soonest-first within the range (limit 200); the header shows the active range + query; empty state is range-aware. The live "Today's queue" summary is unchanged (always today). Verified: default shows only today, ranges include the right days, `q` narrows within the range; e2e updated (its seed sits 3 days out, so that assertion now queries a wide range) — suite 50/50. |
| 2026-07-11 | **Patient queue tokens (per doctor visiting-window, FCFS).** Every appointment with a doctor now gets a **token number** on create / staff-edit / WhatsApp self-book / reschedule, scoped to a **(doctor, day, visiting-window) session** — so a doctor's 9–12 window numbers 1,2,3… and their 4–7 window restarts at 1,2… the same day; resets daily. New `appointments.queue_session` + `queue_number` (migration 0018) with a UNIQUE (clinic, session, number) index; assignment is FCFS max+1 with retry-on-race, **stable** across cancellations (per owner's choice). Core: `core/appointments/queue.ts` (`queueSessionKey`, `withQueueNumber`, `getDayQueue`); `checkDoctorSlot` now also returns availability+flexible so callers build the session without a re-query. Patients learn their number: it's in the WhatsApp booking ack, the reschedule reply, and the `notifyAppointmentBooked` confirmation ({{7}}). UI: a **# token column** on both appointment lists + a shared **`QueueSummary`** ("Now serving #X · N waiting · M done", token chips coloured by status) shown on `/clinic/appointments`, `/reception`, and the **doctor panel** ("Your queue today"). No-doctor ("Any") appointments carry no token. Verified end-to-end: WhatsApp bookings assign w0→#1,#2,#3 and w1→#1, reply carries the token, list shows "Now serving #2" (skipping the completed #1); typecheck green; suite 50/50. |
| 2026-07-11 | **Themed appointment status dropdown (no more system-blue hover).** The status control was a native `<select>` whose open-option highlight is OS-drawn (blue) and unstylable. Rebuilt `appointment-actions.tsx` on **Base UI Select** (same lib as the DatePicker): the highlighted item now uses the theme `bg-accent`/`accent-foreground` via `data-[highlighted]`, the popup uses `bg-popover`/`border-border`, and a ✓ marks the current status. Same behaviour (set any status → supports undo; applied via `useTransition`; server-side transition guards unchanged). E2E updated — options now mount in a portal on open, so the detail assertion checks the trigger + current-value label instead of every option label. Suite 50/50. |
| 2026-07-11 | **Toast no longer cut short + Fee column left-aligned.** `FlashToast` was calling `router.replace` to strip the flash param, which triggered a Next navigation that remounted the toast subtree and dismissed it after ~2s; now it strips the param via `window.history.replaceState` (no navigation), so the toast runs its full duration (bumped to success 4s / error 6s). The appointment-list **Fee** column (both `/clinic/appointments` and `/reception`) is now left-aligned to match When/Patient/Doctor (was `text-right`). Typecheck green. |
| 2026-07-11 | **Error toast on failed create/edit appointment.** `core/ui/toast.tsx` refactored into a reactive presentational `Toast` (`variant` success/error, `token` to re-trigger the same message) + a `FlashToast` wrapper (the redirect/query-param success case used by the lists). The appointment form now pops a red **error toast** whenever `createAppointment`/`updateAppointment` returns an error (e.g. out-of-hours / on-leave / over-cap slot), re-triggered per attempt via a nonce bumped on each settled action; replaced the inline error `<p>`. Typecheck green; suite 50/50. |
| 2026-07-11 | **Create/edit appointment → redirect to the list + success toast; fixes edit blanking the doctor.** The discount value is now a string-backed input so it can be cleared (a numeric state snapped a blank field back to 0). On **edit**, `updateAppointment` now **redirects to the appointments list** (`?updated=1`) instead of returning `saved:true` and staying — which is what caused React 19's post-action `<form>` reset to blank the controlled doctor `<select>` ("selected doctor removed"); navigating away means the reset artifact is never shown. **Create** already redirected, now with `?created=1`. Both `/clinic/appointments` and `/reception` read the flash param and render a new reusable `core/ui/toast.tsx` success toast ("Appointment scheduled." / "Appointment updated.") that strips the param on mount and auto-dismisses. Removed the dead inline "Saved." indicator. Typecheck green; suite 50/50. |
| 2026-07-11 | **Per-appointment discount off the doctor's fee + fee shown on the lists.** The create/edit appointment form now has a **Discount** field: a type toggle (**Amount (Rs)** — the default — or **Percent (%)**) plus a value (e.g. 500, or 20). New columns `appointments.discount_type`/`discount_value` (migration 0017, additive, free-text/int not enums). The **net fee** (doctor's `consultation_fee` − discount, clamped to `[0, fee]`) is derived live via pure CORE `core/appointments/fee.ts#computeFee` (shared by the client form + server) — never stored, so a fee change flows through; percent is capped at 100 server-side. The form shows a live "Fee Rs X − Rs Y discount = **Rs Z**" preview once a doctor is picked. Both appointment lists (**/clinic/appointments** + **/reception**) gained a **Fee** column showing the net (with the full fee struck through when discounted) on desktop and a "Fee:" line on mobile. The WhatsApp booking confirmation now quotes the **net** fee. Typecheck green; migration 0017 applied. |
| 2026-07-14 | **Soft delete + Trash — nothing is hard-deleted (migration 0027).** Every deletable table (clinics, users, patients, appointments, visits, recalls, procedures, doctor_leaves) got `deleted_at`/`deleted_by`/`delete_group`/`deleted_by_cascade` (`softDeleteColumns()`) + `clinics.trash_retention_days` (default 30); `users` username/email uniqueness became PARTIAL (`WHERE deleted_at IS NULL`) so names free up after a soft delete; per-table partial trash indexes. **Phase 2:** added `notDeleted()` (`core/db/tenant.ts`) to EVERY read across core + app (auth/session, availability/queue/booking/reschedule, sales ledger+report, recall + appointment crons, scribe/prescription routes, all lists/details/counts/pickers), and converted every delete action to a soft delete — deletePatient/deleteClinic cascade-hide children under one delete group and void sales; deleteStaff revokes sessions; the only remaining `db.delete()` on these tables is the legal purge. **Phase 3:** `trash` permission (view + "create"=Restore; clinic-admin default, grantable per user), `core/trash` (list/restore/purge, re-snapshotting sales on restore), `/clinic/trash` (within retention) + `/admin/trash` (all clinics, no window, super-admin **Purge** with step-up password), super-admin sets retention on the clinic form. **Trash filters:** search + type + deleted-by + date-range, plus per-clinic for the super admin. **Phase 4:** hardened the update-by-id edit actions with `notDeleted()`, added a permanent SOFT DELETE / TRASH e2e section, confirmed the grant flow. Verified across phases (12/12 schema, 6/6 hide+persist, 9/9 trash pages+ACL+restore, 7/7 filters, 6/6 grant flow) + **suite 58/58**; migrations `0000`–`0027` applied. See `.claude/database.md` §1 for the soft-delete model. |
| 2026-07-14 | **Per-clinic WhatsApp sender numbers (Meta Cloud API) — Phases 1–5 built + Phase 6 documented.** Goal: each clinic sends WhatsApp from ITS OWN number so patients see the clinic's number, with clinic-customisable signature/notes — via one WABA token + a per-clinic `phone_number_id`. **P1 (migration 0029):** `clinics` gains `whatsapp_phone_number_id` (partial-UNIQUE, the inbound routing key), `whatsapp_display_number`, `whatsapp_sender_name`, `whatsapp_signature`, `whatsapp_notes` jsonb (`WhatsappNotes`); env `WHATSAPP_PROVIDER` (aisensy|cloud, default aisensy), `WHATSAPP_CLOUD_TOKEN`/`WABA_ID`/`API_VERSION`/`VERIFY_TOKEN`. **P2:** `core/integrations/whatsapp` became a provider dispatcher; new `cloud.ts` posts to Graph `/{phone_number_id}/messages` (type:template) with the same `SendResult`; AiSensy unchanged. **P3:** the notification channel resolves the clinic's sender (`getClinicSender`) on the cloud provider and sends FROM its number, appending the clinic's per-event `{{note}}` + `{{signature}}`; `event` wired into booking/reminder/recall. **P4:** new `GET/POST /api/whatsapp/cloud` — verify handshake + routes inbound by `metadata.phone_number_id` → clinic → patient (digits-only match), advances receipts, `X-Hub-Signature-256` verified when `WHATSAPP_APP_SECRET` set. **P5:** super-admin provisioning fields on the clinic settings form (phone_number_id/number/name, unique-checked) + a clinic-admin "message personalization" card on `/clinic/whatsapp` (signature + notes, `settings:edit`, cloud-only). **P6 (docs):** `docs/whatsapp-cloud-plan.md` now has the full env checklist, per-event template variable maps (exact `{{n}}` order incl. trailing note/signature), the "always-present variable" rule, go-live steps, and instant rollback (`WHATSAPP_PROVIDER=aisensy`). Default provider unchanged so nothing sends differently; suite 61/61 (3 new cloud-webhook tests); migrations `0000`–`0029`. Live sending needs the WABA token + a verified clinic number (owner setup). |
| 2026-07-17 | **Doctor–clinic discount bearing + settlement (migration 0041, 6 phases + follow-ups).** Owner-approved model: whoever BEARS a discount absorbs it in full — **no spillover** — and a doctor's balance may go **negative** (they owe the clinic). Recognised as an **accrual snapshot at completion**, independent of collection, in `discount_settlements` (one signed, zero-sum row per party); earnings (`sale_shares`) switched to a **gross-% collected basis** so a doctor's true net = gross earnings + settlement, converging to make-whole as the patient pays (formula in `docs/discount-bearing-plan.md` §3; pure `core/appointments/discount-bearing.ts#computeBearing`, 33 unit tests). Folded into `getDoctorBalances` (new signed `borne` + `adjustments`, negative Outstanding), the shares report/statement, dashboard KPIs, P&L, and the discounts report (Clinic-bears/Doctor-bears via `discountBorneSplit`, dropping a per-row context fetch). **Settlement actions** (`doctor_settlement_actions`): bidirectional waives (amount-based on `/clinic/shares` + **per-line** on the appointment detail), doctor→clinic repayments, write-offs, reversals — gated by a new **`share_waive`** ACL (a doctor waives his own by identity). **Approvals** now require only the bearing party (`syncDiscountApprovals` via `discountBorneSplit`, split-portion aware). Appointment form gained a **Split** control (doctor's %/Rs portion, live preview). **Review fix:** the settlement was being lost for a completed-but-unpaid / 100%-discount visit (`collected ≤ 0` voided it) — now recorded regardless of collection (it's accrual). DB-reconciled across all clinics (zero-sum, Outstanding = earned+borne+adjustments−paid, clinic cut = net−share, netProfit = revenue−doctorShares−expenses); tsc clean; unit 33/33; e2e 61/61. |
| 2026-07-17 | **Charts diversified by data job + KPI sparklines + palette review.** The analytics were all bar-family; matched each chart form to its data's job. New **`AreaChart`** (single-series filled area + crosshair) for accumulating-flow trends → Sales "Collected revenue over time". New **`HBarChart`** (horizontal ranked bars, exact figure on the right, single teal hue = one measure) for "compare a measure across categories" → Sales By-doctor / By-procedure and P&L Expenses-by-category / Doctor-shares (were tables/lists); pure CSS so it renders in the server component. Together with the existing grouped bars (P&L) + grouped+cumulative line (Shares), four forms instead of one; removed the now-unused single-series `SalesChart`. **KPI sparklines** (`core/ui/sparkline.tsx`, pure SVG, no hooks, server-safe): tiny inline trend under the time-based dashboard numbers — Collected (30d) + Net profit (30d) reuse the per-day P&L buckets `getFinanceKpis` already computes (`collectedTrend`/`profitTrend`, zero extra queries; profit sparkline red on a loss), and Revenue-recovered (hero) adds one per-month query over 6 months (revenue feature only). Snapshot KPIs (Outstanding, Payable) get none. **Review (ran the dataviz palette validator, not eyeballed):** the teal(chart-1)+blue(chart-2) multi-series pair sits in the CVD floor band (tritan ΔE 7.4) — legal only with secondary encoding — so added a **`dashed`** LineChart series (solid "Cumulative earned" vs dashed "Cumulative paid", legend swatch reflects it) for non-colour separation, and made `HBarChart` **negative-safe** (size by \|value\|, tint negatives red, e.g. a doctor who net-bore a discount in P&L by-doctor). Grouped bars keep colour-only identity (position + legend separate them). All charts theme-aware; tsc clean; e2e 61/61. |
| 2026-07-17 | **Overview ("Day report") — consolidated period report.** One-click daily/period report at `/clinic/overview` (default today), reached from a **"Day report"** button on the dashboard + an Overview card in the Reports hub. `core/finance/overview.ts#getOverview` **composes the existing tested cores** with the same range (`getSalesReport` / `getDiscountsReport` / `getSharesReport` / `getProfitAndLoss` / `getCashSummary` / `getWaiversTotal`) so it **reconciles exactly** with each standalone report — no new money logic. **Performance** (collected / discounts / doctor shares / net profit, by visit date) and **Cash** (money in/out, by payment date) live in separate, clearly-labelled sections — never blended (the cash-vs-accrual discipline). Sections: summary cards · "where the collected money went" (Doctor shares vs Clinic cut) · **per-doctor shares** (earned / discount-borne / net — "what each doctor earned") · Cash by method · Discounts & waivers · Collected-by-doctor / Billed-by-procedure / Expenses-by-category; dual-render + print. Doctor filter scopes the performance sections (clinic-wide profit/expenses/cash hide). Additive helpers: daybook `getCashSummary` (range), `DoctorShareRow` gains `grossEarned`/`borne`, `getWaiversTotal`. Gated by the `finance` feature + `finance:view` (P&L-level); the standalone Day Book stays the focused cash view. DB-reconciled across all clinics (collected==sales, doctorShares==shares report, netProfit==P&L, collected=shares+clinicCut, Σ per-doctor==doctorShares); tsc clean; unit 33/33; e2e 61/61. Plan: docs/overview-report-plan.md. |
| 2026-07-17 | **Overview money-flow waterfall + dashboard "Today's money flow" card.** New `WaterfallChart` (theme-aware SVG, anchored totals on a zero baseline, floating deductions linked by dashed connectors, value labels, red result on a loss) — a fifth chart form, the canonical "how a total became a result" viz. Made it the **Overview's headline chart**: **Collected → −Doctor shares → −Expenses → Net profit** (exact — the share step = P&L cost `collected − expenses − profit`, net of waivers, so it lands on Net profit); replaced the plain 2-bar shares-vs-clinic split. Added a compact **"Today's money flow"** waterfall **card on the dashboard** (finance-gated, today-scoped via `getProfitAndLoss("today")`, with a "Full day report →" link) — the one Overview taste the 30-day KPIs don't show; kept the "Day report" button, no full Overview card (avoids duplicating the KPIs). tsc clean; unit 33/33; e2e 61/61. |
| 2026-07-19 | **Refund split into its own ACL + wired to the UI.** Refunds were bundled with Void under `billing:delete` (and the `refundAppointmentPayment` action had no UI). Added a dedicated **`refund`** permission resource (View/Create/Delete; Edit greyed out — a refund is reversed, not edited; `sales`-gated like billing). `refund:create` = issue a refund, `refund:delete` = reverse a refund entry; **voiding a payment/advance stays `billing:delete`**, so the two powers grant independently. Role defaults: clinic_admin (all) + manager get refund; **receptionist does NOT** (front-desk collects, admin/manager refunds) — grantable per-user via the existing permission grid (picks up the new row with zero UI changes). Wired an actual **Refund form** into the appointment payment panel (gated `refund:create`, capped at collected) and made the history Void button kind-aware ("Reverse" for a refund row → `refund:delete`, "Void" otherwise → `billing:delete`). No DB migration (slugs are code-defined, stored in `users.permissions`). tsc clean. |
| 2026-07-19 | **Finance document/delivery edge completed (4 gaps) + refund ACL split.** Audited finance vs docs/finance-plan.md; engine was complete but 4 plan items lived only on the document/delivery edge — all now shipped, same feature+ACL gates. **(1) Receipt + statement (print):** `/clinic/appointments/[id]/receipt` (money received + running balance) and `/clinic/patients/[id]/statement` (per-visit bill/collected/outstanding + ledger + closing balance/credit), both via the shared `InvoicePrintFrame` (thermal/A5/A4); linked from the payment panel + patient Account card; `sales`+`billing:view`. **(2) Payments ledger** (`/clinic/payments`, `core/finance/payments-ledger.ts`): clinic-wide money in/out register — period/doctor/method/type/patient filters, in/refunds/net summary, dual-render mobile, CSV (`?type=payments`); nav under Finance + Reports-hub card; `sales`+`billing:view`. **(3) Invoice WhatsApp delivery** (`core/notifications/billing.ts`, `AISENSY_INVOICE_CAMPAIGN`): "Send on WhatsApp" on the payment panel; two gates `billing:view`+`whatsapp:create`. **(4) Recurring-expense cron** (migration `0043` adds `expenses.recurrence`+`next_run_on`; `core/expenses/recurring.ts`; `GET /api/cron/expenses` daily 02:00 in vercel.json, `CRON_SECRET`): a recurring row is a template cloned into a plain expense each due period, advancing `next_run_on` (catch-up capped 24, idempotent); expense form gained a monthly/weekly selector. **Also: refund ACL** — dedicated `refund` resource (view/create/delete) split out of `billing:delete`, wired to a Refund form on the panel; void stays `billing:delete` (kind-aware); receptionist no refund by default. tsc clean; unit 33/33; e2e 61/61; recurring cron DB-verified (2 missed months → 2 clones, template advances, 2nd run no-ops). |
| 2026-07-20 | **Dental clinical arc (module-owned) — Phases 0–6.** The dental module went from a thin shell to a full clinical record WITHOUT touching core (the app uses zero `db.query.*`, so module Drizzle tables live in `src/modules/dental/db/schema.ts`, wired only via the `drizzle.config.ts` glob). New module tables: `dental_records` / `dental_charts` (odontogram), `perio_exams`, `lab_cases`; new CORE tables: `patient_medical_history`, `clinical_attachments` (+ `patients.photo_consent`), `treatment_plans` / `treatment_plan_items` (migrations 0044–0049). `ModuleDefinition.clinicalRecord` contract + client UI registry (`config/clinical-record-ui.ts`) keep core specialty-agnostic. Patient detail grew: allergy banner, medical/dental history, odontogram (+ printable chart), periodontal chart, imaging/documents, treatment plans (+ printable estimate), lab tracker, clinical history. New ACL resources **`attachments` / `plans` / `lab`** (+ role defaults). Each phase committed; DB-verified via tsx harnesses + rendered-HTML checks; tsc + unit + e2e green. |
| 2026-07-21 | **Dashboard finance section reworked.** Money-flow waterfall retied from "today" (vanished on a quiet day) to the **last 30 days**, reusing the P&L already computed in `getFinanceKpis` (no extra query). Top finance row now **mirrors the P&L cards** (Collected − Doctor shares − Expenses = Net profit) with sparklines; **Outstanding + Payable-to-doctors moved to the stats grid** as plain cards. Colours match P&L (Collected teal / shares blue `chart-2`) except **Expenses yellow `#eab308`**; net profit green (red on loss) via a new optional per-step `color` on `WaterfallStep`. Money-flow "Full report" → `/clinic/pl`. Verified on the running app. |
| 2026-07-21 | **Top-bar avatar → name initials fallback.** `SelfAvatar` (PanelShell) showed a generic icon with no picture while the settings page showed initials; now it renders initials too (icon only when there's no name), via a shared `staffInitials()` (derived from the full name, never the prefix — "Dr. Bilal Aziz" → "BA"). Skips the avatar request entirely when there's no picture. Account page uses the same helper so the two can't drift. Verified: three shell spots render "DA" for Demo Admin. |
| 2026-07-21 | **Clinic-ops v1 (3 items) shipped.** **(1) Prescription history** — per-patient "Prescriptions" card on the shared patient detail (approved `visits.note.prescriptions`, projected to drug lines ONLY so a `prescriptions:view`-only viewer never sees the rest of the note) + reprint via the existing `/api/prescriptions/[visitId]` PDF; verified list + real `%PDF-` (200). **(2) No-show rate** — `core/appointments/no-shows.ts` (`no_show / (completed + no_show)`, cancelled excluded, per-doctor, optional doctor filter); `/clinic/no-shows` report (`appointments:view`, NOT finance-gated, nav under Operations) + a dashboard stat + an Overview card; verified 8.1% 30d / 16.3% year. **(3) Expenses → central Trash** — `expense` entity wired into `core/trash` (list + restore + purge) and both Trash UIs (`trash` ACL); verified delete → list → restore. Also **moved `/clinic/overview` → `/clinic/reports/overview`** (consistent with `/clinic/reports/daybook`; old path 404s). No migrations. tsc clean. |
| 2026-07-21 | **Planning: infra build/deploy split (docs/todo.md §B/§Z).** Decided to build all **non-deploy-gated** platform/infra now (§B: in-app + email-CODE notifications, Postgres RLS, rate limiting, hash hardening) and defer everything needing a chosen host or external credentials to a new **§Z Final v1 phase** — file-storage swap, connection pooler, AI API keys (Claude/Whisper), email provider, WhatsApp Cloud go-live — activated together at deploy. |
| 2026-07-21 | **Login rate limiting (brute-force gate).** `core/security/rate-limit.ts` — a dependency-free in-memory fixed-window `Limiter`. `signIn` throttles FAILED logins: 5/15min per username (reset on success) + 50/15min per IP (generous, so a NAT'd clinic isn't locked out), checked before any DB/hash work; over-limit → "Too many attempts, try again in N minutes" without revealing which credential was wrong. In-memory = per-instance (correct for a single box; swap for a shared store at §Z multi-instance). Password hashing already uses the async (chunked) bcryptjs API; the native-hash swap stays §Z. Verified via the real login action: 1–5 "Incorrect…", 6th locked; a fresh username still allowed (per-key). |
| 2026-07-21 | **Tenant-scope guard (RLS substitute).** Chose a query-layer guard over native Postgres RLS (RLS needs a per-request DB session / connection-pinning that fights the shared-pool + parallel-`Promise.all` model — high risk, low incremental value for a trusted single-tier app). `core/db/tenant-guard.ts`: derives every `clinic_id` table from the schema and, via a Drizzle `logger` hook, flags any query touching one without a clinic_id scope; `unscoped("reason", …)` opt-out for intentional cross-tenant reads; WARN by default (telemetry, never blocks), `TENANT_GUARD_STRICT=1` THROWS (tests/CI). `users` excluded (auth lookups pre-date a known clinic). **Wrapping pass:** wrapped every cross-tenant system path (admin logs, the 3 crons, both WhatsApp inbound webhooks) + scoped one id-only whatsapp_messages update. Verified: strict mode is clean end-to-end — unit + e2e 61/61, zero flags on any clinic/admin/cron/webhook surface. |
| 2026-07-21 | **In-app notifications (bell) — P1–P3.** CORE per-user alerts. **P1:** `notifications` table (migration 0050, partial `WHERE read_at IS NULL` index for O(index) unread) + `core/notifications/in-app.ts` (best-effort `notify`/`notifyMany`/`notifyUsersWithPermission` + self-scoped `getUnreadCount`/`list`/`markRead`/`markAllRead`). **No new ACL resource** — the inbox is self-scoped; targeting uses EXISTING permissions (`notifyUsersWithPermission`). **P2:** `NotificationBell` in the shell (server-rendered badge, dropdown, mark-read, deep-links, 60s-poll + on-focus refresh; no websockets). **P3:** 5 triggers — discount approval needed (new-pending dedup) / decided (actor-excluded), WhatsApp inbound (booking/reschedule→appointments:edit, message→whatsapp:view), doctor payout. Appointment alerts **deep-link to the specific appointment** (handlers now return the id). Firebase/Redis/realtime weighed → deferred to v2 (60s poll fits clinic staff + keeps notifications in our Postgres, no PII to Google). Verified under strict guard (exact recipients, non-holders/actor excluded, idempotent) + a real WhatsApp booking → notification opens that appointment; e2e 61/61. See docs/notifications-plan.md. |
| 2026-07-21 | **Email notifications (code) — P1–P2 (password reset).** `core/notifications/email.ts`: provider-agnostic SMTP channel (nodemailer), config-gated like WhatsApp → no-op-with-log until `SMTP_*` env is set (live send = §Z). Branded HTML+text template. `password_reset_tokens` (migration 0051, sessions pattern — SHA-256 single-use expiring) + `core/auth/password-reset.ts` (enumeration-safe request; validate; consume = set bcrypt hash + burn tokens + **revoke all sessions** + clear must_change_password) + reset rate limiters. Public `/forgot-password` + `/reset-password` pages (token validated server-side) + a "Forgot password?" link on login. **No new ACL** (pre-login). Verified end-to-end on the running app: token lifecycle, no enumeration, real reset POST → 303 /login?reset=1, new password works / old rejected / token single-use; e2e 61/61. See docs/email-plan.md. |
| 2026-07-21 | **Generic route throttle (AI-scribe cost cap).** Reusable `throttle()` (peek-then-hit) + `tooManyRequests()` (429 + Retry-After) on the existing `Limiter` (`core/security/rate-limit.ts`) — drop-in for any Route Handler. `/api/ai/scribe` is now per-user throttled (20/10min) so a looping recorder can't run up paid Whisper+Claude spend. Verified: 20 pass, 21st→429 (retry-after 599); e2e 61/61. |
| 2026-07-21 | **Cleanup — dead module navItems + 126 orphaned test users.** Removed the `ModuleDefinition.navItems` field / `NavItem` type / `getClinicWorkspace` aggregation / dental config — it was computed but rendered NOWHERE (PanelShell drives nav). Deleted 126 `clinic_id NULL` non-super-admin user rows (machine-named e2e/verify leftovers; the FK set-null on clinic-delete kept them). **Corrected a stale todo:** the "dead `/doctor` + `/reception` route stubs" are NOT stubs — those dirs house ~10 shared components `/clinic` imports (`appointments-list`, `invoice-print`, `scribe-panel`, …), so they can't be deleted without a refactor → reframed as v2. tsc + e2e 61/61. |
| 2026-07-21 | **Security + performance pass (A–D).** (A) **Response security headers** via `next.config` — HSTS, X-Frame-Options DENY, nosniff, Referrer-Policy, Permissions-Policy (`microphone=(self)` for the scribe), `poweredByHeader:false`. (B) **AI-scribe audio cap 25MB** checked from **Content-Length BEFORE parsing** (clean 413, no buffering; 500 previously came from Next's body limit) + a post-parse guard. (C) **Per-request dedup** — `getSessionUser` wrapped in React `cache()` (layout+page+guards call it 2–3× → ONE session⋈users lookup) + a new `cache()`-wrapped `getClinic()` (layout+dashboard → ONE clinics read). (D) **WhatsApp webhook secret via `x-webhook-token` header** (out of the URL/logs; query still works). Verified on the running app: 5 headers present + no X-Powered-By; 28MB→413; /clinic 200; header-token→200/none→401; e2e 61/61. |
| 2026-07-21 | **Scale pressure-test (10k+ clinics) → docs/scale-plan.md.** Reviewed the whole app at fleet scale. Verdict: the **data model scales** (every query is `byClinic()`-scoped + indexed → per-clinic cost stays flat), but several pieces need additive infra as active-clinic count grows: **in-memory rate limits → Redis** (per-instance leak when horizontal), **connection pooler**, **S3 storage**, **crons process all clinics sequentially with an N+1** (→ BullMQ queue + join), **super-admin cross-tenant scans** (`listAllTrash`/`/admin/logs` unbounded → paginate), **append-only tables** (activity_logs/notifications/whatsapp → retention + partitioning; the notification prune becomes mandatory), then read replicas + partitioning. Captured with trigger points; all slot into existing seams, no rewrite. |
| 2026-07-21 | **CSP report-only (nonce-based, host-agnostic).** `proxy.ts` (Edge) mints a per-request nonce (Web Crypto), exposes it via `x-nonce`, and sets `Content-Security-Policy-Report-Only` (strict `script-src 'nonce' 'strict-dynamic'`; `'unsafe-eval'` dev-only for HMR; `style 'unsafe-inline'`; img/media `data:`+`blob:`; `frame-ancestors 'none'`; `report-uri /api/csp-report`). Root layout nonces its theme script (+ `suppressHydrationWarning` — the browser blanks the nonce attribute post-parse, tripping hydration). New `/api/csp-report` logs violations (no auth/DB). Verified: report-only header present, enforcing CSP absent (never blocks), Next auto-nonced its framework scripts (10 nonce'd `<script>` → **ready to enforce**); e2e 61/61. Flip-to-enforce recipe in docs/scale-plan.md §4. |
| 2026-07-21 | **v1 launch-readiness assessment → docs/launch-checklist.md.** Verdict: **no more feature-building for v1** — the clinic app is complete; what remains is a deploy/go-live checklist. Consolidated ALL 9 launch blockers into **todo.md §Z** (host+TLS, prod Postgres+migrations `0000–0051`+seed super-admin, secrets, server timezone, cron scheduling, `serverActions.allowedOrigins`, DB backups) alongside the external integrations (AI keys, email SMTP, WhatsApp go-live — the longest-lead item). Key insight: on a **single Linux/Windows VM**, local storage + no pooler are fine → not launch blockers (only for serverless/multi-instance). Also: §A (payments / SaaS billing / marketing) all → **v3 commercial**; Load/scaling hardening moved to §Z; staff-invite email dropped. |
| 2026-07-22 | **WhatsApp template-variable hardening + template docs.** Meta rejects any send whose body variable is blank / whitespace-only / has a newline, and a template has a FIXED param count — which would otherwise force with/without-token template splits. **(1)** `sendWhatsAppTemplate` (`core/integrations/whatsapp`) now sanitizes EVERY param — collapse whitespace, trim, substitute `—` for a blank — so event vars (booking queue token `{{6}}`, a doctor-less booking, recall clinic `{{3}}`) are never empty. **(2)** `getClinicSender` resolves the always-present trailing `{{signature}}` via a **fallback chain** (clinic signature → WhatsApp sender name → clinic name) so it's never blank and the Cloud param count always matches. **Net: no template variable can ever be empty/missing** → one approved wording per template, no variant splits. Also **documented all 9 outbound/reply messages** (triggers, campaign env vars, exact `templateParams` order, ready-to-paste sample bodies) in docs/whatsapp-cloud-plan.md §C/§C.1/§D and **corrected the stale `{{note}}`** references (that per-event var was removed in migration 0030 — templates are signature-only). Resolves the §Z "always-present variable" go-live decision. Verified: no-signature clinic → clinic-name fallback, a set signature wins; tsc + e2e 61/61. |
| 2026-07-22 | **Super-admin Migration A (control-plane foundation).** Migration `0052`, additive. `clinics` gains the SaaS control plane: lifecycle (`status` default `'active'` so existing clinics stay usable, `trial_ends_at`/`activated_at`/`suspended_at`/`suspend_reason`), owner+contact (`owner_name`/`owner_email`/`owner_phone`/`country`/`city`/`address`/`region`/`timezone` default `'Asia/Karachi'`), billing (`monthly_price`/`billing_cycle` default `'monthly'`/`grace_days` default 7), **granular per-clinic control** `capabilities` text[] (NULL = all allowed), and `notes`. New `clinic_payments` table (manual clinic→FlexicaAI ledger, mirrors `patient_payments`: amount/method/reference/`months_covered`/note/occurred_at/recordedBy snapshot, soft-deletable). `users` gains 2FA (`totp_secret`/`totp_enabled`/`totp_backup`); `sessions` gains `impersonated_clinic_id` (support impersonation, later feature). See docs/super-admin-plan.md §11. |
| 2026-07-22 | **Super-admin Feature 1 — panel 2FA (TOTP).** `core/auth/totp.ts` — dependency-free RFC-6238 TOTP (base32 secret, HMAC-SHA1, 30s/6-digit, ±1 skew window) + one-time backup codes (SHA-256-hashed, single-use); **verified against the RFC 6238 test vectors** (T=59→287082, T=89→359152). `/admin/security` enrolment page (super_admin-guarded): begin (mint secret + `otpauth://` for manual key entry — no QR dep) → confirm with a live code → 10 backup codes shown once; regenerate + disable both step-up on password. Login is a **single-action two-phase challenge** — `signIn` returns `totpRequired` when the account has TOTP on, the form reveals the 6-digit field (username/password controlled so they survive React 19's post-action field reset) and re-posts; password is re-verified every pass, so the pending state grants nothing (no extra table/cookie). TOTP failures count toward the brute-force gate (username counter isn't reset until 2FA also passes). **Verified end-to-end on the running server-action HTTP surface:** correct pw + no code → 2FA challenge (no session); wrong code → "Invalid authentication code" (no session); valid TOTP → 303 /admin + session cookie; backup code → 303 + session, **reuse rejected** (single-use); wrong pw → generic error. tsc clean. |
| 2026-07-22 | **Super-admin Feature 2 — clinic lifecycle & access block.** `core/clinics/status.ts` — `isClinicUsable(clinic)` (active always; trial until `trial_ends_at`; suspended/past_due/cancelled never) is the single source of truth, enforced in **`requireRole`** — the one chokepoint every clinic panel page AND every clinic mutation passes through. A clinic-staff user whose clinic isn't usable is redirected to a new **`/paused`** page (reason + sign-out); `getClinic` is request-cached (no added query), super_admin (no clinic) is exempt, `/paused` uses `requireUser` so it never loops. Actions (super-admin only, audited): `setClinicStatus(clinicId, status, reason)` — moving to a non-usable status revokes all staff sessions (immediate lock-out; the requireRole block is the real gate, revoke is defense-in-depth), active clears the suspend fields — and `extendTrial(clinicId, days)` (base = later of now / current trial end, never shortens; re-enables a suspended clinic). UI: `ClinicLifecycle` on clinic detail (badge + Suspend-with-reason / Resume / Activate / Cancel / Reactivate / Extend +30d), shared `ClinicStatusBadge`, and a **status filter + Status column** on the clinics list. **Verified end-to-end over real HTTP:** staff → /paused for suspended/past_due/cancelled/expired-trial, usable for active + future-trial, super_admin reaches /admin, /paused bounces a usable-clinic user home, list filter + detail controls render. tsc clean. |
| 2026-07-22 | **Super-admin Feature 3 — ⭐ granular per-clinic control (capabilities).** `clinics.capabilities` is a WHITELIST of allowed `resource:action` slugs (NULL/`'*'` = all). The key move is **wrapping `can`**: `clinicAllows(caps, r, a)` + `can`/`canAccess` now compute **clinic capability ∩ user permission**, and `getCurrentUser` carries the clinic's capabilities on the user — so EVERY existing gate (nav `accessibleResourceIds`, page `requireWorkspace`, button booleans like `canCreate`, AND the create/edit/delete server actions) respects the super-admin's per-clinic control with **zero coverage-pass churn**; undefined caps (super-admin) = unrestricted, nothing else changes. Action `setClinicCapabilities(clinicId, slugs[])` stores the whitelist or NULL when all-allowed; `updateClinic` auto-allows a newly-enabled feature's slugs when a restricted whitelist exists (no silent lockout). UI: a **Capabilities** card on clinic detail reusing the staff `PermissionMatrix` (uncheck to disable an action for the whole clinic + "Allow all"); feature toggles stay in the settings form. **Verified end-to-end over HTTP** (clinic_admin, isolating capability from user-permission): caps NULL → "New appointment" shows; whitelist minus `appointments:create` → button hidden, `/clinic/appointments/new` redirects, the `createAppointment` action's `can(...)` blocks it, unlisted resources' nav hidden, page still loads via `appointments:view`; admin matrix renders. The behavior-flag catalog idea is superseded by resource:action caps (named flags deferred). tsc clean. |
| 2026-07-22 | **Super-admin Feature 4 — clinic identity & contact.** A dedicated `updateClinicContact(clinicId, …)` action (kept out of the busy `updateClinic` settings save) persists the Migration-A columns: owner name/email/phone, city/country, **data region** + **timezone**, address, and private internal notes — zod-validated (email format), empty→NULL, audited, super-admin only. UI: an "Owner & contact" card on clinic detail with curated PK+GCC region/timezone selects. **Verified over HTTP:** a full save persisted all 9 fields (timezone Asia/Dubai); a bad email was rejected with no write (confirmed via DB state — the prior `owner_name` survived, proving validation fired before the write). tsc clean. |
| 2026-07-22 | **Super-admin Feature 5 — impersonation ("view as clinic"), READ-ONLY.** `getSession` surfaces `sessions.impersonated_clinic_id`; `getCurrentUser` resolves a super-admin who has it set as a **read-only clinic_admin** of that clinic — full VIEW of the workspace, but `capabilities` clamped to `VIEW_ONLY_CAPABILITIES` (∩ the clinic's own caps) so no mutation `can()` passes; the real super-admin `id`/`username` stay for the audit trail, and the Feature-2 login-block is exempt (support views suspended clinics). Actions: `startImpersonation(clinicId, password, totp?)` — password step-up + TOTP/backup code when the super-admin enrolled 2FA (the Feature-1 deferred step-up), heavily audited, sets the flag, redirects to /clinic — and `endImpersonation()` which reads the REAL session user (resolved role is clinic_admin, so requireRole can't be used), clears the flag, audits, returns to the clinic detail. UI: "Open workspace (view as clinic)" + step-up form on clinic detail, and a persistent amber **"Viewing {clinic} as support — read-only · Exit"** banner via a new PanelShell `banner` prop. **Verified end-to-end over HTTP:** no-impersonation super-admin → /clinic bounces to /admin; flag set → /clinic renders the dashboard + banner, "New appointment" absent, `/clinic/appointments/new` redirects (read-only); a suspended clinic stays reachable while a normal staffer of it gets /paused (exemption is specific, not a hole); `endImpersonation` form → 303 to the clinic detail + flag NULL in the DB. `startImpersonation` = proven password step-up + proven session write. tsc clean. |
| 2026-07-22 | **Super-admin Feature 6 — manual clinic billing (paid-through + carry-forward).** `core/admin/billing.ts` (mirrors `core/billing/*`, one tier up): PURE `computeClinicBalance` — date-driven `paidThrough = (activatedAt ?? createdAt) + Σ months_covered`, `billingStatus` free/active/due(grace)/overdue, carried-forward `owed = monthsOverdue × price`; `getClinicBilling`, `recordClinicPayment` (extends paid-through then syncs), `voidClinicPayment` (soft-delete + sync), `listDueClinics` (cross-tenant `unscoped`), `sweepClinicBillingStatus`. **Auto-status** `syncClinicBillingStatus` flips `clinics.status` **active↔past_due ONLY** (trial/suspended/cancelled untouched) → feeds the Feature-2 login-block; recovery on payment, time-based downgrade via the new **`/api/cron/billing`** daily sweep (vercel.json 03:00, CRON_SECRET). Actions (super-admin, audited): `setClinicPrice` / `recordClinicPaymentAction` / `voidClinicPaymentAction`. UI: a **Billing card** on clinic detail (price/cycle/grace · balance summary · record-payment form · history + void) + a **due/overdue list on `/admin`**. Uses the Migration-A `clinic_payments` table. **Verified end-to-end over HTTP:** price 5000 + activated 120d ago → card Overdue/carried-forward; cron → active→**past_due** (`changed:1`); record-payment action (6mo/30000) → payment persisted (recordedBy=admin) + status recovered **past_due→active**, card Active/Rs 30,000/history, `/admin` list cleared; void (soft-delete)+sweep → back to past_due. tsc clean. |
| 2026-07-22 | **Super-admin Feature 8 — company financial dashboard ("how much are WE earning").** `core/admin/metrics.ts#getCompanyMetrics` (all cross-tenant aggregates inside ONE `unscoped`, grouped/summed in SQL — no per-clinic loops): clinics-by-status · new-this-month · **MRR** (Σ active `monthly_price`) · **collected this month / year** (Σ `clinic_payments`) · **overdue total/count** (reuses the billing balance math) · **6-month collection trend** · **top clinics by revenue**. UI: `CompanyMetricsPanel` at the top of `/admin` — KPI cards (MRR · collected + trend sparkline · overdue · total clinics) + clinics-by-status breakdown + top-clinics list (Feature-6 due/overdue list below). AI/WhatsApp **cost + gross margin** deferred (need Feature-7 unit-cost config). **Verified over HTTP:** one active priced clinic (5000) + two payments (5000+15000 this month) → dashboard shows MRR **Rs 5,000**, Collected this month **Rs 20,000**, the clinic in Top clinics, status breakdown + sparkline; nested `unscoped` (metrics→listDueClinics) clean. tsc clean. |
| 2026-07-22 | **Clinic status toolkit — payment-due banner + connectivity indicator (all staff).** Two always-on signals in every clinic user's shell. **Connectivity:** a client `ConnectionStatus` in PanelShell combines browser online/offline events with an active `/api/ping` probe (20s) → catches "wifi up but server unreachable"; silent while healthy, red pill offline / brief green on recovery. New auth-free `/api/ping`. **Payment due:** the clinic layout warns ALL staff while the subscription is past paid-through but still usable — amber "Payment due" (within grace), red "Payment overdue" + owed (pre-lock window); priced clinics only, skipped during impersonation, via a new lightweight `getClinicBalanceSummary` (single Σ months/Σ amount aggregate). Notices now STACK in the shell banner (impersonation + payment + announcements). Also: **Feature 7 (usage & cost) deferred to final-phase v1** (needs live AI/WhatsApp unit costs). Verified over HTTP: due→amber, overdue→red+owed, free→none, dashboard still reachable; /api/ping 200. tsc clean. |
| 2026-07-22 | **Feature 9 — internal super-admin RBAC (sub-roles).** `core/auth/admin-permissions.ts` — a COMPANY-side capability catalog (`clinics:manage`/`capabilities:manage`/`billing:manage`/`impersonate`/`announcements:manage`/`delete`/`purge`/`metrics:view`) stored in `users.permissions` (separate namespace from clinic slugs; NULL = owner/all so the seed admin is unchanged). Sub-role presets owner/support/billing; `canAdmin`/`isAdminOwner`/`adminSubRoleOf`. `requireAdminCapability(cap)` + `requireAdminOwner()` guards — EVERY admin action gated by its capability, the `/admin` metrics panel + due list gated on `metrics:view`. New owner-only **`/admin/team`** (add super-admin + sub-role, change sub-roles, suspend/reactivate; can't demote/suspend self). Verified over HTTP: owner reaches team/announcements/metrics; a billing sub-role is redirected from /admin/team (owner-only) + /admin/announcements (no cap), keeps the clinics list + metrics. tsc clean. |
| 2026-07-22 | **Feature 10 (part 1+2) — announcements + per-clinic data export.** **Announcements** (migration 0053): `announcements` table (`clinic_id` NULL = broadcast to all, else targeted; level info/warning; active + optional window). `core/admin/announcements.ts` (all cross-clinic reads `unscoped`); `/admin/announcements` post/toggle/delete; shown in the clinic notice bar. **Data export:** `core/admin/export.ts#exportClinicData` + `GET /api/admin/clinics/[id]/export` (super-admin + `clinics:manage`, 403 otherwise) → full JSON dump (clinic + staff[safe] + patients/appointments/visits/recalls/procedures/payments/invoices/expenses/leave), staff exported WITHOUT auth secrets; "Export data (JSON)" link on clinic detail. Verified: announcements show broadcast+targeted in the clinic bar + create action persists; export → 200 attachment (6 staff/40 patients/206 appts), no `password_hash`/`totp`, 403 unauth. Deferred (scale-triggered): WhatsApp provisioning tracker, bulk actions, onboarding checklist. tsc clean. |
| 2026-07-22 | **Team ACL + password step-up on delete.** Team management is now a grantable capability, not a hardcoded owner/super_admin gate: new `team` resource in `ADMIN_RESOURCES` (view/create/edit/delete). Team pages + every team action gate on the specific `team:*` slug (`requireAdminCapability`) — page/nav on `team:view`, Add form on `team:create`, profile edits/password/capabilities/state/reassign on `team:edit`, delete on `team:delete`; without `team:edit` the profile renders read-only. **Delete now requires a password step-up** — `deleteSuperAdminAction(userId, password)` re-verifies the actor's own password via the shared `verifyCurrentUserPassword` + the reusable `ConfirmDeleteDialog` (same pattern as clinic delete). **Anti-escalation:** `canGrantAdminCapabilities` — an actor can only grant/create capabilities they hold themselves (a full admin/owner grants anything). Retired the `teamManager`/`canManageTeam` nav plumbing + `requireTeamManager` (Team nav is now `cap:"team:view"`). **DATA backfill (run on deploy):** appending capability slugs grows the "full set", so any explicitly-full super_admin (perms stored expanded, marker `purge:delete`) must get the 4 `team:*` slugs appended — owners (NULL perms) are unaffected. **Verified end-to-end over HTTP:** page/nav/add-form/profile-forms/capability-editor/danger-zone gate correctly across owner / view+create+edit / view-only / no-team roles; delete with wrong password → `"Incorrect password."` + record stays live, correct password → soft-deleted; escalation guard blocks granting `billing:delete` from an actor lacking it (perms unchanged) yet allows granting held caps; backfill applied to the one full admin. tsc clean. |
| 2026-07-22 | **Team suspend/deactivate: styled in-app modal instead of native `confirm()`.** New reusable core `ConfirmDialog` (same portal/overlay/Escape styling as `ConfirmDeleteDialog`, no password) for consequential-but-reversible actions; the team Danger zone uses it for Suspend + Deactivate (destructive-styled confirm since it unassigns clinics). Verified: triggers render per state, modal content ships in the client bundle, no native `confirm()` remains. tsc clean. |
| 2026-07-23 | **Admin dashboard: scoped metrics + ARR + revenue ACL.** (a) `getCompanyMetrics({ assignedTo })` scopes every KPI (MRR/collected/overdue/totals) to a team member's assigned clinics — matching the already-scoped clinic list (clinic queries on `assigned_to`; payment queries via EXISTS on the owning clinic; overdue via the same assignee filter); owner/super_admin stay company-wide + a "covers your assigned clinics only" caption. (b) Added **ARR** (= MRR × 12) as a sub-line on the MRR card. (c) New **`revenue`** ACL resource (`revenue:view`) gates the MRR **and** ARR figures specifically — owner + super_admin by default, grantable to others; separate from `metrics:view` so a scoped user sees the panel without headline revenue. The MRR card is server-rendered only when `revenue:view` holds, so the value never reaches the browser otherwise. DATA backfill: `revenue:view` appended to full super_admins. **Verified over HTTP:** owner MRR 8,000/collected 20,000/4 clinics vs scoped sales MRR 5,000/collected 8,000/1 clinic + caption; a granted user → MRR 5,000 + ARR 60,000; a user without `revenue:view` → panel shown but no MRR/ARR and "MRR" absent from HTML (overdue KPI still shown). tsc clean. |
| 2026-07-23 | **Owner Finance Phase 1 — company serving-cost tracking** (docs/owner-finance-plan.md). The company-side finance module's cost half. Migration 0057: `platform_cost_rates` (company-level unit-cost config — per scribe call, per WhatsApp message, USD + USD→PKR FX, versioned by `effective_from`). `core/admin/cost.ts` — `getCostRates`/`setCostRates`/`computeServingCost` estimates variable cost as counts × rates (scribe = `visits` with audio, outbound `whatsapp_messages`), per clinic + total, `unscoped`. `/admin/finance/costs`: this-period cost KPIs + editable rate card + per-clinic breakdown. New **`finance`** admin ACL resource (view/create/edit/delete) — owner + super_admin default, grantable; full super_admins backfilled. Retrofit (same day): period filter (`resolveSalesRange` + `FilterSelect`/`DateRangeFields`) + a cost trend chart (`MultiBarChart`, scribe vs WhatsApp per bucket); `computeServingCost` takes a `ResolvedRange`, returns per-bucket trend in one local-time pass. Verified over HTTP: ACL (owner/full · finance:view read-only · none bounced), save writes a new rate version, estimate matches counts×rates per clinic, period filter recomputes (30d 4 scribe/10 WA/Rs 21 vs quarter 7/18/Rs 37). tsc clean. |
| 2026-07-23 | **Owner Finance Phase 2 — company operating-expenses ledger** (migration 0058). `company_expenses` + `company_expense_categories` — FlexicaAI's OWN opex (payroll/rent/software/…), company-level (NO `clinic_id` → tenant guard ignores them, no `unscoped` needed). `core/admin/company-expenses.ts` (CRUD, soft-delete/restore, `companyExpensesTotal`/`ByCategory`/`Trend`) mirrors the clinic Expenses one tier up + adds the graph aggregates. `/admin/finance/expenses`: filters (period/category/method/search/**Trash**), **graphs** (monthly trend `MultiBarChart` + by-category `HBarChart`), add/edit/delete + category management. Recurring cron `/api/cron/company-expenses` (+ vercel.json, reuses the clinic recurring date math). Nav: "Company finance" is now a group (Serving cost · Operating expenses). Gated by `finance` — page `finance:view`, mutations `finance:create|edit|delete`. **Verified over HTTP:** ACL 4 roles (owner/full · view-only read-only · none bounced); period/category/method/search filters; both graphs; soft-delete → Trash view → restore; recurring cron generated a due copy; totals (30d Rs 170k → Rs 200k after the recurring copy). Bug caught + fixed in verification: `formatValue={rs}` passed a function to a client component (Next forbids) → dropped it (matches the clinic P&L HBarChart usage). Create action covered by proxy (identical cost-rates form replay + cron-driven insert). tsc clean. |
| 2026-07-23 | **Owner Finance Phase 3 — company P&L dashboard.** `core/admin/pnl.ts#getCompanyPnl(range)` — CASH basis: **collected revenue** (`clinic_payments`) **− serving cost** (`computeServingCost`) **− operating expenses** (`companyExpensesTotal`) **= net profit**; plus gross margin, margin %, per-clinic margin (revenue − its serving cost; opex is company-wide), and a per-bucket revenue/cost/profit trend (zips the serving-cost + opex + revenue trends over one `ResolvedRange`). MRR/ARR carried as the run-rate. `/admin/finance` (new nav group root): headline **net profit** (green/red) + component KPIs + gross margin, a `MultiBarChart` trend with a status-coloured profit series, and a per-clinic margin table (lowest first — surfaces clinics that cost more than they pay). **CSV export** `/api/admin/finance/pnl/export` (UTF-8 BOM; summary + per-clinic + trend sections; 403 without `finance:view`). Period filter reuses `CostFilters`; MRR/ARR gated on `revenue:view`. Nav "Company finance" group now = P&L · Serving cost · Operating expenses. **Verified end-to-end over HTTP:** net profit matches independent DB totals (Rs 19,654 = 30,000 − 346 − 10,000; 65.5% margin); revenue/serving/opex/gross-margin KPIs; per-clinic row (PNL Clinic 30,000/36/29,964) + negative margins sorted first; trend chart; MRR shown for `revenue:view` / hidden without; CSV export 200 (correct figures) + 403 for a no-finance user; the page bounces a user lacking `finance:view`. Completes the Owner Finance v1 (cost + opex + P&L). tsc clean. |
| 2026-07-23 | **Owner Finance Phase 4 — clinic subscription invoices** (migration 0059). Invoices FlexicaAI issues TO clinics for their subscription (distinct from patient `invoices`). `company_settings` (singleton — company-global invoice counter + prefix `KL-INV-`) + `clinic_invoices` (numbered, period, amount, soft-delete). `core/admin/clinic-invoices.ts`: `issueClinicInvoice` allocates the number under a `FOR UPDATE` lock on `company_settings` (mirrors the patient-invoice pattern one tier up), `list`/`void`/`restore`, `invoicedTotal`/`invoicedTrend`, `getClinicInvoiceForPrint`. `/admin/finance/invoices`: filters (clinic/period/Trash), invoiced-total KPI + trend `MultiBarChart`, an issue form that pre-fills the clinic's `monthly_price`, and a ledger with a **printable receipt** (`[id]/print`, reusing the shared `InvoicePrintFrame` — FlexicaAI issuer → clinic bill-to) + void/restore. Nav group gains "Subscription invoices". Gated `finance:view` (page/print) / `finance:create` (issue) / `finance:delete` (void). `clinic_invoices` carries `clinic_id` so cross-tenant reads run `unscoped`. **Verified end-to-end over HTTP:** ACL 4 roles (owner/full · view read-only · none bounced); issuing via the real form produces **sequential numbers** (KL-INV-1 @5000, KL-INV-2 @4500, counter→3); list + invoiced total Rs 9,500 + trend; print page (label + bill-to clinic/owner + FlexicaAI issuer + amount) and print gated for a no-finance user; clinic filter; void (view-only 303, full 200) → Trash view → restore. Clinic refund/credit deferred (optional). tsc clean. |
| 2026-07-23 | **Owner Finance — clinic refund/credit** (migration 0060, completes the Phase 4 deferred bit). `clinic_payments.kind`: `payment` (money IN, +balance, +cash revenue), `refund` (money OUT, −balance, −cash revenue), `credit` (non-cash goodwill, +balance, NOT cash revenue). `core/admin/billing.ts`: `signedBalanceAmount` + `computeClinicBalance`/`getClinicBalanceSummary` are sign-aware (a refund subtracts from paid), `recordClinicPayment` takes a `kind` (refund/credit carry `months_covered` 0). **Collected revenue is now CASH-aware everywhere it's summed** — `getCompanyPnl` (P&L) + `getCompanyMetrics` (dashboard MRR-panel collected/trend/top) count payment − refund and EXCLUDE non-cash credit, via one `case when kind='refund' then −amount when kind='credit' then 0 else amount end` fragment. UI: the clinic Billing card's record form gains a **Type** select (Payment / Refund / Credit) — follow-up fields show only for a payment — and the history shows Refund/Credit badges + signed amounts. **Verified end-to-end over HTTP:** a clinic with pay 8000 / refund 2000 / credit 500 → balance paid 6500, credit Rs 1,500, history badges + −Rs 2,000; record-refund via the real form stored `kind='refund'`/`months_covered=0`; company collected went cash-aware **Rs 6,000** (not the naive 10,500); P&L per-clinic revenue reflects it. Also fixed a P&L bug found in verification — a revenue-only clinic (no serving cost) rendered its name as "—" (added a clinic-name lookup). tsc clean. |
| 2026-07-23 | **Owner Finance — serving cost + gross margin on the main `/admin` dashboard.** Fills the slots that were stubbed in `getCompanyMetrics` now that Feature 7 (cost) exists. New `getCompanyMetrics({ withCost })` → `servingCostThisMonth` (AI scribe + WhatsApp, counts × the current rates) + `grossMarginThisMonth` (collected − serving cost) + `hasCost`; computed ONLY when `withCost` (so the extra count/rate queries are skipped for viewers who won't see them), scoped to the assignee via the same EXISTS pattern as the payment scope. `/admin` passes `withCost: showRevenue` (revenue:view — same gate as MRR/ARR), and `CompanyMetricsPanel` renders the two KPIs (margin green/red) only when `showRevenue && hasCost` — server-rendered, so nothing leaks otherwise. **Verified over HTTP:** owner sees Serving cost **Rs 382** + Gross margin **Rs 19,618** (= collected 20,000 − 382, matching an independent company-wide calc); a `metrics:view`-only user (no `revenue:view`) sees neither and "Serving cost" is absent from the HTML. Completes the Owner Finance module deferrals (only precise token/minute AI metering + QBO/Xero export templates remain). tsc clean. |
| 2026-07-23 | **Owner Finance — precise token/minute AI metering** (migration 0061). Replaces the flat count×rate scribe estimate with real usage. `ai_usage` table (one `whisper` row [audio_seconds] + one `claude` row [input/output_tokens] per scribe run; `cost_pkr` SNAPSHOTTED at record-time rates so a later rate change never rewrites history). `platform_cost_rates` gains `whisper_minute_cost` + `claude_input_cost`/`claude_output_cost` (per 1M tokens). The scribe engine now returns Whisper `duration` (switched to `verbose_json`) + Claude `message.usage` (input/output tokens), threaded through `runScribe`; the scribe route records it best-effort via `core/ai/usage.ts#recordScribeUsage` (never blocks the note). `computeServingCost` (costs page) + `getCompanyMetrics` (dashboard) now compute AI cost from **metered** `ai_usage.cost_pkr`, falling back to the flat `scribe_call_cost` estimate ONLY for an audio visit with no metered row (an anti-join). The rate form + read-only view gained the metered rate fields. **Verified over HTTP:** rate form saves metered rates (whisper 0.006, claude 3/15 per 1M); a metered visit (whisper 120s→Rs 3 + claude 5000in/2000out→Rs 13 = 16) + an un-metered audio visit (est 0.01×280→3) = **Rs 19** on the costs page (scribe calls 2); dashboard serving cost metered-aware (**Rs 328** = metered 16 + un-metered estimate + WhatsApp). The scribe WRITE trigger needs live OpenAI/Anthropic keys (absent in dev) — the recording math was confirmed by construction (matches the inserted snapshots). Only QBO/Xero export templates + v3 plans/dunning remain deferred. tsc clean. |
| 2026-07-23 | **Owner Overview dashboard** (`/admin/overview`) — the operational counterpart to the financial pages, fulfilling super-admin-plan §3 (usage/cost/health). `core/admin/health.ts#getClinicHealth(range)` — per-clinic ENGAGEMENT (appointments, scribe calls, WhatsApp, new patients over the period) + a global **last-activity** signal (max created_at across visits/appointments/whatsapp via a UNION) + usage **cost/margin** (reuses `computeServingCost` per clinic + cash-aware collected); flags **churn risk** = a live (active/trial) clinic with no activity for ≥ `inactiveDays` (21) or never. New `/admin/overview` page (nav's new first item, `metrics:view`): money + health KPI strip (Clinics, At-risk, Overdue, Collected, and — gated on `revenue:view` — MRR/ARR, serving cost, gross margin), an alerts row (payments due/overdue + churn-risk quiet clinics), and a per-clinic **activity & usage table** (last-active, appts/scribe/WhatsApp/new-patients, serving cost, collected, margin — money cols `revenue:view`-gated, quietest first with a "quiet" badge). Period filter reuses `CostFilters`; scoped to the assignee for non-full-access. **Verified over HTTP:** a clinic last-active 40d ago surfaces in the churn-risk alert + table (with the quiet badge) while a fresh clinic doesn't; the raw DB confirms the at-risk set (1 of 6 active/trial); a `metrics:view`-only user (no `revenue:view`) sees the page + churn/activity but NOT the money columns (Serving cost/MRR absent). tsc clean. |
| 2026-07-23 | **Overview churn: configurable threshold + logins + at-risk clinic data.** The "quiet for ≥ N days" window is a dropdown (7–90, default 21, `days` query param → `getClinicHealth` inactiveDays). The last-activity signal now also counts **staff logins** (`activity_logs` action='login') alongside visits/appointments/WhatsApp — a clinic whose staff still log in isn't wrongly flagged. `HealthRow` gained the account manager, owner phone/email and MRR; the small churn list became a dedicated **at-risk clinics table** (manager, contact, MRR, appts, scribe) — who to call. Verified: a 25-day-old-login-only clinic is at-risk at 21 not 30; a 5-day login isn't flagged; the table shows manager+phone+MRR. tsc clean. |
| 2026-07-23 | **Admin lands on the Owner Overview after login** (not the clinics list). `ROLE_HOME_ROUTE.super_admin` → `/admin/overview` + the PanelShell admin brand/home link. Every admin sub-role holds `metrics:view` (the Overview gate). Clinics list stays at `/admin`. Verified: super-admin login 303→/admin/overview. |
| 2026-07-23 | **Overview usage/cost anomaly flags.** Metering says what a clinic costs; flags say which are wrong. `core/admin/health.ts` adds per-clinic flags (withCost only): **loss** (serving cost > MRR), **thin_margin** (≥ 50% of MRR), **usage_spike** (≥ 3× the prior equal-length period, floor Rs 200 — a 2nd `computeServingCost` over the prior window is the baseline); returns a `flagged` list (loss first). `/admin/overview`: a "Usage flags" KPI + a dedicated flagged table (flags, manager, MRR, serving cost, margin) + inline badges on the activity table, all `revenue:view`-gated. Verified: 6000/5000→"Cost > MRR", 6000/10000→"High cost", 900-vs-200/20000→"Usage spike"; a no-revenue user sees none. tsc clean. |
| 2026-07-23 | **Overview anomaly thresholds tunable + persisted** (migration 0063). `company_settings` gains `thin_margin_pct` (50) / `spike_multiple` (3) / `spike_floor_pkr` (200); `core/admin/company-settings.ts` get/set (clamped, shared `upsertSettings`); `getClinicHealth` takes an `anomaly` param instead of constants. A **"Flag rules"** form on the Overview (full admin ∩ `revenue:view`) edits them; the "Cost > MRR" loss flag stays definitional. Verified: a 60%-of-MRR clinic flags "High cost" at the default 50%, drops it when saved to 80%, flags again at 40%. tsc clean. |
| 2026-07-23 | **Docs refresh.** Brought `.claude/database.md` current (was stale at migration 0043): added §3b (super-admin control plane + Owner Finance tables — `clinic_payments`, `announcements`, `platform_cost_rates`, `ai_usage`, `company_expenses`/`_categories`, `clinic_invoices`, `company_settings`), the clinic/user column additions, the soft-delete list, and the `0044`–`0063` migration index. Updated CLAUDE.md's post-MVP section (super-admin control plane · Owner Finance · Owner Overview) and this changelog. |
| 2026-07-28 | **Overview alert follow-up / snooze** (migration 0069) — the "like payment due" action for churn/usage-flag alerts. `clinics.health_followup_at` + `health_followup_note`; `core/admin/health.ts` fetches them, adds `isAtRisk`/`followupAt`/`followupNote`/`snoozed` to each row, and returns a new **`followingUp`** list. A clinic with a FUTURE follow-up drops out of `atRisk` + `flagged` (stops nagging) and the "At risk"/"Usage flags" KPIs, parked under a new **Following up** card (why · date · note · manager) until the date passes, then it re-surfaces. `setHealthFollowup(clinicId, at, note)` core mutation + `setHealthFollowupAction` (gated `metrics:view`; a scoped account manager may only action clinics assigned to them; audit-logged). New `HealthFollowup` modal (date + note + Clear, portal, mirrors `ConfirmDialog`) on each at-risk/flagged row and in the Following-up card. Also made the three Overview tables whole-row clickable via the existing `RowLink` (was chevron-only). tsc + lint clean. |
| 2026-07-28 | **Payment-due notice: toggle + pill restyle** (migration 0070). `clinics.payment_notice_enabled` (bool, default true) lets the owner / full super-admin / a clinic's **account manager** switch the clinic-facing subscription-due reminder on/off per clinic (`setPaymentNoticeEnabled` + `setPaymentNoticeEnabledAction`, gated `metrics:view` + assignee scope, audit-logged) — a toggle on the clinic billing card (not billing-edit-gated, so a billing:view account manager can still flip it). The reminder itself moved from a top banner to a floating **bottom-centre pill** (`core/ui/payment-notice-pill.tsx`) matching the `ConnectionStatus` no-internet indicator (amber = due, red = overdue, **no amount shown** — a nudge, not an invoice). Both pills now share ONE bottom-centre stack in `PanelShell` (a `flex-col-reverse` container, new `bottomPill` prop): the connectivity pill sits at the very bottom, the payment pill stacks directly above it, and when one is absent the other drops to the bottom — so they never overlap. `ConnectionStatus`/`PaymentNoticePill` no longer self-position. The toggle affects ONLY the soft clinic-facing pill — not the super-admin dues dashboard nor the hard `past_due` access lock. `src/app/clinic/layout.tsx`. tsc clean (the one PanelShell lint is pre-existing). |
| 2026-07-28 | **Overdue no longer auto-pauses a clinic; pausing is manual + password-gated.** Policy change: an overdue subscription NO LONGER auto-flips `clinics.status` to `past_due` (that lock is removed from `syncClinicBillingStatus`/the billing cron — the hook now only ever auto-RESUMES a legacy `past_due` clinic once its balance clears, never locks). Overdue clinics keep working and just show the dues list + the clinic-facing pill. **Pausing access is now a deliberate manual action:** `setClinicStatus` requires `canManageTeam` (owner / full super-admin — NOT an account manager) AND a password step-up (`verifyCurrentUserPassword`) for any transition to an unusable status (suspend / cancel / past_due). The clinic lifecycle UI relabels Suspend → "Pause access", adds a password field, routes Cancel through the password `ConfirmDeleteDialog`, and hides all pause/resume/cancel controls unless `canPause` (full admin). One-off data fix: existing auto-paused `past_due` clinics flipped back to `active`. tsc + lint clean. |
| 2026-07-29 | **Clinic logo on printed documents** (migration 0071, `clinics.logo_key`). Owner/super-admin/**account-manager** upload a clinic logo — on the clinic detail page (a new "Logo" card) and optionally on the new-clinic form; scoped like the rest of the clinic page (`clinics:edit` ∩ full-admin-or-assignee). Stored on local FS via `saveClinicFile` (per-clinic `logo/` subdir). Printed **as uploaded** at the top of invoices & receipts (a thermal / B&W printer renders it mono itself — CSS grayscale muddied it and brightness(0) destroyed detail, so no filter), sized per paper format (thermal 40px · A5 52px · A4 64px) via `InvoicePrintFrame`'s new `logo` prop. **Print** inlines the image as a base64 data URI (`core/clinics/logo.ts#getClinicLogoDataUri`) for reliability; the **admin preview** is served via `GET /api/admin/clinics/[id]/logo` (route, not a data-URI prop, so the big image doesn't trip Next's 1 MB Server-Action body limit on the form). Cap **1 MB** (`core/clinics/logo-limits.ts`, under the 1 MiB body limit), validated client-side (friendly error, no crash) + server. NULL logo → nothing is printed. Clinic admins can't change it (branding is company-controlled; they still own paper size). tsc clean. |
| 2026-07-29 | **Clinic Invoices: nav item + year-based numbering + richer search.** (1) Added an **Invoices** item to the clinic Finance nav (after Payments), gated `billing:view` ∩ the `sales` feature — the register was previously Reports-hub-only. (2) **Search** the register by invoice #, patient name, **phone**, **MRN** (full "KL-…" or partial; TZ-correct reconstruction), and the old **patient number** (`external_ref`) — `getInvoicesList`. (3) **New invoice-number format** `<prefix><YYYY>-<7-digit>` (e.g. `INV-2026-0000005`), the sequence **RESETS each calendar year** (migration 0072: `clinics.invoice_year` + `invoices.invoice_year`; uniqueness now (clinic, year, no); allocation locks the clinic row and restarts at 1 on a year rollover). `formatInvoiceNo()` centralises the label. **Existing invoices re-rendered** in the new format (backfilled `invoice_year` from `issued_at` — all current data is 2026, so 1..N stays clean). Verified: labels, per-format search, backfill. tsc clean. |
| 2026-07-29 | **Payment-receipt numbering (RCP series) + receipt breakdown + ledger search.** Per-visit receipt number `<receipt_prefix><YYYY>-<7-digit>` (e.g. `RCP-2026-0000012`), a series distinct from invoices, **allocated once on the first money-in** for an appointment (`core/billing/payments.ts#ensureReceiptNumber`, in the `recomputeCollected` hook, clinic-row-locked) and **reset per year** — migration 0073 adds `clinics.receipt_prefix/next_receipt_no/receipt_year` + `appointments.receipt_no/receipt_year` (partial-unique per clinic+year). Existing PAID visits backfilled (sequential per clinic/year by first-payment order). The **receipt page** now prints the `RCP-` number in the header and **restores the per-payment breakdown** (date · type · method · amount) so a partial-payment receipt is self-explanatory under its one number. The **Payments ledger** (`/clinic/payments`) now **shows a Payment # (RCP) column** (list + CSV) and is **searchable by payment # (RCP) and MRN #** (+ existing name/phone) — `core/finance/payments-ledger.ts` (TZ-correct MRN reconstruction). Advances (no appointment) show "—". `formatReceiptNo` reuses the invoice format. Verified: backfill, RCP label, search by RCP/partial/MRN/name. tsc clean. |
| 2026-07-29 | **Appointment create/edit flow + Upcoming/Past toggle + per-row receipt print.** (1) **Create** an appointment now redirects to the NEW appointment's detail page (`/clinic/appointments/[id]?created=1`, "Appointment scheduled." flash) — where the payment panel + Print bill/receipt live — instead of the list, so staff collect the fee / print immediately without hunting. **Edit** stays on the edit form with a "Changes saved." toast (both no longer bounce to the list). (2) The appointment list gains an **Upcoming / Past** direction toggle (a `PeriodTabs` group): the period pills (Today/7d/15d/30d/Quarter/6mo/Year) window FORWARD (upcoming, default) or BACKWARD (past — review completed visits / no-shows); flipping re-applies the active preset in the new direction; direction is inferred from the URL range on reload. `apptWindow(period, today, dir)` in `reception/appointment-filters.tsx`; client-side only (sets the existing from/to). (3) Each row of the Payments ledger (`/clinic/payments`) gets a **Print** link to that payment's visit receipt (advances → "—"). tsc + lint clean. |
| 2026-07-30 | **Financial-history archive (read-only, per-transaction).** A clinic migrating off its old PMS can now bring its **old bills / receipts / expenses / doctor-payouts** into FlexicaAI for audit + lookup (docs/financial-archive-plan.md). Migration **0074** adds ONE generic `imported_transactions` table — `type` discriminator (invoice/payment/refund/expense/doctor_payout) + `raw` jsonb keeping the original row verbatim, snapshots for patient/doctor names, soft-delete + `import_batch_id`. **Upload is admin-side** (owner/super-admin/account-manager) via the existing `/admin/clinics/[id]/import` wizard — four new passes (`fin_invoice`/`fin_payment`/`fin_expense`/`fin_payout`) grouped under "Financial history"; all reuse the parse → column-map → dry-run preview → batch-commit → **undo** machinery, plus a **reconciliation totals footer** (so the operator matches the old system before committing). Patient match old-ref→phone→name (else archived UNLINKED), doctor by name; a negative payment auto-becomes a **refund**; per-doc-number dedup. **READ-ONLY by construction** — a SEPARATE table (not an `imported` flag), so NO live report (sales/payments/receivables/P&L) can see it; `core/finance/imported-history.ts` is the only reader, behind the clinic-side **`/clinic/history`** viewer (summary cards + type/period/text filters + CSV + a persistent "Historical — read-only" banner), gated by the `sales` feature ∩ `billing:view`. The one sanctioned bridge to live data: an **opt-in** (default off) toggle on the payments commit **SETS** (never adds) each patient's `opening_balance` = max(0, Σinvoices − Σpayments + Σrefunds). As-built deviations (see the plan's §13): reused the existing admin `import:create` capability (already gates PII + clinical-note imports) instead of a new slug, and extended the existing importer rather than a parallel page. Verified end-to-end against a live clinic (import/dedup/negative→refund/totals/summary/unmatched-search/derivation 3500/undo→0). tsc clean. |
| 2026-07-30 | **Clinics list — lifecycle & billing columns + horizontal scroll.** The super-admin `/admin` clinics list now shows, per clinic, **Trial start**, **Active start**, **First payment**, and **Package** (billing cycle), alongside the existing Status/Assigned/Specialties/Created. Only genuinely new data was a `clinics.trial_start_at` column (migration **0075**; stamped when a clinic first enters `trial` via `setClinicStatus`/`extendTrial`, never overwritten, existing trials backfilled from `created_at`); active start = existing `activated_at`, package = existing `billing_cycle` (with a `billingCycleLabel` helper), first payment = derived (earliest `clinic_payments` `payment` via a new `getFirstPaymentDates` grouped query, one round-trip for the page). The two billing columns (Package, First payment) show only to billing viewers (`canSeeBilling`); trial/active dates always. Scoping unchanged — owner/super-admin see all clinics, an account manager only their assigned ones. The `Table` already wraps in `overflow-x-auto`, so the widened table scrolls horizontally (min-w set). Verified column data reads against the live DB. tsc + lint clean. |
| 2026-07-30 | **UI/UX audit + Phase-1 fixes (the three P0s).** Wrote `UI_UX_AUDIT.md` (full static product/design/a11y audit — scores, P0–P3 findings, roadmap). Then shipped Phase 1: **(1)** branded `app/error.tsx` + `app/global-error.tsx` + `app/not-found.tsx` (previously ZERO across 77 pages — thrown errors/bad URLs showed Next defaults); Logo + recovery CTAs (Try again / Go to dashboard). **(2)** Rebuilt `ConfirmDialog` + `ConfirmDeleteDialog` on **Base UI `Dialog`** (modal) so focus is trapped, moved in on open + **restored to the trigger** on close, background scroll locked, Escape/backdrop dismiss — fixing the hand-rolled portals' WCAG 2.4.3 focus failures; the delete dialog keeps all its autofill-hardening. **(3)** Replaced the last **3 native `confirm()`** calls (announcement delete, billing void, import undo) with the styled `ConfirmDialog`. Same public props on both dialogs → no call-site changes elsewhere. tsc + lint clean. (Live keyboard/screen-reader verification still pending — see the audit's Method Gap.) |
| 2026-07-30 | **Audit Phase 2 (part 1): global toast queue + first-run onboarding.** **(P1-2)** Replaced the single, bottom-fixed, non-stacking, auto-only `Toast` with a real notification system: a tiny pub/sub store (`core/ui/toast-store.ts`) + an imperative `toast()` / `toast.success()` / `toast.error()` / `toast.dismiss()` API, and a single `<Toaster/>` host mounted once in the root layout that renders a bottom-centre **stack** (newest lowest, capped at 4) — each toast **auto-dismisses, has a × dismiss, and PAUSES on hover** (banked-remaining-time resume), with per-variant live-region semantics. The existing `<Toast>` / `<FlashToast>` are now thin **compat wrappers** that push into the queue, so every current call-site keeps working but gains stacking/dismiss for free. **(P1-6)** Added a first-run **`OnboardingChecklist`** on the clinic dashboard (shown to the clinic admin until the three essentials — add team → add patient → book first appointment — are done, then it self-hides): real done-state from live counts, a progress bar, and per-step CTAs. tsc + lint clean. |
| 2026-07-30 | **Audit Phase 2 (part 2): shared `DataTable` + first migrations.** **(P1-3 / P2-5 / P2-7)** Built `core/ui/data-table.tsx` — one client table component giving what the ~21 hand-rolled `<table>`s lacked: **client-side sorting** on any column (with a `sortValue`), an optional **sticky header**, a real **empty state**, and a **mobile card view** (each row collapses to a card below `md`). Column config = `{ id, header, cell, sortValue?, align?, cardTitle?, hideOnCard? }`; a server page serialises its rows and a small client wrapper defines the columns. Migrated the first two tables as proof across both patterns: the **import-history** table (already a client component — now sortable by date/type) and the **`/clinic/history`** ledger (server page → new `history-table.tsx` client wrapper — sortable columns + mobile cards, tab-shaped). Remaining raw-table screens (payments/invoices/receivables/expenses/discounts/shares/no-shows/daybook + the clinics list) still to migrate incrementally; print layouts + matrices deliberately excluded. tsc + lint clean. |
| 2026-07-30 | **DataTable rollout (batches 1–3).** Migrated the clean flat-list ledgers onto the shared `DataTable` via per-page client wrappers — each gains **sortable columns + a consistent mobile card view** and sheds its hand-rolled desktop+mobile markup: **payments**, **invoices**, **expenses** (batch 1); **discounts**, **no-shows** (by-doctor), **daybook** (by-method) (batch 2); the admin **clinic-billing payment history** (batch 3, inline since it's already a client component). Combined with the two earlier proofs (import history + `/clinic/history`), that's **9 tables** on the shared component. Deliberately DEFERRED (structurally complex — need reshaping, not a mechanical swap, and a live check): the **shares** page (multi-table + charts + payout/settlement forms), **receivables** (nested patient→visits), **P&L / reports-overview** (aggregate/by-period), and the **admin clinics list** (~10 conditional columns + whole-row links). Print layouts + matrices remain out of scope. tsc + lint clean throughout. |
| 2026-07-30 | **DataTable: whole-row links + admin clinics list migrated.** Extended `DataTable` with an accessible **`rowHref`** (built on `RowLink` — whole row + mobile card navigate, inner links/buttons still work, Enter/Space opens). Migrated the super-admin **clinics list** (`/admin`) onto it via a new `clinics-table.tsx` client wrapper: every column now **sortable** (name/status/package/trial/active/first-payment/assignee/created), a **mobile card view**, and the conditional billing columns (Package, First payment) still gate on `canSeeBilling` — replacing the bespoke `Table`+`RowLink` block. The page serialises each row (specialties pre-resolved, first-payment folded in from the map) for the client wrapper. 10 tables now on the shared component. Still deferred: shares (multi-table), receivables (nested), P&L / reports-overview (aggregate). tsc + lint clean. |
| 2026-07-30 | **DataTable: totals-footer support + the deferred report tables.** Added an optional per-column **`footer`** to `DataTable` (renders a `<tfoot>` totals row on desktop + a totals card on mobile when any column defines one). Migrated the flat tables in the three "deferred" pages: **P&L by-period**; **reports-overview** by-doctor, **cash-that-moved** (now with a proper totals footer), and discounts; and **shares** — per-doctor **balances** (sorted by outstanding) + **payouts** (conditional Doctor column when viewing all doctors, admin-only Void). **16 tables** now on the shared component. **Receivables is intentionally NOT migrated** — it's a per-patient `<details>` accordion (visits nested + expandable), which a flat table would regress; it stays as-is. tsc + lint clean. |
| 2026-07-30 | **Audit Phase 3 (part 1): semantic status-color tokens.** Killed the scattered `emerald/amber/sky` literals + hardcoded hexes the audit flagged (P2-1). Added **`--success` / `--warning` / `--info`** design tokens (globals.css, light↔dark, tuned to the emerald-600/amber-600/sky-600 the app used) mapped into `@theme` so `text-success` / `bg-warning/10` etc. are real utilities; **danger** reuses `--destructive`. Added `success` / `warning` / `info` **Badge variants**. Swapped the repeated text-tone literals (`text-emerald-600 dark:text-emerald-400` → `text-success`, +amber/sky) to tokens **app-wide (25 files, exact-match → no visual change)**. Migrated the status BADGES to the variants — `ClinicStatusBadge`, the clinic-billing status/credit/refund badges. Tokenised the **semantic** chart colours (profit→success, loss→destructive, expenses→warning) to theme-aware CSS vars (charts now adapt to dark mode); category colours (scribe/WhatsApp) intentionally kept. tsc + lint clean. |
| 2026-07-30 | **Audit Phase 3 (part 2): designed empty states.** Added a reusable **`EmptyState`** component (centred icon + title + optional description/CTA) for "no data yet" moments — replacing the bare line of muted text the audit flagged. Wired it into **`DataTable`** (new optional `emptyIcon`), so ALL 16 migrated tables get a designed empty uniformly, with icons on the high-traffic ones (clinics → Building2, payments → HandCoins, history → Archive). Reusable on pages/cards too. Also **verified the whole session with a production `next build`** (not just tsc/lint) — which caught + fixed a latent server-only-in-client import in `/clinic/history`; the build now passes for every route. tsc + lint clean. |
| 2026-07-30 | **Audit Phase 3 (part 3): loading skeletons + touch targets** (the last open P1s). **P1-1** — new `Skeleton` + `PanelSkeleton` primitives; the 4 panel `loading.tsx` boundaries now render a **content-shaped skeleton** (title → KPI cards → list rows) instead of a bare spinner, so navigation feels faster. **P1-4** — a `globals.css` `@media (pointer: coarse)` rule gives every design-system button a **40px minimum** hit area on touch/stylus devices (incl. dense table actions + square icon buttons); mouse/desktop layouts are untouched (WCAG 2.5.8). Production `next build` passes. Audit updated (all P0s + all P1s now FIXED; overall 6.5 → 8.0; remaining: breadcrumbs, form validation, dashboard deltas/charts, skip-link). |
| 2026-07-30 | **Audit Phase 3 (part 4): P2-2/P2-3/P2-8.** **P2-2** — a `Breadcrumbs` component wired into the deep admin flow the audit named (clinic detail → import) + the shared appointment detail, replacing lone "← Back" links (verified live). **P2-8** — a **skip-to-content** link in `PanelShell` (first focusable → `#main-content`, verified live) + an **icon-button aria sweep**: an exhaustive multiline search confirmed **every** icon-only button and link already has an accessible name — no gaps. **P2-3** — a `zodErrorMessage()` helper surfaces **all** validation issues (deduped/joined) across 15 server actions instead of only the first; native HTML5 attrs already cover inline field validation. Production build passes. Only P2-4 (dashboard deltas/charts — needs prior-period data + a live eyeball) and P2-6 (measured contrast) remain in P2; overall audit score 6.5 → ~8.5. |
| 2026-07-30 | **Audit P2-4: KPI trend deltas.** `getFinanceKpis` now also computes the SAME four totals for the PREVIOUS 30-day window (reusing `getProfitAndLoss` on a constructed prior range, so a delta can never disagree with the P&L report). New `DeltaBadge` — % change vs the prior period + up/down arrow, coloured by direction (`higherIsBetter`: revenue up = green, expenses up = red), rendering nothing when there's no baseline — wired into the 4 finance KPI cards on the clinic dashboard. Verified data + math against a live clinic (Clinic002: −63% collected vs prior 30d; no-baseline clinics correctly show no delta); build passes. (Dashboards already carry charts — waterfall / h-bar / multi-bar / sparklines.) **Every P0, P1, and P2 except measured contrast (P2-6) is now resolved; overall audit 6.5 → ~8.5.** |
| 2026-07-30 | **"Payments coming up" — pre-due reminder.** A proactive heads-up BEFORE a clinic falls due, complementing the existing due/overdue lists. Per-clinic **`clinics.payment_reminder_days`** (migration **0076**, default **5**), editable on the clinic detail billing card (owner / full super-admin / the clinic's account manager, scoped — `setPaymentReminderDaysAction`, gated like the payment-notice toggle; 0 disables). `listDueClinics({ includeUpcoming: true })` gained an **`upcoming`** alert bucket — a still-paid (`active`, owed ≤ 0) clinic whose `daysRemaining ≤ its payment_reminder_days` — sorted soonest-first; the default `listDueClinics()` is unchanged (metrics unaffected). Shown as a **sky-toned "Payments coming up" box on `/admin`** (below the amber dues box) and a **card on `/admin/overview`** (below "Payments due / overdue"), both showing the due date + days remaining + account manager, and both **assignment-scoped** (owner/super-admin all clinics, AM only theirs). Verified the classification via the pure billing math (paid-through in 4d → upcoming; 43d → not; unpaid → overdue) and that the default list still excludes upcoming. tsc + lint clean. |
| 2026-07-30 | **Upcoming-payment count on the company dashboard + always-visible reminder field.** (1) `getCompanyMetrics` now returns `upcomingCount` / `upcomingTotal` (from `listDueClinics({ includeUpcoming })`, scoped like the rest); the **"Clinics by status"** panel on `/admin` gained a billing row — **"Payments coming up N (Rs …)"** + **"Due / overdue N"** — under the status counts. (2) The per-clinic reminder-days field now shows on the Billing card **regardless of monthly price** (it was hidden at price 0, i.e. exactly when setting a clinic up), with a hint that it takes effect once a price is set. tsc + lint clean. |
| 2026-08-21 | **Cron jobs proven + one-command install** (delta D-19). Verified all six job routes actually run end to end against the dev DB, exactly as cron invokes them: `recalls` {processed:15,skipped:15} and `reminders` {processed:2,skipped:2} (skipped = WhatsApp unconfigured, the graceful path), `expenses` generated 2 due recurring rows, `company-expenses`/`billing`/`reconcile` clean; a second pass was a **no-op on every one** (idempotent), all logged `"op":"cron.*"` with correlation ids and **zero** error/warn reports; unauthenticated and wrong-token both 401. New **`deploy/install-cron.sh`** replaces the static `deploy/flexicaai.cron`, which is deleted — it held a second copy of the schedule, and two copies of a schedule drift like two copies of a bill formula (ADR-014). One `JOBS` table renders the crontab; `core` installs the four pure-DB jobs, `all` adds the two needing WhatsApp (D-19's two-part trigger), `print` dumps it for hand-install, `check` verifies. Because a job that is never invoked raises no error, every failure mode here is silent, so the installer front-loads them: it **refuses to write** unless the app answers and `CRON_SECRET` genuinely authenticates (401/503 caught at install, not at 03:00), and rejects a non-existent `RUN_USER`. **Bug found and fixed in review:** the secret file was to be written `0600 root:root` while the cron line runs *as* `flexica` — that user could never read it, so all six would have failed nightly into syslog only; now `0640 root:<run-user>` (root-owned so the app user can't rewrite its own credential), and `check` re-asserts both that and the user's existence. Docs updated: CLAUDE.md §2a, README, launch-checklist, `core/security/cron.ts`, architecture ADR-012 + the D-19 row. **Installing remains on hold** by the owner's direction until the WhatsApp/AI keys arrive — safe pre-launch, since with no live clinics all six are no-ops. |
| 2026-08-21 | **Staff delete now warns about stranded drafts (D-18, part 1).** Deleting a clinician revokes their login, but a draft can only ever be approved by its author (ADR-007) — so their unapproved notes become unreachable by everyone, and nothing said so. The delete dialog now asks `core/clinical/drafts.ts#countOpenDrafts` (CORE, per ADR-014) and states it before the password step: "*N* unapproved notes … deleting this account leaves them unreachable — they will not reach the patient's record, and will not appear in Trash. Have them review first, or suspend instead (that is reversible)." `ConfirmDeleteDialog` gained an optional `warning` slot rendered as a `role="alert"` callout ABOVE the password field, deliberately omitted when the count is 0 — a warning shown every time is one nobody reads. **Investigation corrected the delta:** users are SOFT-deleted, so `ON DELETE SET NULL` never fires and `visits.doctor_id` keeps pointing at the trashed account; a fix keyed on `doctor_id IS NULL` would have missed every ordinary staff deletion. The real trigger is *the author cannot authenticate*, which also covers suspend/deactivate. New `scripts/test-orphaned-drafts.ts` (suite 14) proves the count fires, that `doctor_id` survives the soft delete, that the draft is invisible in all four places it could surface (author list · another doctor's · the clinic admin's · the patient timeline · Trash), that transcript+note are still stored, and that suspension is reversible so no warning is due. Those invisibility checks PIN current behaviour and flip when D-18 is decided. tsc clean; lint at baseline; unit 14/14. **The approval question stays open** — a product decision on whose name signs a colleague's note. |
| 2026-08-21 | **D-18 closed — stranded drafts (ADR-022).** ADR-007's author-only rule is enforced in a WHERE clause, so **no permission could relax it**: deleting a clinician left their unapproved notes unreachable by everyone (the clinic admin holds ALL_PERMISSIONS and still could not see them) and invisible in every surface — not approved so the patient timeline filtered them out, not deleted so they never reached Trash, not yours so no draft list showed them. Transcript, AI note and audio of a real consultation, lost in silence. **Two parts.** (1) **Warned:** the staff-delete dialog states the count first (`countOpenDrafts`), via a new optional `warning` slot on `ConfirmDeleteDialog` (`role="alert"`, above the password field, omitted at 0 — a warning shown every time is one nobody reads). (2) **Fixed:** new **`handover`** permission resource (view/create/delete) — its own slug rather than a `clinical` action, exactly as `refund` was split out of `billing:delete`, because it is a different authority in kind (`clinical` = document YOUR patients; `handover` = finish a colleague's); `PERM_ACTIONS` is closed, so a custom action was not available anyway. Held by `clinic_admin` through ALL_PERMISSIONS, grantable to anyone, nobody else by default. `core/clinical/drafts.ts#draftAccessCondition` is the ONE predicate open/approve/discard now share (D-16 was exactly the bug of that rule living in one of three), widened only by `authorIsStranded()` = soft-deleted OR suspended/deactivated OR purged. **Deliberately narrow:** a holder cannot touch an ACTIVE colleague's draft — a grant that could would trade ADR-007 away for the whole clinic to fix the rare case. **Attribution needed no migration** — `visits.doctor_id` (dictated) and `approved_by` (signed) already existed; the patient timeline now renders "approved by X" only when the two differ. UI: a separate "Left by a former colleague" card in the scribe workspace, never merged into your own pending drafts, with the author named on every row. **Correction to the delta as written:** users are SOFT-deleted, so `ON DELETE SET NULL` never fires and `doctor_id` keeps pointing at the trashed account — the originally-planned `doctor_id IS NULL` fix would have missed every ordinary staff deletion. `scripts/test-orphaned-drafts.ts` (suite 14, 17 checks) proves the warning fires, that `doctor_id` survives the soft delete, that nothing changes WITHOUT the grant, that a holder can reach it WITH the grant, that a holder still cannot reach an active colleague's draft, that approval records both people, and that suspension strands reversibly. tsc clean; lint at baseline; unit 14/14; e2e 87/87; build clean. |
| 2026-08-22 | **D-11 closed — `activity_logs` bounded, and the view lookup indexed (ADR-023, migration `0081`).** Two problems. **(1) The one that was actually costing us:** `logView` ran a SELECT-then-INSERT to de-duplicate, and **no index served the SELECT** — Postgres walked the global `created_at` index across the dedupe window and filtered, so one user opening one patient got slower as OTHER clinics got busier, on the hottest write in the app. Now ONE `INSERT … SELECT … WHERE NOT EXISTS` against a new partial index `activity_logs_view_dedupe_idx (actor_user_id, entity, entity_id, created_at desc) WHERE action='view'`, which also removes the check-then-insert race. **A trap found and recorded while doing it:** collapsing the null-`entity_id` branch into `IS NOT DISTINCT FROM` reads better but is NOT btree-indexable — proven on 60k seeded rows, `=` gives an Index Only Scan while `is not distinct from` drops to a Bitmap Heap Scan over two indexes plus a filter, i.e. it silently gives back the entire optimisation. Both branches kept. **(2) Retention:** `company_settings.activity_log_retention_days` + `core/audit/retention.ts` + `GET /api/cron/log-retention` (7th job, 03:45, added to `install-cron.sh`), with a super-admin control on `/admin/logs` showing live row count / table size / oldest row so the window is set against real data. **Default is 0 = keep everything, deliberately** — this is the audit trail over patient data (CLAUDE.md §10) and how long an access log must survive is a regulatory decision, not an engineering one; a 90-day floor guards any window that is set, and this is the only hard delete in the audit path (soft-deleting audit rows would leave the growth problem untouched). **Partitioning considered and rejected** at this size; the revisit trigger is recorded in ADR-023. **Bug caught by the build:** the client retention form imported the `server-only` company-settings module, pulling `node:async_hooks` into the browser bundle — fixed by moving the option list to a PURE `core/audit/retention-options.ts` that both sides share, the same pattern as `fee.ts` / `permissions.ts` (conventions.md §3). Verified: retention prunes exactly the rows past the window and is idempotent; all 7 crons return 200. `scripts/test-log-retention.ts` (suite 15, 13 checks). tsc clean; lint at baseline; unit 15/15; e2e 87/87; build clean. |
| 2026-08-22 | **D-07 closed — Trash pages in SQL (ADR-024).** `collect` used to SELECT every soft-deleted row of nine tables with no limit, merge them in memory, then filter and sort in JavaScript. Under ADR-006 nothing is ever removed, so that set only grows — and `/admin/trash` has no retention window at all, so it was every trashed row the platform had ever produced, on one page. Now: every filter pushed into SQL, each source bounded to `offset + limit`, merge, cut the page, then resolve deleter/clinic names for that page only. `countAll` reuses the SAME `cond` closure the page queries were built from, so the total and the pages cannot disagree about what "matching" means. `Pagination` added to both `/clinic/trash` and `/admin/trash`; `listClinicTrash`/`listAllTrash` now return `{items, total}`. **The search had to move into SQL** — filtering after the page is cut returns short pages and a lying total; it matched deleter and clinic NAMES as well as the row's own text, so those resolve to id sets first and fold into each entity's WHERE. **Not a nine-branch UNION, deliberately:** every label is a formatted string (`Rs 400 · 12 Jan`, a leave's date range) that would have to be rewritten in SQL and kept in step with the TS rendering it, and a module cannot join a core union at all since core must never import a specialty table (ADR-001). **Two tenant-guard findings surfaced:** `listAllTrash` is cross-tenant by definition but was emitting unscoped queries the guard flagged into an unwatched void — now says `unscoped`; and a type-excluded entity short-circuited to a bare `false`, producing SQL with no `clinic_id` — the scope is appended regardless now. Also fixed 4 pre-existing guard violations in `test-tooth-amend`'s teardown (deletes by primary key, now declared `unscoped`), because a recurring violation trains people to ignore the guard (ADR-018). `scripts/test-trash-paging.ts` (suite 16, 24 checks) seeds 12 entries INTERLEAVED across four entity types and proves global ordering, that consecutive pages tile the set with no overlap or gap, that paged order matches an unpaged read exactly, that search keeps pages full and the total honest, and that a page past the end is empty rather than an error. tsc clean; lint at baseline; **guard output back to zero**; unit 16/16; e2e 87/87; build clean. |
| 2026-08-22 | **D-12 closed — reports aggregate in SQL (ADR-025).** Four reports selected every row in a date range and folded it in JavaScript. Each now aggregates at the grain it actually displays. **P&L** (`finance/pl.ts`): five unbounded scans — sales, shares, settlements, actions, expenses — became `GROUP BY date_trunc('day', …)`. The day is the finest bucket the report offers, so grouping there loses nothing and bounds every result by the LENGTH OF THE RANGE (~365 rows for a year) instead of by how busy the clinic is; days fold into weeks/months via the existing TS `startOfBucket`, so the bucketing rule is deliberately NOT duplicated into SQL. **Cash summary / day book**: `GROUP BY (kind, method)` — `aggregateCash` accumulates whatever rows it is handed, so pre-summing needed no logic change at all. **Discounts report**: totals via `sum(…) filter (where status …)`, rows paged. **Receivables**: `GROUP BY patient` — the output was always a list of PATIENTS and the appointments were only a means to it; per-visit detail is now fetched for the page alone. **The money math stayed in one place:** the discounts total needed the discount in rupees, which `computeFee` owns, so `bill-sql.ts` gained ONE `appointmentDiscountSql({ raw })` that `appointmentNetSql` is now expressed in terms of — raw vs approval-gated is a parameter, not a second copy of the clamp (the report shows a pending discount at its would-be value; the bill does not). `scripts/test-report-aggregation.ts` (suite 17, 30 checks) is a **differential** test per ADR-015: it recomputes every figure the old way, row by row in TS, from the same seeded rows. A sale is seeded at **23:30** on purpose — the day grouping depends on Postgres and Node agreeing where a day ends (the D-14 single-timezone assumption), and that check would catch it landing in the wrong bucket. Two of its own assertions were wrong and got fixed rather than the code: a 150% discount is now rejected by migration `0080`'s CHECK (D-17 defending itself), and the receivables expectation had to be DERIVED from the seeded rows rather than hand-worked. tsc clean; lint at baseline; unit 17/17; e2e 87/87; build clean. |
| 2026-08-22 | **D-01: ten app files off direct DB access (52 → 42).** The ratchet's allowlist only shrinks, so this is the delta's unit of work rather than a one-off. Migrated by CLUSTER rather than at random, because the duplication is what makes it worth doing: **`core/clinics/options.ts`** (`listClinicOptions` / `…WithPrice` / `listClinicActorOptions`) retired the same clinic-picker query written inline in FOUR pages — announcements, invoices, logs, trash — plus the actor-picker written in two. **`core/users/profile.ts`** (`getMyProfile` / `getMyAvatarKey` / `getMyTotpState`) covers the self-service reads in account, admin/account, admin/security and the avatar route; it deliberately exposes no "get any user" function, since these back pages that are NOT capability-gated and a lookup that could take someone else's id is one refactor from being handed one. **`core/audit/log-query.ts#listAdminActivityLogs`** took the super-admin log query out of its page. **`core/users/staff-export.ts`** + `listProceduresForExport` took the two CSV routes; the procedures route also stopped reading the clinic row inline and uses the request-cached `getClinic` (conventions.md §6). **Two behaviour differences found and PRESERVED rather than unified:** the logs picker includes trashed clinics (a trashed clinic still has logs) while the forward-looking pickers don't, and the logs actor list includes trashed staff while Trash's excluded them — hence `includeDeleted` / `liveOnly` flags, verified against seeded trashed rows rather than assumed. A refactor that changes what a filter returns is a bug wearing a tidy diff. Also **re-proved the ratchet fires** by re-adding a `@/core/db` import to a just-pruned file: it errored, and my first check missed it only because the grep used forward slashes against a Windows backslash path — the "a broken config reports zero problems" trap, checked rather than trusted. tsc clean; lint at baseline; unit 17/17; e2e 87/87 (covers trash + CSV exports); build clean. |
| 2026-08-22 | **D-08 closed — the scribe is a job, not a request (ADR-020, migration `0082`).** `POST /api/ai/scribe` held the doctor's connection open for the whole Whisper + Claude round trip: minutes on a single node, nginx forced to `proxy_read_timeout 300s`, and **no resume path** — a dropped run left the audio stored and the APIs billed with nothing to show. Now it stores the audio, inserts the visit as `transcribing` and returns **202**; `core/ai/scribe-job.ts` runs the AI from Next 16's `after()` and lands the result; the client polls until it leaves that state. **It cost TWO statuses, not the one ADR-020 predicted** — `failed` is not optional, because without it a dead run has nowhere to say so and the doctor stares at a spinner over a real recording. Both new states are invisible to every clinical surface *by construction* (each filters `= 'draft'`/`= 'approved'`). **Three properties hold it up:** the job CLAIMS the row before calling a provider, so a retry racing the sweep does the paid work once; a recovery cron (`/api/cron/scribe-recover`, 8th job, every 10 min) goes looking, because work not tied to a request has nothing to retry it; and that sweep matches `coalesce(transcribe_started_at, created_at)` — **a run whose `after()` never fired has a NULL start time and `null < cutoff` is NULL**, so the obvious comparison would miss forever exactly the case it exists for (caught while writing it, and now a test). Doctor-facing: a self-refreshing "Recordings being written up" card with **Try again** (reuses the stored audio — no re-dictation) and Discard; `discardDraft` now reaches `failed` but deliberately NOT `transcribing`, so the job can't write a note onto a soft-deleted visit. nginx no longer needs the 300s timeout for this route; `client_max_body_size 25m` still does. `scripts/test-scribe-async.ts` (suite 18, 28 checks) covers the state machine, idempotent claim, both recovery cases, retry-only-from-failed, and author-only access; e2e updated from the old "400 when unconfigured" contract to 202 + the run settling to `failed` with the recording kept (**91/91**, was 87). **NOT proven end to end:** no Whisper/Claude keys exist, so no real transcription has run through this path — one live dictation is owed when they land. tsc clean; lint at baseline; unit 18/18; build clean. |
| 2026-08-22 | **D-01: six more app files off direct DB access (42 → 36), plus 19 duplicate clinic reads removed.** The big cluster was `select … from clinics where id = X limit 1` written inline in **27 of the 42** files — a violation conventions.md §6 already names, because `getClinic` is request-cached and every one of those was a duplicate round trip per render. Codemodded the clinic-side callers only: the super-admin ones (`admin/actions.ts`, the logo route) additionally filter `notDeleted(clinics.deletedAt)`, a real guard against acting on a trashed clinic that `getClinic` does not apply. **The first codemod pass was wrong and got reverted:** the regex matched queries that merely JOIN `clinics` while selecting from another table, renamed their variable and broke eight files — tsc caught it. The fix was to constrain the match by what the projection SELECTS (every column reference must be `clinics.…`), which then skipped 8 join-shaped queries correctly; that rule is now recorded on the delta so the next person doesn't repeat it. Six files fully retired: `admin/team/page` (new `core/admin/team.ts#listCompanyTeam` — the owner-visibility rule travels WITH the query, since fetching the row and hiding it afterwards is a UI filter, not a boundary), `clinic/settings/page` + `clinic/trash/page` (reuse `getMyProfile` / `listClinicActorOptions` from the last batch), `clinic/settings/actions` (new `core/clinics/settings.ts`, kept separate from `core/admin/*` — same table, different authority), `clinic/whatsapp/page`, and `clinic/procedures/procedures-panel` (its query was byte-identical to the export route's, so both now share `listProcedureCatalog`). Re-proved the ratchet fires on a pruned file. Lint came out at 40 problems, one BELOW the 41 baseline, so the baseline was ratcheted down to 40 — a floor that is not lowered when it drops is just a number. tsc clean; unit 18/18; e2e 91/91; build clean. |
| 2026-08-22 | **D-01: three more app files retired (36 → 33), by collapsing two list/export duplications.** Both the patients list page and `GET /api/patients/export` built the SAME search inline — name / phone / MRN-digits, byte for byte — and the export's own comment admitted it "mirrors the list search". Same story for staff: the page and `GET /api/staff/export` each carried the role filter and the name search. That is the duplication worth removing, not the line count: **an export that quietly returns a different set from the list on screen is a bug nothing visible would report.** New `core/patients/list.ts` (`patientSearchWhere` + `listClinicPatients` + `listPatientsForExport`) and `core/users/staff-list.ts` (renamed from `staff-export.ts`, since it is no longer export-specific) — one WHERE, one column set, two callers each. The export variants are deliberately UNBOUNDED, unlike D-12's reports: a CSV that stopped at a page would be wrong in a way the file itself could not show. Retired: `api/patients/export`, `clinic/patients/patients-list`, `clinic/staff/page`. tsc clean; lint back at the 40 baseline; unit 18/18; e2e 91/91 (the patients + staff list pages and both CSV routes are all covered); build clean. |
| 2026-08-22 | **D-01: the whole scribe domain moved into core (33 → 30).** Took a DOMAIN rather than scattered files this time — `api/ai/scribe/route.ts`, `clinic/scribe/actions.ts` and `clinic/scribe/scribe-panel.tsx`, 14 statements between them. New `core/clinical/scribe.ts` owns the tenant scoping, the soft-delete filters and the author rule for all of it: patient picker + search, recent visits, own drafts, the appointment-ownership check, the tenant check the upload route makes, the scribe-run insert, and the load/approve/discard row operations (all three still sharing `draftAccessCondition`, so D-16's bug can't come back). **Where the seam had to fall, and why:** approving a note resolves the enabled MODULE's contract — `clinicalSchemasFor` to validate the note's shape, `clinicalRecordFor` to persist the specialty chart — and **core may never import `/modules` or the registry** (ADR-001, §3). So the extraction is the QUERY, not the operation: core takes an already-validated note and does the UPDATE; the action keeps permissions, module resolution, recall capture and `revalidatePath`. Same shape as Trash taking module rows as data. Two small wins fell out: the prescription send was joining `clinics` a third time purely to label a WhatsApp message (now the request-cached `getClinic`), and `searchPatients` had a dead dynamic `import("@/core/db/schema")` whose bindings were never used. tsc clean; lint at baseline; unit 18/18; e2e 91/91 (cross-tenant scribe → 404 exercises the new tenant check; prescription tenant isolation still holds); build clean. |
| 2026-08-22 | **D-01: the appointments read-side (30 → 27).** The cluster here was the clinic's DOCTOR LIST, written inline three times — appointment detail, the booking panel, the doctors/leave panel — each with a slightly different projection. **`core/appointments/doctors.ts#listClinicDoctors` already existed**, but returned a view model built for the availability calendar (name pre-formatted, no fee, no daily cap, alphabetical). Rather than add a fourth near-duplicate it was EXTENDED: the raw name parts, `consultationFee` and `dailyLimit` join the shape, plus two options — `doctorId` (a doctor viewing their own schedule is scoped to themselves) and `order`. **`order` exists purely to preserve behaviour:** all three call sites listed doctors newest-first, and quietly re-sorting a list alphabetically during a refactor is still a behaviour change. Also extracted: `listUpcomingLeaves` (`end_date >= today`, NOT `start_date` — leave that began last week and runs through next week is still current, and dropping it would show a doctor as available while they are away), `getAppointmentDetail`, and the booking picker's `listRecentPatients` / `getPatientForPicker`. Retired: `appointments/appointment-detail`, `appointments/new-appointment-panel`, `doctors/doctors-panel`. tsc clean; lint at baseline; unit 18/18; e2e 91/91 (appointment list + detail + new, the doctors panel with limit and leave controls, and the WhatsApp reschedule/booking paths all still pass); build clean. |
| 2026-08-22 | **D-01: the printed documents (27 → 22).** Five pages that put money or clinical data on paper — visit invoice, payment receipt, patient account statement, clinical chart print, doctor share statement — each read the clinic row inline AND its own entity. All five clinic reads became the request-cached `getClinic` (they aliased `whatsappSignature` as `signature`, which tsc caught). Then the real duplication: **the invoice and receipt queries differed by exactly three columns** while joining the same two tables, so they now share `core/billing/invoice.ts#getAppointmentForDocument` — two printed money documents disagreeing about a visit is the worst possible place for a drifted query. The statement and chart print shared their patient identity block (`getPatientHeader`), and the share statement's doctor lookup became `listClinicDoctors({ doctorId })`, which also means the statement header and every doctor picker now spell the name the same way. **Verified by RENDERING, not just typechecking:** a throwaway clinic + completed appointment + minted session, then all three money/clinical print pages fetched over HTTP — 200 with the patient's name on the page. e2e does not cover these routes, so a typecheck alone would not have caught a bad row shape. **A prune bug worth recording:** three entries would not delete from the allowlist because `sed`'s pattern treats `[id]` as a character class — the same bracket trap that already bit the ESLint glob (ADR-014). Pruned literally instead, and the ratchet was re-proved to fire on a BRACKETED path specifically. tsc clean; lint at baseline; unit 18/18; e2e 91/91; build clean. |
| 2026-08-22 | **D-01: the clinic dashboard and self-service account (22 → 20).** Two files that were mostly query. **`clinic/page.tsx`** held five `count()` queries and TWO hand-written SQL statements — including the correlated EXISTS that defines **"Revenue Recovered", the metric the whole product is sold on**, living in a React component where it could not be read, reused or tested without rendering a page. Now `core/clinics/dashboard.ts`: `getClinicDashboardCounts` + `getRecoveredCount` + `getRecoveredTrend`, with the `COALESCE(sent_at, due_at)` subtlety documented (a recall marked sent without a timestamp would otherwise never match and quietly under-count the headline number). **`account/actions.ts`** moved its five self-service writes into `core/users/profile.ts` beside the reads. Two deliberate shapes there: `setMyPasswordHash` takes the HASH, never a plaintext password, because a query-layer function accepting one is an invitation to store one; and `setMyAvatarKey` RETURNS the previous key rather than deleting the old file itself, so storage stays out of the query layer and an ordering mistake leaves a user with no picture rather than a stale one. Also gave `listUpcomingLeaves` a `doctorId` option, since the dashboard shows a doctor only their own leave. **Verified by the metric itself:** e2e asserts "Revenue Recovered = Rs 4,000 (1 return visit × 4000)" against seeded data, so the extracted SQL provably returns the same number as the inline version. tsc clean; lint at baseline; unit 18/18; e2e 91/91; build clean. |
| 2026-08-22 | **D-01: the admin team + 2FA write paths (20 → 18).** First batch of pure WRITE actions rather than reads. `core/admin/team.ts` gained the lifecycle — create, suspend, deactivate, reactivate, edit, password reset, capabilities, reassign, soft-delete — and `core/users/profile.ts` the 2FA trio. **Every team write is narrowed to `role = 'super_admin'` as well as the id**, which is not belt-and-braces: these take a userId from a form, and without the role clause a mistyped or tampered id could reach a CLINIC's staff account and reset its password from the company panel. The actor-dependent checks (owner guard, no-escalation, "you can't reduce your own access") stay in the action, because they need the ACTOR and that is auth's business, not the query layer's. Two shapes worth noting: the **suspend vs deactivate distinction is a transaction boundary** — both revoke sessions, only deactivate unassigns clinics, and a deactivated account left as account manager on live clinics means nobody is watching them while they appear covered; and `softDeleteTeamMember` mints its own `delete_group`, since tying a parent and the rows its deletion hid into one restorable batch is the function's business, not the caller's. **Verified by running them:** a throwaway member + assigned clinic + minted sessions, then all six lifecycle transitions asserted — suspend keeps clinics, deactivate unassigns them, delete does both plus the soft-delete. e2e does not cover these write paths, so a typecheck alone would have proved nothing. tsc clean; lint at baseline; unit 18/18; e2e 91/91; build clean. |
| 2026-08-22 | **D-01: `clinic/actions.ts` — the biggest single file yet (18 → 17, 13 statements).** Three new core modules by AUTHORITY, not by table: `core/users/clinic-staff.ts` (a clinic admin editing their employees) now sits beside `core/users/profile.ts` (a person editing themselves) and `core/admin/team.ts` (the company editing its super admins) — same `users` table, three different rights, so no function can be handed an id it has no business touching. Plus `core/patients/manage.ts` for the patient writes. **Every clinic-staff write narrows to `STAFF_ROLES`**, which is what stops one clinic admin editing or deleting another; that rule was previously a local `const` in a 700-line action file. Four actions had also repeated the same "find an editable staff member" lookup verbatim — one function now, and it returns the name too, since every caller wrote it into an audit line and a second query for one string on a row already in hand is just a round trip. **The patient soft-delete cascade is the sharp part** and is documented where it lives: the cascade touches only rows still LIVE (an independently-trashed child keeps its own delete group, so restoring the patient can't revive something binned on purpose), and the realised-revenue rows are HARD-deleted because they are derived state (ADR-016) re-snapshotted on restore — keeping trashed copies would just be a second source of truth. **e2e turned out NOT to cover this**: its trash test mimics the delete with raw SQL rather than calling the action, so the cascade had never been exercised. Verified directly instead — 10 checks over MRN allocation, tenant-scoped update, group inheritance, the independently-trashed child, cascade-hidden visits/recalls, removed sale rows, and an idempotent second delete. tsc clean; lint at baseline; unit 18/18; e2e 91/91; build clean. |
| 2026-08-22 | **D-01: `appointments/actions.ts` (17 → 16, 15 statements).** Two new core modules, split by what actually shapes them. **`core/appointments/manage.ts`** — the QUEUE is the design constraint: an appointment carries a per-doctor, per-window token, so an edit that moves it must surrender the old token and take a new one, and taking one races other bookings. `withQueueNumber` retries on a unique violation, which means the write has to be re-runnable — hence `insertAppointment` / `updateAppointmentFields` are their own functions rather than branches buried in the action. **`core/appointments/leave.ts`** — setting leave is not just a row: it CANCELS the doctor's bookings in the range, in the SAME transaction, because a clinic that records leave and leaves the bookings standing has told the patients nothing and the doctor the wrong thing. Notifying those patients deliberately stays OUTSIDE the transaction (ADR-016: a WhatsApp provider must not be able to roll back a leave entry), so the functions return the cancelled ids and the caller notifies after. Also: the booking picker's search moved to `core/patients/list.ts` beside the list's, and `softDeleteAppointment` deliberately does NOT void the sale — that is `core/sales/ledger`'s entry point, and folding it in would make a soft delete depend on the sales domain. **Verified by running the cascade** (e2e only loads these pages, it never books): 10 checks — bookings inside the range cancelled, the INCLUSIVE last day included, outside untouched, a COMPLETED visit not rewritten, moving the range cancelling what the new range clashes with, and trashing the leave NOT reinstating bookings patients were already told were off. tsc clean; lint at baseline; unit 18/18; e2e 91/91; build clean. |
