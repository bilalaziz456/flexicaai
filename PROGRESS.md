# PROGRESS.md — Klenic Implementation Tracker

> This file tracks what has been implemented and what remains. It is updated after
> every meaningful change. The build order follows CLAUDE.md §11. Do not jump ahead.
>
> **Legend:** ✅ done · 🔨 in progress · ⬜ not started · ⚠️ blocked/needs attention

_Last updated: 2026-07-06_

---

## Product decisions

- **No public signup — accounts are invite/admin-provisioned only** (decided 2026-07-06,
  supersedes an earlier trial-signup idea). There is no `/signup` page.
  - **Super Admin** (seeded directly) creates **clinics + each clinic's Clinic Admin**,
    and selects that clinic's specialties → `clinics.modules_enabled` (Step 5).
  - **Clinic Admin** creates their own staff: doctors, receptionists (Step 6).
  - **No account = no login.** Everyone provisioned this way uses the single `/login`.
  - The **specialty checkboxes** (Dental now; Derma/Hair "coming soon") therefore live on
    the Super Admin "create clinic" screen (Step 5), rendered from the module registry so
    new modules appear with zero UI changes.
- **Login = single shared page** for all roles; the proxy routes each role to its panel.
- **Stack change: Supabase → local PostgreSQL + Drizzle + custom auth** (decided 2026-07-06,
  supersedes CLAUDE.md's original Supabase stack; §2/§5/§10 updated). Owner wants data on
  own Postgres + fastest local queries. Implications:
  - DB access is 100% server-side (no browser DB client). Multi-tenancy enforced by
    filtering every query by `clinic_id` in the query layer (Postgres RLS later, optional).
  - Auth is home-grown: `users` (bcrypt) + `sessions` (opaque cookie token, SHA-256 in DB).
  - **Drizzle vs raw SQL policy:** default Drizzle; raw SQL via `db.execute(sql\`…\`)` on the
    same pool for heavy analytics / hand-tuned queries. Full policy in `src/core/db/index.ts`.
  - File storage → local filesystem for now (audio/PDF/photos), S3-compatible later.

---

## Environment

- Node: v24.18.0 ✅
- npm: 11.16.0 ✅
- git: **2.55.0** ✅ (repo initialized; commit identity set locally as "Bilal Aziz" &lt;bilalaziz456@gmail.com&gt;)
- Next.js: **16.2.10** (spec said "14+"; scaffold gave latest — App Router, satisfies "14+")
- React: 19.2.4 · Tailwind: **v4** · TypeScript: strict ✅
- **PostgreSQL 17** ✅ (local service `postgresql-x64-17` running) · **Drizzle ORM** + `pg`
  · **bcryptjs** · drizzle-kit / tsx / dotenv (dev)
- Note: an `AGENTS.md` (added by create-next-app) warns Next 16 has API changes vs
  older docs — check `node_modules/next/dist/docs/` when unsure. Imported from CLAUDE.md.

---

## Build order (CLAUDE.md §11)

### 1. Project setup ✅  _(reworked to local Postgres 2026-07-06)_
Next.js 16 (App Router) · TypeScript strict · Tailwind v4 · shadcn/ui · **Drizzle + local Postgres**
- [x] Next.js app scaffolded (App Router, TS strict)
- [x] Tailwind configured (v4)
- [x] shadcn/ui initialized — output realigned to `/core/ui` + `/core/lib` per §3
- [x] Folder structure created per CLAUDE.md §3 (`/core`, `/modules`, `/config`)
- [x] DB layer wired: `core/db/index.ts` (single `pg` pool + Drizzle + raw escape hatch), `core/db/schema.ts`
- [x] Zod-validated env (`core/lib/env.ts`): server-only `DATABASE_URL` (no public vars now)
- [x] `.env.example` + `.env.local` updated to `DATABASE_URL` + seed vars
- [x] `drizzle.config.ts` + npm scripts (`db:generate/migrate/push/studio/seed`)
- [x] Typecheck clean + production build passes
- **Left for you:** set your Postgres password in `.env.local` `DATABASE_URL`, create the `klenic`
  database, then `npm run db:migrate` (see "How to run the DB" below).

### 2. Auth ✅  _(reworked to custom session auth 2026-07-06)_
- [x] Username/password login (`(auth)/login` + `signIn`/`signOut` in `core/auth/actions.ts`); no public signup
- [x] Roles: super_admin, clinic_admin, doctor, receptionist (`core/types/auth.ts` + `user_role` pg enum)
- [x] Custom sessions: `core/auth/session.ts` (opaque cookie token, SHA-256 hash in `sessions`), `password.ts` (bcrypt)
- [x] Server guards `requireUser` / `requireRole` (`core/auth/user.ts`) — the REAL auth gate; `signOut` + button
- [x] Edge-safe proxy (`src/proxy.ts`): coarse cookie-presence check only (no DB in Edge); role enforced in pages
- [x] Placeholder panel pages at `/admin`, `/clinic`, `/doctor`, `/reception` (role-gated)
- [x] Seed script `scripts/seed.ts` (`npm run db:seed`) creates the first super_admin
- [x] Typecheck + build green
- **Left for you:** after migrating, `npm run db:seed` to create your super admin, then log in.

### 3. Core DB schema ✅
- [x] `clinics` (with `modules_enabled` text[])
- [x] `users` (role, clinic_id, bcrypt hash, is_active) + `sessions`
- [x] `patients` (clinic-scoped; phone/WhatsApp, DOB, data-consent flag)
- [x] `appointments` (`module` text tag, status enum, scheduled_at, doctor)
- [x] `visits` (`module` tag, `status` draft/approved enum, transcript, JSONB note, approved_by)
- [x] `recalls` (`module` tag, due_at, status enum, source_visit link) — recall engine reads these
- [x] `module` kept as free-text tag (NOT enum) so new specialties need no schema change
- [x] Indexes on every `clinic_id` + common lookups (name/phone/date/status)
- [x] Tenant-scoping helper `byClinic()` (`core/db/tenant.ts`) — the multi-tenancy boundary
- [x] Migrations `0000` + `0001` applied; production build green
- **Note:** specialty clinical data (e.g. dental tooth-chart rows) will live in module tables
  linked to `visits`, never as columns on these core tables (CLAUDE.md §5).

### 4. Module registry + dental skeleton ✅
- [x] `ModuleDefinition` contract + specialty-catalog types (`core/types/module.ts`)
- [x] Dental module: `config.ts`, `prompts/scribe.ts`, `recall-rules.ts`, `drug-formulary.ts` (Pakistan brands)
- [x] `/config/modules.ts` registry: `MODULES`, helpers (`getModule`, `loadModules`, `getClinicWorkspace`)
- [x] `SPECIALTY_CATALOG` for Step 5 checkboxes: Dental=available, Derma/Hair=coming_soon (architected, not built)
- [x] Verified golden rule: `/core` imports from neither `/modules` nor `/config` (grep-clean)
- [x] Typecheck + build green
- **Adding a specialty later:** implement `/modules/<id>/config.ts`, register in `/config/modules.ts`,
  flip catalog entry to "available" — zero `/core` changes.

### 5. Super Admin panel (`/admin`) ✅ (billing deferred)
- [x] `/admin` layout guards super_admin; header + sign out
- [x] Clinics list (`/admin`) with specialty badges
- [x] Create clinic + **specialty checkboxes** (from `SPECIALTY_CATALOG`) + its Clinic Admin,
      in one transaction (`/admin/clinics/new`) — no service-role client needed (local Postgres)
- [x] Clinic detail (`/admin/clinics/[id]`): toggle modules + staff list (uses `byClinic()`)
- [x] Reusable `SpecialtyCheckboxes` (dental selectable; derma/hair disabled "coming soon")
- [x] Verified: `/admin` + `/admin/clinics/new` render 200 with a real super_admin session
- [x] **Force password change on first login** — `must_change_password` col (migration 0002),
      `/change-password` flow, enforced in `requireRole`; created clinic admins start with the flag.
      Verified: flag on → `/admin` 307 → `/change-password` 200.
- [x] **Edit a staff member's name + username** (unique-checked) from the clinic detail page
- [x] **Reset a staff password** — issues a temp password + sets the flag + revokes their sessions
- [x] **Rename clinic**; **suspend/reactivate** account (`is_active`, revokes sessions on suspend);
      **delete clinic** (danger zone, confirm by typing the name; cascades all its data)
- [ ] View billing / usage (deferred — not MVP-critical)
- [x] **Search** clinics by name on `/admin` (URL-driven `?q=`, debounced, case-insensitive)
- **Still deferred (revisit later):** billing & usage · audit log of admin actions (§10) ·
  pagination on clinics · clinic-level suspend. Not premature to skip now.

### 6. Clinic Admin panel (`/clinic`) ✅
- [x] Layout guards `clinic_admin` (via `requireClinicAdmin`, guarantees non-null clinicId); shows clinic name; nav
- [x] Dashboard: staff + patient counts (clinic-scoped, index-backed COUNTs)
- [x] Staff: add doctor/receptionist (temp password → must-change), list, suspend/reactivate, reset password
- [x] Patients: add (name, phone/WhatsApp, DOB, gender, address, consent), list, **search by name/phone**
- [x] All writes/reads clinic-scoped via `byClinic()`; staff mgmt also filters `clinic_id` so cross-clinic ids match 0 rows
- [x] Trigram (pg_trgm GIN) indexes on `patients.full_name` + `patients.phone` for fast ILIKE (migration 0006)
- [x] Verified: /clinic, /clinic/staff, /clinic/patients render 200 as clinic001; role isolation (clinic_admin → /admin 307)
- **Note:** clinic admin can only create `doctor`/`receptionist` — never admins.

### 7. Doctor panel — voice scribe (`/doctor`) ✅ _(needs API keys to run live)_
- [x] Voice recorder (client, browser MediaRecorder) — `scribe-workspace.tsx`
- [x] Audio → local filesystem storage (`core/integrations/storage`, clinic-namespaced; swap to S3 later)
- [x] `/api/ai/scribe`: Whisper (OpenAI, separate) → Claude (`claude-sonnet-4-6`) → structured note, using the CLINIC'S MODULE prompt (generic engine in `core/ai/scribe-engine` + `prompt-runner`; JSON via prompt + safe parse — Sonnet 4.6 has no `output_config.format`)
- [x] Draft → review/edit (generic `NoteEditor`, specialty-agnostic) → approve → save to `visits`
- [x] Flywheel: `visits.ai_draft` + `audio_key` (migration 0007) freeze the original AI output for edit-diffing
- [x] Drug validation against the module formulary (warns, doesn't block)

### 8. Prescription generator 🟨 _(PDF done; WhatsApp delivery deferred to Step 9)_
- [x] Dental drug formulary validation (drugs checked against the module formulary; scribe already warns at draft time)
- [x] PDF generation — generic CORE `core/lib/prescription-pdf.ts` (pdf-lib, built-in Helvetica; specialty-agnostic, renders {drug,dosage,duration}); `GET /api/prescriptions/[visitId]` serves it from the APPROVED visit note, clinic-scoped; doctor "Prescription" link on approved visits
- [ ] WhatsApp delivery → Step 9

> Lib note: used **pdf-lib** (not react-pdf/pdfkit from CLAUDE.md §2). Reason: both
> pdfkit and @react-pdf/renderer have font-file/asset bundling issues under Turbopack;
> pdf-lib embeds standard fonts with zero config and renders reliably in a Next route.

### 9. WhatsApp integration 🟨 _(infra done; needs AiSensy account + approved template to run live)_
- [x] CORE AiSensy client `core/integrations/whatsapp` (v2 Campaign/template API; specialty-agnostic; config-gated)
- [x] `whatsapp_messages` log table + enums (migration 0008) — records every send + inbound; source for the Step 11 queue
- [x] Notification channel `core/notifications/whatsapp` — records FIRST (queued), then sends, then updates status (nothing lost if unconfigured)
- [x] Inbound webhook `POST /api/whatsapp/webhook` (shared-token secured; logs incoming messages, best-effort patient match by phone; advances outbound status on delivery/read receipts)
- [x] Prescription delivery: signed public link (`core/lib/signed-link`, HMAC+expiry) → `GET /p/rx/[token]` serves the PDF with no session; doctor "WhatsApp" button (`sendPrescriptionToWhatsApp`) delivers it
- [x] Verified: webhook 401/200 + inbound row logged + status receipt advances; public link 200 PDF, tampered token 404. Typecheck green.
- **Config to go live:** `AISENSY_API_KEY` + an approved `AISENSY_RX_CAMPAIGN` template (with a document header), `WHATSAPP_WEBHOOK_TOKEN`, `LINK_SIGNING_SECRET`, `APP_URL`. Map the template body params in `sendPrescriptionToWhatsApp`.
- Deferred: per-clinic WhatsApp numbers (currently one platform-level AiSensy account); text/session messages (template-only for now).

### 10. Recall engine ✅ _(reminder send needs WhatsApp live; booking = Step 11)_
- [x] CORE engine `core/recall` — `scheduleRecall` (capture) + `processDueRecalls` (send due reminders via the WhatsApp channel), specialty-agnostic
- [x] Capture: approving a visit schedules a recall from the note's `nextVisit {reason, afterDays}` (`approveVisit`)
- [x] Cron endpoint `GET /api/cron/recalls` (CRON_SECRET-secured; Bearer or ?token) + `vercel.json` daily schedule (09:00)
- [x] Success → recall `sent`+sentAt; provider failure leaves it `pending` to retry (attempt still logged in `whatsapp_messages`); no phone → skipped
- [x] Clinic `Recalls` page + nav item (read-only list, responsive table/cards)
- [x] Verified: cron 401 without secret; authorized run picks only DUE recalls (`{processed:1,sent:0,skipped:1}`), leaves pending + logs a failed WhatsApp row when unconfigured; recalls page renders. Typecheck green.
- **To go live:** WhatsApp configured (Step 9) + an approved `AISENSY_RECALL_CAMPAIGN` template; set `CRON_SECRET` on Vercel.
- Booking (recall `sent` → `booked`) lands with the reception panel / inbound WhatsApp reply — Step 11.

### 11. Receptionist panel (`/reception`) 🟨 _(appointments + WhatsApp queue done; payments deferred)_
- [x] Appointments: list (`/reception`, newest first, patient+doctor, status badges), schedule (`/reception/new` — patient picker + doctor + datetime + reason), status actions (confirm/complete/cancel/no-show, icon-only on mobile); mobile `+` FAB; all clinic-scoped
- [x] WhatsApp queue (`/reception/whatsapp`): inbound + outbound messages for the clinic, newest first, patient-matched (reads the Step 9 `whatsapp_messages` log)
- [x] Reception nav = Appointments + WhatsApp (shared PanelShell); responsive table→cards throughout
- [x] Actions `createAppointment` / `setAppointmentStatus` / `searchClinicPatients` (tenant-scoped)
- [x] Verified with a seeded receptionist: all three pages 200, appointment + inbound message render, Confirm action + nav present. Typecheck green.
- [ ] Payments (JazzCash/Easypaisa/Raast/Stripe) — DEFERRED (needs provider accounts; like billing). Record-a-payment can be a light follow-up.
- Booking a recall → appointment from an inbound reply: natural next connection (recall `sent` → `booked` + create appointment).

### 12. Owner dashboard ✅ — MVP finale 🎉
- [x] **"Revenue Recovered" hero metric** on the clinic-admin dashboard (`/clinic`): return
      visits driven by recall reminders × the clinic's average visit value (PKR).
- [x] "Recovered" = a recall in status `sent`/`booked`/`completed` whose patient then had a
      `completed` appointment on/after the reminder — correlated `EXISTS` (raw SQL on the same
      pool per the db/index.ts analytics policy), all clinic-scoped.
- [x] Owner setting `clinics.avg_visit_value` (migration 0009, default 3000) editable inline via
      `AvgVisitValueForm` → `updateClinicSettings` (validated, clinic-scoped).
- [x] Supporting stat cards: Return visits · Recalls sent · Upcoming appts · Patients · Staff
      (each index-backed COUNT; the last three deep-link to their pages).
- [x] Verified with a seeded clinic_admin + a recall→completed-appointment scenario: dashboard
      200, hero renders **Rs 5,000** (1 return visit × 5,000 avg), settings form present; test data
      cleaned up. Typecheck green.

---

## 🎉 MVP complete (CLAUDE.md §11 steps 1–12)

All twelve build-order steps are done. Per CLAUDE.md §11, **stop here** — do NOT build derma,
hair, mobile apps, or advanced analytics until instructed. Config left to go live (owner's task):
`ANTHROPIC_API_KEY` + `OPENAI_API_KEY` (scribe), an AiSensy account + approved
`AISENSY_RX_CAMPAIGN`/`AISENSY_RECALL_CAMPAIGN` templates + `WHATSAPP_WEBHOOK_TOKEN` +
`LINK_SIGNING_SECRET` (WhatsApp/recalls), `CRON_SECRET` (Vercel cron), and the deploy-time
scaling fixes in "Deployment & scaling". Deferred within-MVP items: billing/usage (Steps 5–6),
payments (Step 11), audit log.

---

## Deployment & scaling (DEFERRED — handle at server deploy, not now)

Current settings are deliberate MVP/local defaults. Under load (~1000 concurrent
requests) the failure points are known and must be addressed **before production**:

- **DB connection pool** — `core/db/index.ts` uses a single pool `max: 10`. On
  Vercel serverless each instance opens its own pool → concurrent instances can
  exhaust Postgres `max_connections` (~100) → "too many connections" errors.
  **Fix:** put a pooler in front (PgBouncer / Neon / Supabase pooler / Vercel
  Postgres) and tune `max` to `pooler_limit ÷ expected_instances`.
- **Login CPU / event-loop block** — `auth/password.ts` uses **bcryptjs (pure JS,
  cost 12)**, chosen for no native build on Windows. Pure-JS hashing runs *on*
  the Node event loop (~250ms each), so a burst of logins serializes and stalls
  all other requests. **Fix (prod):** native `bcrypt`/`argon2` (thread pool) or a
  worker thread, and **rate-limit `/login`** per IP.
- **No rate limiting** anywhere — add per-IP throttling (login + general) so a
  burst/abuse can't take the app down.
- **Session validation hits the DB every request** (`sessions ⋈ users`). Fine for
  now; consider a short-TTL cache if it becomes hot.
- **Verify with a real load test** against a **production build** (`npm run build
  && npm start`), not `next dev` — e.g. `autocannon` or `k6`. Dev numbers are
  meaningless for capacity.

> Reads (indexed, tenant-scoped) hold up for bursts; the real risks are login
> CPU and serverless connection exhaustion. Owner's call: tackle at deploy time.

---

## How to run the DB (local Postgres)

1. In `.env.local`, set `DATABASE_URL` — replace `YOUR_PASSWORD` with your Postgres password.
2. Create the database (once):
   `psql -U postgres -c "CREATE DATABASE klenic;"`
3. Apply migrations: `npm run db:migrate`
4. Seed the first super admin: `npm run db:seed` (set `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` first).
5. `npm run dev`, open http://localhost:3000, log in with the seeded admin → lands on `/admin`.

Other DB commands: `npm run db:generate` (new migration after schema change) ·
`npm run db:push` (dev-only quick sync) · `npm run db:studio` (browse data in a GUI).

> **This machine:** local Postgres runs on **port 5433** (not the default 5432); `.env.local`
> reflects that. Migration `0000` applied and a super admin is seeded — login works.

---

## Change log

| Date | Change |
|------|--------|
| 2026-07-05 | Created PROGRESS.md tracker. Confirmed empty project; at Step 1. |
| 2026-07-05 | Scaffolded Next.js 16 (TS strict, Tailwind v4, App Router, `src/`, `@/*` alias). |
| 2026-07-05 | Restored CLAUDE.md — create-next-app had clobbered it to a single `@AGENTS.md` line; full guardrails recovered, import kept. |
| 2026-07-05 | Built §3 folder tree (`/core`, `/modules/dental`, `/config`) with `.gitkeep`s. |
| 2026-07-05 | Installed `@supabase/supabase-js`, `@supabase/ssr`, `zod`. Wired browser+server Supabase clients and validated env. |
| 2026-07-05 | Initialized shadcn/ui; relocated `button.tsx`→`core/ui`, `utils.ts`→`core/lib` to honor §3. |
| 2026-07-05 | **Step 1 complete** — typecheck + `next build` green. Next: Step 2 (Auth). |
| 2026-07-05 | Git 2.55 installed; repo initialized (`main`). Added `.gitattributes` (LF normalization) and `.idea` ignore. |
| 2026-07-05 | Pushed to GitHub: https://github.com/bilalaziz456/klenic (origin/main). |
| 2026-07-06 | **Step 2 (Auth) complete** — roles, login/signup, session proxy, route protection, role guards, placeholder panels. Module-agnostic (authorizes by role, never specialty). Typecheck + build green. |
| 2026-07-06 | Next 16 learning: `middleware.ts` convention deprecated → renamed to `src/proxy.ts` (`export function proxy`), per bundled docs. |
| 2026-07-06 | Fixed Grammarly hydration warning (`suppressHydrationWarning`); set Klenic metadata; `/` now redirects to `/login`. |
| 2026-07-06 | **Decision revised:** removed public signup entirely — accounts are admin-provisioned only (Super Admin → clinics + clinic admins; clinic admin → staff). Deleted `/signup` page/form/action + login link; specialty checkboxes move to Step 5. Build green. |
| 2026-07-06 | **Major rework: Supabase → local PostgreSQL + Drizzle + custom session auth.** Removed `@supabase/*`; added drizzle-orm/pg/bcryptjs/drizzle-kit/tsx/dotenv. New DB layer (`core/db/index.ts` + `schema.ts`), custom auth (`session.ts`/`password.ts`/`constants.ts`), Edge-safe proxy, seed script. Generated migration `0000_*`. Updated CLAUDE.md §2/§5/§8/§10/§11. Typecheck + build green. |
| 2026-07-06 | **DB live end-to-end.** Postgres on port 5433; created `klenic` DB, applied migration `0000`, seeded super admin. Verified: `/login` 200, `/admin` 307→login, form renders. Login works in browser. |
| 2026-07-06 | **Step 3 (Core DB schema) complete.** Added `patients`, `appointments`, `visits`, `recalls` (module-tagged, status enums, JSONB note, indexed by clinic_id). Added `byClinic()` tenant helper. Migration `0001` applied → 7 tables. Typecheck + build green. |
| 2026-07-06 | **Step 4 (Module registry) complete.** `ModuleDefinition` contract + dental module (scribe prompt, recall rules, PK drug formulary) + `/config/modules.ts` registry with specialty catalog (dental available; derma/hair coming_soon). Golden rule verified: /core imports no module. Build green. |
| 2026-07-06 | **Step 5 (Super Admin panel) complete.** `/admin` clinics list, create-clinic-with-admin (transaction) + specialty checkboxes from registry, clinic detail with module toggles + staff. Added shadcn checkbox/table/badge. Verified authed pages render 200 (Dental selectable, derma/hair coming soon). Build green. Billing deferred. |
| 2026-07-06 | **Super Admin management tier complete.** `must_change_password` (migration 0002) + forced `/change-password` flow; reset staff password (revokes sessions); rename clinic; suspend/reactivate accounts; delete clinic (confirm-by-name, cascades). Verified force-change redirect (flag on → /admin 307 → /change-password 200). Build green. Auth bits reused by Step 6. |
| 2026-07-06 | **Login switched from email → username.** Added `users.username` (unique, migration 0003, backfilled admin@klenic.local → `admin`); email now optional contact. Updated login/create-clinic forms, signIn, seed (`SEED_ADMIN_USERNAME`), displays. Verified: login form shows Username, credential check passes (admin/change-me-now). Login now: **admin** / change-me-now. |
| 2026-07-06 | **UI polish.** Reusable `PasswordInput` with show/hide eye (login, change-password, create-clinic). Font → Plus Jakarta Sans (the `--font-sans` var was previously unwired → browser default). Teal brand palette in light+dark (primary/ring/accent/charts). Admin top bar: removed duplicate "Admin" on the left (now "Klenic" wordmark), username pill on right. Colored auth backgrounds. Verified via curl. |
| 2026-07-06 | **Edit clinic staff.** Super admin can edit a staff member's full name + username (unique-checked, `updateStaffProfile`) from the clinic detail page, alongside the existing rename-clinic. Verified staff row + Edit control render (200). Build green. |
| 2026-07-06 | **Per-account dark/light/system theme.** `users.theme` enum (migration 0004, default system) + mirrored `klenic_theme` cookie; no-flash inline script applies `.dark` before paint and follows OS for "system". `ThemeToggle` (System/Light/Dark) in headers persists via `setThemePreference` action + cookie; login seeds the cookie from the account's saved theme. Verified: dark→html.dark, light→none, script present. Root layout now dynamic (reads cookie). |
| 2026-07-06 | **UI/UX fixes.** Keyed inputs to kill Base UI defaultValue warning (staff/rename edits); Chrome autofill kept on-theme in dark (unlayered box-shadow); unified solid input surface (`--input-bg`) so inputs look identical everywhere; clearer dark borders; create-clinic now redirects to the list. |
| 2026-07-06 | **Clinics search + perf rule.** Name search on `/admin` (URL `?q=`, debounced, case-insensitive). Saved standing memory: build every data op for fastest response. Added pg_trgm GIN index on `clinics.name` (migration 0005). |
| 2026-07-06 | **Step 6 (Clinic Admin panel) complete.** `/clinic` dashboard (counts) + staff (add doctor/receptionist, suspend/reset) + patients (add, search by name/phone). All clinic-scoped via `byClinic()`. Trigram indexes on patients name/phone (migration 0006). Verified 200s + role isolation. |
| 2026-07-07 | **UI: brand logo + panel chrome.** Vector-traced `logo2.png` → theme-aware SVGs (`logo.svg`/`logo-dark.svg`) with refined Plus Jakarta Sans tagline; icon-only transparent favicon set (`app/icon.svg`/`favicon.ico`/`apple-icon.png`). Locked exact logo colors into theme tokens (`--brand-teal #0FB4BB`, `--brand-blue #069FC5`, `--brand-navy #082957`; primary/ring/charts + `bg-brand-gradient`). Responsive shells: desktop sidebar + mobile hamburger drawer (animated slide/fade) for `/clinic` + `/admin`, icon+text nav, sign-out icon-only on mobile. Added `loading.tsx` boundaries (spinner) so nav doesn't linger on the old page. Mobile FAB for "New clinic". |
| 2026-07-07 | **Scaling review (deferred to deploy).** Analyzed ~1000-request behavior; documented deploy-time fixes in "Deployment & scaling" section: pool `max:10` → needs a connection pooler on serverless; bcryptjs (pure-JS) login blocks the event loop → native bcrypt/argon2 + login rate-limit; no rate limiting yet; load-test on a prod build. Owner: handle at server deploy. |
| 2026-07-07 | **Step 7 (voice scribe) complete.** Generic CORE scribe: `core/integrations/storage` (local fs, clinic-namespaced), `core/ai/prompt-runner` (Anthropic SDK, `claude-sonnet-4-6`, safe JSON parse — Sonnet 4.6 lacks `output_config.format`), `core/ai/scribe-engine` (Whisper transcribe + note-gen). `POST /api/ai/scribe` (doctor-guarded, tenant-scoped) uses the clinic's MODULE prompt → saves a DRAFT visit. Doctor UI: `/doctor` shell + `ScribeWorkspace` (MediaRecorder → review → approve) + generic `NoteEditor`. Migration 0007: `visits.ai_draft`+`audio_key` (flywheel). Drug-formulary warnings. Verified end-to-end (auth→patient→module→storage→AI-key gate) via a minted doctor session; typecheck green. **Needs `ANTHROPIC_API_KEY` + `OPENAI_API_KEY` in `.env.local` to run live.** Added `@anthropic-ai/sdk`. |
| 2026-07-07 | **Nav unified + delete/suspend UX + Step 8 (prescription PDF).** All four panels use one shared `PanelShell` (sidebar/hamburger); doctor+reception got the shell; deleted per-panel shells. Clinic staff/patients pages match the admin flow (list-first + search + `/new` page + mobile `+` FAB; table→cards on mobile with icon actions); same responsive cards+icons on the super-admin clinic-detail staff table. Delete flows: reusable `ConfirmDeleteDialog` (portal modal, step-up password re-auth via `core/auth/reauth`, autofill-proof text field with eye toggle) on staff+clinic deletes. Suspend/reactivate a clinic_admin cascades to all clinic staff (bidirectional, sessions revoked on suspend). Login now shows a distinct "suspended" message after the password verifies. Fixed duplicate-username crash (`isUniqueViolation` walks the drizzle `.cause` chain). **Step 8:** `core/lib/prescription-pdf.ts` (pdf-lib) + `GET /api/prescriptions/[visitId]` → PDF from the approved visit note, clinic-scoped, formulary-validated; doctor "Prescription" link. Verified: PDF text decoded + checked (patient/doctor/diagnosis/Rx/advice/footer). Added `pdf-lib`. Typecheck green. |
| 2026-07-07 | **Step 9 (WhatsApp) — infra.** CORE AiSensy client (`core/integrations/whatsapp`, template Campaign API, config-gated), `whatsapp_messages` log (migration 0008), notification channel (`core/notifications/whatsapp`, record-then-send), inbound webhook (`/api/whatsapp/webhook`, token-secured, logs inbound + advances outbound status on receipts, best-effort patient match). Prescription delivery: HMAC signed public link (`core/lib/signed-link`) → `/p/rx/[token]` serves the PDF session-free; doctor "WhatsApp" button. Env: APP_URL, AISENSY_*, WHATSAPP_WEBHOOK_TOKEN, LINK_SIGNING_SECRET (dev values added to .env.local). Verified end-to-end (webhook 401/200 + logged, status receipt → delivered, public link 200 PDF, tampered → 404). Needs an AiSensy account + approved template to send live. Typecheck green. |
| 2026-07-07 | **Step 10 (recall engine).** CORE `core/recall`: `scheduleRecall` (capture) + `processDueRecalls` (sends due reminders via the WhatsApp channel, specialty-agnostic). Capture wired into `approveVisit` from the note's `nextVisit`. Cron `GET /api/cron/recalls` (CRON_SECRET, Bearer/?token) + `vercel.json` daily 09:00. Success → recall `sent`; failure → stays `pending` (retry) with the attempt logged; no phone → skipped. Clinic `Recalls` page + nav (responsive table/cards). Verified: cron 401 unauthorized, authorized run `{processed:1,sent:0,skipped:1}` on a seeded due recall, pending retained + failed WhatsApp row logged when unconfigured; recalls page 200. Env: AISENSY_RECALL_CAMPAIGN, CRON_SECRET. Typecheck green. |
| 2026-07-07 | **Step 11 (receptionist panel) — appointments + WhatsApp queue.** `/reception` appointments list (patient+doctor, status badges, confirm/complete/cancel/no-show icon actions, responsive table→cards, mobile + FAB); `/reception/new` schedule form (patient picker + doctor select + datetime + reason); `/reception/whatsapp` queue (inbound+outbound from the Step 9 log, patient-matched). Reception nav = Appointments + WhatsApp. Actions: createAppointment / setAppointmentStatus / searchClinicPatients (tenant-scoped). Verified with a seeded receptionist: all pages 200, appointment + inbound message render, actions + nav present. Payments DEFERRED (provider accounts). Typecheck green. |
| 2026-07-09 | **Scheduling form adapts to the doctor's hours.** The new-appointment form (shared by reception + clinic admin) now splits Date and Time: for a doctor with set hours (not flexible), the Time field is a **dropdown of valid slots** within that doctor's working window for the chosen date (30-min steps, past slots on today filtered out); for a flexible doctor or "Any doctor", it's a **free time picker** (hour/min/AM-PM). Off-day/leave shows "Doctor doesn't work that day". `doctorDayAvailability` now returns `flexible` + the day's `window`; Submit is disabled until a valid slot is chosen. Extracted `TimeSelect` to `core/ui/time-select.tsx` (shared by the schedule editor + booking form). Suite 47/47; both modes verified visually at mobile width. |
| 2026-07-10 | **Specific-hours doctor → time is radio buttons of the visiting windows** (not a slot dropdown). When creating an appointment, a non-flexible doctor shows one radio per working window for the day ("9:00 AM – 12:00 PM", "4:00 PM – 7:00 PM"); the appointment books at the chosen window's start. A single window shows one (pre-selected) radio. The free hour/min/AM-PM dropdown now shows ONLY for flexible / "Any" doctors. Past windows are dropped for today. Suite 47/47. |
| 2026-07-10 | **Appointment status is a dropdown (any status → supports undo).** Replaced the per-status action buttons with a single status `<select>` (Scheduled / Confirmed / Completed / Cancelled / No-show) on the lists + detail page — so a status can be changed to anything, including **undo** (e.g. Confirmed → Scheduled). `setAppointmentStatus(id, status)` is now a direct action (no form), applied via `useTransition`; it no-ops when the status is unchanged, and patient notices (cancel / whatsapp-confirm) fire only on a real transition (no duplicates). E2E updated to assert the dropdown + full status set: 50/50. |
| 2026-07-10 | **Appointments open to a detail page (edit + delete).** Each appointment row (clinic admin + reception) now has **Open** → a detail page (`/clinic/appointments/[id]` and `/reception/appointments/[id]`, shared `AppointmentDetail` body). There the clinic admin/receptionist can change the **doctor / date / time / duration / reason** (`updateAppointment`, enforced by `checkDoctorSlot` — out-of-hours rejected), run the **status actions** (confirm/complete/cancel/no-show), and **delete** the appointment (`deleteAppointment`, step-up password). Time uses the free picker; the server enforces the doctor's hours. Clinic-scoped (no cross-tenant leak). E2E: both lists open, detail renders for both roles, edit persists, tenant isolation — 50/50. |
| 2026-07-10 | **Patients list opens to a full detail page (edit + delete).** Each patient row now has **Open** → `/clinic/patients/[id]`, matching the staff/clinic "open to manage" pattern. The detail page lets the clinic admin **edit** all fields (`updatePatient`) and **delete** the patient (`deletePatient`, step-up password; cascades appointments/visits/recalls), and shows the patient's recent appointments (read-only). Clinic-scoped via `byClinic` — a foreign patient's data isn't shown (verified: no cross-tenant leak). E2E: list→Open, detail renders, edit persists, tenant isolation — 49/49. |
| 2026-07-10 | **Reverted the date dropdown → native date input; allow past dates.** The appointment Date field is the native `<input type="date">` again (no upcoming-dates dropdown, no min), so staff can record appointments on **previous dates** (e.g. logging a walk-in that already happened). Backend already allowed it (`createAppointment` only rejects invalid dates). Removed the today-only window filtering. Verified 4/4 (past-date appointment created + stored; native date input). |
| 2026-07-10 | **Multiple time windows per day (split shifts).** A weekday can now have several working windows (e.g. Mon 09:00–12:00 AND 16:00–19:00). Core `isDoctorAvailableAt` checks ALL windows for the weekday; `windowsForWeekday` + `describeAvailability` handle multiples; removed the "one window per weekday" validation. The schedule editor lets you **add/remove multiple time ranges per day** ("+ Add another time"). The appointment slot list generates slots across all windows (and skips the gap); booking is blocked in the gap. `doctorDayAvailability` now returns `windows[]`. Verified 9/9 (two windows enforced incl. the gap; edit removes a window; slots span both). **Fix:** invalid ranges (end ≤ start) are now validated **client-side** with an inline error + disabled submit, so a bad submit no longer round-trips the server and wipes the form (the React-19 form-reset that made it look like saved data was lost). |
| 2026-07-09 | **Scheduling form: strict specific/flexible split.** The doctor's `flexible_hours` is now passed with the doctor list, so the form knows up front (before a date is picked) which picker to show: a **flexible/"Any" doctor gets the free time picker**; a **specific-hours doctor NEVER shows the free picker** — only the visiting-hours slot list (with "Pick a date to see available times" before a date is chosen). Fixes the free picker briefly showing for a specific doctor pre-date. |
| 2026-07-09 | **Per-doctor "Flexible hours" toggle** (`users.flexible_hours`, migration 0016). When ON, the doctor can be booked at ANY time — working hours are not enforced (leave and the daily cap still apply); ON is how "no restriction" is expressed now. When OFF, appointments (staff, WhatsApp self-book, reschedule) may only be made during the doctor's visiting hours, and a doctor with no hours set + not flexible is not bookable ("no visiting hours set"). Checkbox in the doctor schedule editor (`DoctorScheduleFields`); enforced in the shared `checkDoctorSlot`; reflected in the "slots left" hint. Verified 8/8 (flexible books any time incl. off-days/3am; strict blocks off-day/after-hours/no-hours; leave still overrides flexible; createStaff persists the flag). Suite 47/47. |
| 2026-07-09 | **Reschedule wording/status fix (light).** A WhatsApp reschedule now (a) **preserves the appointment's status** — a `confirmed` appointment stays confirmed after a move (it's not a new request needing re-approval), instead of being downgraded to `scheduled`; and (b) sends an accurate **"Your appointment has been rescheduled to \<time\> with Dr X. Fee: Rs …"** message via the generic reschedule reply, instead of the previous "Appointment confirmed …" (which was misleading). `checkDoctorSlot` now also returns the fee. Decided NOT to make reschedule pending-until-confirm (unlike new bookings) — a move to an already-validated slot shouldn't add staff re-approval. Verified 6/6 (confirmed stays confirmed, scheduled stays scheduled, correct message, no booked-notice). Suite 47/47. |
| 2026-07-09 | **WhatsApp booking is pending until staff confirm.** A WhatsApp self-booking is now a *request*: the appointment is tagged `appointments.source='whatsapp'` (migration 0015) and the patient gets a "booking request received — the clinic will confirm shortly" ack (not a premature "confirmed"). When the receptionist/clinic admin **Confirms** it, `setAppointmentStatus` sends the patient the confirmation with slot/timing/doctor/fee (`notifyAppointmentBooked`). Staff-created bookings (`source='staff'`) still confirm immediately at booking and are NOT re-notified on confirm (gated by source + prior status). Verified 7/7: whatsapp booking → pending ack + no confirmation; staff confirm → confirmation with Dr + fee; staff booking → immediate + no double. Suite 47/47. |
| 2026-07-09 | **Patient self-booking via WhatsApp** (respects doctor visiting hours). A matched patient can text e.g. "book with Dr Khan monday 3pm"; the webhook runs `handleBookingReply` (`core/appointments/booking.ts`) after the reschedule check: resolves the doctor (named / the clinic's only doctor / else asks which, listing each doctor's hours), parses date+time, validates against the doctor's hours/leave/daily cap (`checkDoctorSlot`), creates the appointment (status `scheduled`), and confirms — or replies with the hours / the reason. Requires an explicit date AND time; "cancel" messages are excluded. New env `AISENSY_BOOKING_REPLY_CAMPAIGN`. Verified end-to-end (webhook): valid slot creates the appt, off-day refused with hours, no-date → guidance-with-hours, two-doctor selection by name, cancel-not-booked — 6/6; suite 47/47. |
| 2026-07-09 | **Reschedule: weekday parsing.** `parse-when.ts` now resolves weekday names (full + abbreviations, optional "next"/"this") to the next upcoming occurrence — e.g. "reschedule next monday 3pm". Word-boundary matching so "next month" is NOT read as Monday. Verified 9/9 (all weekdays, no-time-keeps-existing, natural phrasing, negative case). |
| 2026-07-09 | **Reschedule: broadened intent.** `isRescheduleIntent` now also fires on "postpone" and move/change/shift/rebook when the message names an appointment/booking/slot/visit (and NOT on unrelated "I'll move to Lahore"). Verified across 15 date-format + phrasing cases. |
| 2026-07-09 | **UI: consistent time picker.** Replaced the native `<input type="time">` (inconsistent clock icon / clipped AM-PM across devices) with hour/minute/AM-PM `<select>` dropdowns in the doctor schedule editor; stored value stays "HH:MM" 24h. |
| 2026-07-08 | **UI polish: mobile schedule editor + dashboard links.** Doctor working-days rows stack cleanly on mobile; avg-visit-value Save button aligned + status centred; keyed the input to kill the Base UI defaultValue warning; dashboard "Upcoming appts" → `/clinic/appointments`, "Recalls sent" → `/clinic/recalls`. |
| 2026-07-09 | **Patient self-service reschedule via WhatsApp reply.** A matched patient can reply e.g. "reschedule 12 Jul 3pm"; the inbound webhook runs `handleRescheduleReply` (core/appointments): finds their next upcoming appointment, parses the date/time (`parse-when.ts`, heuristic — ISO / DD-MM / "12 Jul" / today|tomorrow + am-pm|24h), validates the new slot against the doctor's leave/hours/daily cap, moves the appointment (resets the reminder), and confirms with full details — or replies with clear guidance / the reason it couldn't. Extracted the booking slot-validation into shared `core/appointments/availability.ts` (`checkDoctorSlot`) so booking AND reschedule enforce identical rules (createAppointment now uses it too). New env `AISENSY_RESCHEDULE_CAMPAIGN`. Verified end-to-end (webhook): valid slot moves the appt + confirmation, off-day slot refused with reason, no-date → guidance — 8/8; suite 46/46. |
| 2026-07-09 | **Day-before appointment reminder (WhatsApp, daily cron).** `sendDueAppointmentReminders(now)` in `core/notifications/appointment.ts` scans active appointments scheduled TOMORROW that aren't reminded yet (patient has phone) and sends a WhatsApp reminder with doctor + time. Idempotent via new `appointments.reminder_sent_at` (migration 0014; stamped on success, left null to retry on failure). Cron `GET /api/cron/reminders` (CRON_SECRET, Bearer/?token — same shape as recalls) + `vercel.json` daily 18:00. New env `AISENSY_REMINDER_CAMPAIGN` (default `appointment_reminder`; params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} clinic). Verified end-to-end: only tomorrow's appt reminded ("Reminder: your appointment with Dr X is on Fri 10 Jul, 10:00."); today / day-after / already-reminded / phone-less all skipped — 7/7; suite 45/45. |
| 2026-07-09 | **WhatsApp booking confirmation to patients** (doctor + working hours + fee + time). `notifyAppointmentBooked(clinicId, apptId)` in `core/notifications/appointment.ts`, wired into `createAppointment` after insert. Best-effort/clinic-scoped/phone-only, same as the cancel notice. New env `AISENSY_BOOKING_CAMPAIGN` (default `appointment_booked`; params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} hours/{{5}} fee/{{6}} clinic). Body e.g. "Appointment confirmed with Dr Book on Mon 13 Jul, 10:00. Working hours: Mon 09:00–17:00…. Fee: Rs 2,000. — Clinic". Verified 6/6; suite 43/43. |
| 2026-07-09 | **WhatsApp notice to patients when an appointment is cancelled** (with doctor name + time). New CORE `core/notifications/appointment.ts` `notifyAppointmentsCancelled(clinicId, ids)` — best-effort, clinic-scoped, only messages patients with a phone, never throws (a notify failure can't undo the cancellation). Wired into BOTH cancellation paths: manual `setAppointmentStatus(...,'cancelled')` and bulk cancel from `addDoctorLeave`. Uses the standard WhatsApp channel (logs every attempt, no-ops gracefully when unconfigured); new env `AISENSY_CANCEL_CAMPAIGN` (default `appointment_cancelled`, params {{1}} patient/{{2}} doctor/{{3}} time/{{4}} clinic). Verified end-to-end: manual + leave cancels each create an outbound notice ("Your appointment with Dr X on Mon 13 Jul, 10:00 has been cancelled."), phone-less patients get none — 6/6; suite 43/43. |
| 2026-07-08 | **Doctor leave / vacation days.** New `doctor_leaves` table (clinic-scoped date range per doctor, migration 0013). Receptionist (`/reception/doctors`) and clinic admin (`/clinic/staff/[id]`) set leave via the shared `DoctorLeaves` component → `addDoctorLeave`/`removeDoctorLeave` (both roles). Adding a leave **cancels** the doctor's active (scheduled/confirmed) appointments in the range (reports the count); booking is **hard-blocked** on leave days (checked in `createAppointment` before hours/limit), and the schedule form shows "on leave that day". Verified end-to-end with real actions: add-leave cancels the appt, booking blocked on leave day / allowed on non-leave day, remove-leave unblocks — 9/9; permanent suite 43/43. |
| 2026-07-08 | **Per-doctor consultation fee + staff "Open" → full management page.** Added `users.consultation_fee` (int PKR, migration 0012), set/edited alongside the doctor schedule (`DoctorScheduleFields` fee input; parsed in `createStaff`/`updateDoctorSchedule`). Reworked the clinic staff list: each row now shows a single **Open** button (no inline action cluster) → `/clinic/staff/[id]`, which became a full management page for any staff — edit profile (name/username), doctors' schedule + daily cap + fee, reset password, suspend/reactivate, and delete (delete now redirects to the list). Removed the old inline `staff-actions.tsx`. Verified end-to-end with real server actions: create-with-fee, edit fee, edit profile, suspend, reset password (9/9); permanent suite 43/43. |
| 2026-07-08 | **Doctor scheduling: working hours + daily appointment cap, enforced on booking.** New core `core/lib/availability.ts` (per-weekday `DayAvailability`, `isDoctorAvailableAt`, `describeAvailability`, `dayBounds`; specialty-agnostic). Schema: `users.availability` jsonb + `users.daily_appointment_limit` int (migration 0011). Clinic admin sets custom per-day hours + daily cap when **creating** a doctor (`DoctorScheduleFields` in add-staff) and edits them at `/clinic/staff/[id]` (`updateDoctorSchedule`). Booking (`createAppointment`, shared by receptionist + clinic admin) **hard-blocks** off-day / out-of-hours / over-cap bookings (0 = unlimited); the schedule form shows live "N of M appointments left" via `doctorDayAvailability`. Receptionist adjusts caps at `/reception/doctors` (`setDoctorDailyLimit`, both roles). Verified end-to-end (real server actions): create-with-schedule, off-day/out-of-hours/over-limit blocks, limit→0 unblocks — 10/10; permanent suite render checks 43/43. |
| 2026-07-08 | **Clinic Admin can now MANAGE appointments (not just view).** Generalized the appointment actions (`createAppointment`/`setAppointmentStatus`/`searchClinicPatients`) to accept `receptionist` OR `clinic_admin` via a shared `requireAppointmentsAccess()` that routes revalidate/redirect back to the caller's own panel (clinic admin → `/clinic/appointments`, receptionist → `/reception`). `/clinic/appointments` upgraded to the full manager (all appts newest-first, confirm/complete/cancel/no-show, New button + mobile FAB) reusing the reception `AppointmentActions` + `NewAppointmentForm`; added `/clinic/appointments/new`. Tenant scoping unchanged (`byClinic`). E2E: manage controls render + schedule page + clinic-B isolation + reception regression, 39/39. |
| 2026-07-08 | **Clinic Admin: read-only upcoming-appointments view.** New `/clinic/appointments` (clinic-scoped via `byClinic()`, lists scheduled/confirmed appts from now on, soonest first, responsive table→cards, no scheduling actions — reception still owns those). Added "Appointments" to the clinic nav; the dashboard "Upcoming appts" card now links here (previously pointed at `/reception`, which the role guard bounced clinic admins away from → looked like a refresh). E2E: lists upcoming appt + tenant-scoped, 38/38. |
| 2026-07-08 | **Revenue dashboard = super-admin-gated, specialty-agnostic feature.** New core `clinics.features_enabled` text[] (migration 0010) + `core/lib/features.ts` (`CLINIC_FEATURES`, `clinicHasFeature`) — optional platform features, off by default, working across dental/derma/hair alike (core never hardcodes a specialty). Super admin toggles "Revenue dashboard" per clinic on the clinic detail page (`FeaturesForm` → `updateClinicFeatures`); the clinic dashboard shows the Revenue Recovered hero + avg-visit-value setting (and the "Return visits" stat) ONLY when enabled, and skips the recovered analytics query when off (perf-first). E2E extended to cover feature ON (shown) + OFF (hidden) + the admin toggle: 36/36. |
| 2026-07-08 | **E2E smoke test harness** (`scripts/e2e.mjs`, `npm run test:e2e`). Seeds a throwaway two-clinic world + real SHA-256 sessions, exercises every panel + API route over HTTP (auth, role isolation, tenant scoping, Revenue Recovered, prescription PDF + signed link, WhatsApp webhook, recall cron, scribe unconfigured/tenant, bcrypt round-trip), then cleans up (incl. orphaned audio). Full pass **35/35**. Note: the recurring Turbopack "Jest worker" dev 500 on cold route compile is environmental — clear `.next` + restart, not a code bug. |
| 2026-07-07 | **Step 12 (owner dashboard) — MVP finale.** `/clinic` dashboard rebuilt around a "Revenue Recovered" hero: return visits from recall reminders × `clinics.avg_visit_value` (migration 0009, default 3000, editable via `AvgVisitValueForm` → `updateClinicSettings`). "Recovered" = a `sent`/`booked`/`completed` recall whose patient later had a `completed` appointment on/after the reminder (correlated `EXISTS`, raw SQL on the same pool, clinic-scoped). Supporting stat cards (Return visits, Recalls sent, Upcoming appts, Patients, Staff). Verified with a seeded clinic_admin + recall→completed-appt scenario: dashboard 200, hero = Rs 5,000 (1×5000), settings form present; data cleaned up. Typecheck green. **All 12 MVP steps complete — stop per CLAUDE.md §11.** |
| 2026-07-10 | **Appointment edit form now uses the create form's adaptive visiting-hours UI.** Generalized `NewAppointmentForm` to serve both create and edit (props `appointmentId`/`fixedPatient`/`initial`); in edit mode it prefills fields, fixes the patient, and saves via `updateAppointment`. Opening an appointment (`/clinic|/reception/appointments/[id]`) now shows the same doctor-aware time picker as creation — radio buttons of the doctor's visiting-hours window(s) for a specific-hours doctor, a free time picker only for flexible/"Any" doctors — with the doctor prefilled and hours enforced on save. `edit-appointment-form.tsx` trimmed to just the delete button. Verified end-to-end (real server actions): create still books, edit page renders the prefilled radio form, edit saves time/reason/duration, out-of-hours edit rejected — 4/4; permanent suite 50/50. |
| 2026-07-10 | **Fix: appointment edit radio/fields reverting after Save.** Selecting a second visiting-hours window (e.g. 4–7 PM), saving, then seeing the radio snap back to the first window (9–12) was React 19's automatic post-action `<form>` reset: native `form.reset()` unchecks the controlled radios and clears inputs, and React skips re-writing the DOM (props unchanged from the prior render), desyncing the selection. `NewAppointmentForm` now remounts its field group (`key={fieldsKey}`, bumped once each submit settles) so the DOM is rebuilt from live state, and duration/reason are controlled (were `defaultValue`, so they were silently reverting too). Typecheck green; suite 50/50. |
| 2026-07-10 | **Fix: brief flash of the wrong window on Save (supersedes the remount hack).** The prior remount corrected the desync a frame *after* React 19's post-action form reset, so the first window flashed selected before snapping to the chosen one. Root cause removed instead: the visiting-hours selector is now **buttons** (`role="radio"`, React-state only) rather than `<input type="radio">`, so native `form.reset()` has nothing to reset and the selection can't flash — the value is still carried by the hidden `scheduledAt`. Dropped the `fieldsKey` remount; duration/reason stay controlled (immune to the reset). Typecheck green; suite 50/50. |
| 2026-07-10 | **Theme-aware native controls (date picker etc.).** The appointment date field (native `<input type="date">`) — and every other native control — ignored the app theme: in dark mode the calendar icon was dark-on-dark and the picker popup rendered light. Set `color-scheme` on `:root` (light) / `.dark` (dark) so native date pickers, `<select>` popups, spinners and scrollbars follow the theme, and `accent-color: var(--primary)` so the picker's selected-day highlight uses the brand teal. Class-based (`.dark` on `<html>`), so the "system" preference is covered too. Global, so DOB / doctor-leave date fields and all selects match as well. Suite 50/50. |
| 2026-07-10 | **Themed date picker for appointment create/edit.** The native `<input type="date">` popup is drawn by the OS/browser and can't be themed (background/font/accent), so it clashed with the app UI. Added `core/ui/date-picker.tsx` — a calendar popover built on Base UI `Popover` + a hand-rolled month grid, styled entirely with app tokens (`bg-popover`, `border-border`, `bg-primary` selected day, `ring-primary/40` today, muted adjacent-month days), with prev/next month, Today and Clear. Controlled, value stays `YYYY-MM-DD`, past dates still allowed. Swapped into `NewAppointmentForm` (create + edit). Typecheck green; suite 50/50. (DOB / doctor-leave date fields still use native inputs — can adopt this component later for full consistency.) |
| 2026-07-13 | **ACL enforcement — nav gating + appointment permissions (Phase 2 start).** Permissions now actually *take effect*. **Nav gating**: `PanelShell` nav items carry a `resource` tag; the reception + doctor layouts pass `accessibleResourceIds(user)` so a user only sees nav for resources they can access (super/clinic admin unaffected — short-circuit all). **Manager panel**: all reception pages open to managers (`requireRole(["receptionist","manager"])`). **Appointments enforcement** (the worked example): `createAppointment`/`updateAppointment`/`setAppointmentStatus`/`deleteAppointment` check `can(user,"appointments",<action>)` server-side; the reception list hides "New appointment" without create, `/reception/new` redirects viewers, and the appointment-detail Status/Edit/Danger-zone cards are hidden without the matching permission. Verified with a full-access vs view-only manager (New button, nav items, detail cards all gated; server actions blocked) + suite 50/50; tsc clean. Note: discovered this modified Next does a **soft redirect** (in-component `redirect()` → 200 with the target's content, not 307) — recorded in memory. Remaining enforcement (doctor clinical/prescriptions; patients/whatsapp/recalls/procedures/leave) is the next slice. |
| 2026-07-13 | **Access-control lists — foundation + manager role + permissions grid (Phase 1–2 of the ACL build).** Two-tier model: super-admin→clinic (existing `modules/features/log_access`) + **clinic-admin→staff per-user permissions**. New `core/auth/permissions.ts` (PURE/isomorphic): a `resource:action` catalog (View/Create/Edit/Delete; each resource declares which apply — reports are view-only, WhatsApp is view+send, etc.), `ROLE_DEFAULTS` for every role, and `can()`/`canAccess()`/`resourcesForClinic()`. New **`manager`** role (migration 0026 adds the enum value + `users.permissions text[]`) — an operations role that runs the reception panel (`ROLE_HOME_ROUTE`/`ROUTE_ROLE_ACCESS`/reception layout now accept it) with broad defaults (appointments/patients/recalls VCED, procedures VCE, sales/clinical/prescriptions/staff view, settings view). Clinic admins can now create + manage managers (shared `CLINIC_STAFF_ROLES`, staff list/detail/actions extended). New **permissions grid** on the staff detail page (`permissions-grid.tsx`) — a V/C/E/D matrix with greyed-out N/A cells and a View-is-prerequisite rule (button-checkboxes + hidden inputs, reset-proof), saved via `updateStaffPermissions` (sanitised, clinic-scoped, audit-logged) with a "reset to role defaults" action. The same matrix (`permission-matrix.tsx`, shared/controlled) also appears on the **create-staff form** — pick a role and the grid prefills that role's defaults (resets on role change), adjustable before creating; `createStaff` saves the chosen slugs. `users.permissions` NULL = follow role defaults; a stored array overrides. Verified 12/12 (defaults render, override replaces them, manager reaches /reception, blocked from /admin) + 19/19 catalog unit tests + suite 50/50; tsc clean. **Enforcement (making `can()` gate pages/actions/nav) is the next phase** — permissions are stored + editable but not yet enforced beyond role guards. |
| 2026-07-13 | **Consistent list "Open" button (admin clinics list).** The super-admin clinics list (`/admin`) used a plain underlined "Open" text link, unlike every clinic/reception list which uses the outline button + `ChevronRight` ("Open ›"). Standardised it to the same `buttonVariants({variant:"outline",size:"sm"})` + chevron, so the open affordance matches across the whole app. Verified render (admin list 200, no plain-underline Open left); suite 50/50; tsc clean. |
| 2026-07-13 | **Dropdown arrows: consistent gap from the right edge (app-wide).** The select chevrons sat too close to the right border. Base UI Select triggers (all filter bars — period/doctor/status/employee/action/clinic, page-size, appointment status) now use `pl-2.5 pr-3.5` so the chevron insets from the edge. Native `<select>`s (doctor + discount-type on the booking form, patient gender, staff role, patient-admin) get a new **`.select-chevron`** utility (globals.css) — `appearance-none` + a themed double-chevron SVG (matching lucide ChevronsUpDown, light/dark variants) positioned `right 0.85rem center` with `pr-8` text spacing — replacing the OS arrow so their gap is controlled and consistent too. Compact specialised selects (time picker h/m/AM-PM, date-picker month/year) left as-is to avoid distortion. Verified classes render on the real filter + form pages; suite 50/50; tsc clean. |
| 2026-07-13 | **Per-procedure discounts (layered with the appointment discount).** Each procedure line can now carry its OWN discount (Rs or %), applied to that line before the appointment-level discount. New `appointment_procedures.discount_type` / `discount_value` (migration 0025). Bill math is layered — `lineNet = gross − line discount`; `subtotal = fee + Σ lineNet`; `total = subtotal − appointment discount` — centralised in `core/appointments/fee.ts` (`computeProcedureLine` / `computeBill`, + `computeSaleAmounts` so the sales ledger snapshots the FULL discount = Σ line + appointment, keeping gross−discount=net). The per-row net is expressed once in SQL (`procedures.ts#procedureRowNetSql`, mirroring the JS) with correlated `appointmentProceduresNetSql`/`GrossSql` helpers, so the appointment lists, WhatsApp confirmation, reschedule quote, ledger and the report's per-procedure breakdown all stay a single fast aggregate (no N+1) and can't drift from the JS. Booking form: each chosen procedure row gets a Rs/% + value discount control next to the qty stepper, with the discounted line net (original struck). Detail bill card shows each line's discount + a subtotal → appointment-discount → total split. Encoded field is now `"<id>:<qty>:<type>:<value>"` (old 2-part still parses). Verified 14/14 (fee 2000 + X-Ray 1000×2 −10% + Cleaning 3000 −Rs500, appt −10% → net 5,670; list/bill/report + ledger snapshot 7,000/1,330/5,670 all agree) + suite 50/50; tsc clean. Completes the Sales feature's discount model. |
| 2026-07-13 | **Net-sales card on the clinic dashboard.** The clinic-admin dashboard (`/clinic`) now shows a **"Net sales (30 days)"** stat card — the net figure + completed-visit count, linking to the full `/clinic/sales` report — gated by the `sales` feature (hidden when off). Backed by a lightweight `getSalesSummary` (core/sales/report.ts): a single indexed `sum(net_amount)`+count over the (`clinic_id`,`occurred_at`) range, run in parallel with the other dashboard stats. Fixes "I don't see any sales summary on the dashboard" — Sales previously lived only on its own sidebar page. Verified 5/5 (card title, net total, completed count, link) + suite 50/50; tsc clean. |
| 2026-07-13 | **Activity-log Action filter.** Added an **Action** dropdown to both log filter bars (`core/ui/log-filters.tsx`) — filter entries by category (Logins / Views / Creates / Updates / Status changes / Deletes), alongside the existing date/employee(/clinic) filters. Options are **scoped to the viewer**: the clinic admin only sees the categories the super admin granted (`clinics.log_access`), and an out-of-scope `?action=` is ignored server-side so it can never widen access (falls back to all granted categories); the super admin gets all six. `parseLogFilters` now returns `action`; both pages apply `eq(activityLogs.action, …)` when in scope and pass the option list. Verified 5/5 (create/update narrowing, out-of-scope delete ignored for clinic, admin delete/login filtering) + suite 50/50; tsc clean. |
| 2026-07-13 | **Optional consultation fee (procedure-only visits).** New `appointments.charge_consultation` bool (migration 0024, default true). The booking form (create + edit) shows a **"Charge consultation fee (Rs X)"** checkbox whenever the chosen doctor has a fee — uncheck it for a patient who comes only for a procedure, and the consultation drops out of the live bill. Threaded through everywhere the fee is computed: both appointment lists, the WhatsApp booking confirmation, the WhatsApp reschedule quote, the appointment-detail bill card, and the **sales ledger** (`recordSaleForAppointment` + backfill), so a waived fee is reflected in revenue. The bill preview reads cleanly with no fee ("Rs X procedures = …" instead of "Rs 0 fee + …"). Also clarified for the owner: the Sales **graph needs completed appointments** — it's realised revenue, so it stays on the empty state until an appointment is marked Completed (Clinic001 currently has 7 scheduled, 0 completed). Verified 7/7 (procedure-only vs normal bill, checkbox prefill on/off) + suite 50/50; tsc clean. |
| 2026-07-12 | **Sales feature — deferred items completed (procedure quantity + bill card).** Closes the three items deferred during the phased build. (1) **Per-procedure quantity**: the booking form's procedure picker now shows a −/qty/+ stepper + line total for each chosen procedure; the form submits `procedure="<id>:<qty>"` hidden fields, parsed by `parseProcedureSelections` and saved via `saveAppointmentProcedures` (now takes `{procedureId, quantity}[]`, merges dupes, clamps 1–99, snapshots price×qty). Lists / WhatsApp / reschedule / ledger already summed `unit_price×quantity`, so they need no change. (2) **Read-only bill card** on the appointment detail page — consultation fee + each procedure line (`name × qty @ unit`) + subtotal + discount + **total** (via `computeAppointmentTotal`; new `getAppointmentProcedureItems` returns the snapshots). (3) **WhatsApp self-booking procedures**: staff attach them by opening the booking and using the same picker (no separate flow needed) — verified the edit path flows into the bill + sales. Verified 8/8 (bill line items, qty prefill, discounted total) + suite 50/50; tsc clean. Sales feature fully complete, no deferrals remaining. |
| 2026-07-12 | **Sales feature — phase 3: revenue ledger + report (feature complete).** New `sales` table (migration 0023; UNIQUE per appointment) — a realised-revenue ledger written by `core/sales/ledger.ts`: `recordSaleForAppointment` snapshots gross/discount/net (`computeAppointmentTotal` = fee + Σ procedures − discount) + doctor name + `occurred_at`, upserted when an appointment is marked **completed** (`setAppointmentStatus`) and re-snapshotted when a completed appointment is edited; `voidSaleForAppointment` deletes it if it leaves "completed"; `backfillClinicSales` (idempotent) runs when the super admin first enables the `sales` feature (`admin/actions.ts#updateClinic`), so the report shows history immediately. All best-effort — a ledger hiccup never blocks the status change. New **Sales** page (`/clinic/sales`, clinic-admin + `sales`-gated, nav via `PanelShell`): period presets (Today / 30d / quarter / 6mo / year) + custom range + per-doctor filter; summary cards (net, completed visits, discounts, avg/visit); a single-series **net-sales-over-time** bar chart (`sales-chart.tsx` — SVG, `--chart-1` teal, auto hour/day/week/month buckets, hover tooltips, theme-aware; built per the dataviz skill); and per-doctor + per-procedure breakdown tables. Query layer `core/sales/report.ts` (one range-scan powers summary/buckets/by-doctor in local time; a join gives the procedure mix). Verified end-to-end (11/11: gating 404, net/discount/count totals, doctor filter narrowing, empty period, tenant isolation, backfill detection) + suite 50/50; typecheck green. Sales feature (all 3 phases) done. |
| 2026-07-12 | **Sales feature — phase 2: procedures on appointments + totals.** `appointment_procedures` line-items table (migration 0022; snapshots name+price+qty). The booking form (`NewAppointmentForm`, create + edit) gains a **Procedures** multi-select (chips with prices) with a live bill preview; `createAppointment`/`updateAppointment` save/replace the line items (`saveAppointmentProcedures`, snapshotting prices). The appointment **total is now consultation fee + Σ procedures, with the discount applied to that total** — new `computeAppointmentTotal` (core/appointments/fee.ts), used by the form, both appointment lists (net via a `sum(unit_price×qty)` subquery), and the WhatsApp confirmation. Picker/prices only appear when the clinic has the `sales` feature (`getBookingProcedures`). Verified: fee 1,000 + Cleaning 3,000 + RCT 15,000 − 2,000 discount = net Rs 17,000 (gross 19,000 struck) on the list, edit prefills the two procedures, picker shows on new/edit; suite 50/50. Also fixed the WhatsApp **reschedule** reply to quote the full net total (fee + procedures − discount) instead of just the consultation fee. Deferred by agreement: WhatsApp self-booking doesn't attach procedures (staff attach on confirm), quantity fixed at 1. Next: phase 3 (sales ledger + report). |
| 2026-07-12 | **Sales feature — phase 1: procedures catalog.** New `sales` feature in `core/lib/features.ts` (super admin toggles it per clinic in Settings → Features). `procedures` table (migration 0021: clinic_id, name, price PKR, module, is_active). Full CRUD by **clinic admin AND receptionist** (`app/reception/procedure-actions.ts`, shared role guard + `sales`-feature gate, audit-logged) — add / edit (name/price/active) / delete (two-click confirm) — on a shared **Procedures** page at `/clinic/procedures` + `/reception/procedures` (`ProceduresManager` + `ProceduresPanel`). "Import suggested procedures" pulls the enabled specialty's defaults (`ModuleDefinition.procedureTemplates` → dental starter list; `config/modules.ts#procedureTemplatesFor`, deduped, re-runnable). Nav item gated in both panels via `PanelShell.salesEnabled`; direct URL 404s when the feature is off. Phase 1 of the 3-part plan (2: attach procedures to appointments + totals; 3: sales ledger + report). Verified feature on/off gating + render + dental import list; typecheck green; suite 50/50. |
| 2026-07-12 | **Pagination bar moved above the list.** The "Showing X–Y of Z" + page-size dropdown (+ Prev/Next) now render **before** the list on all nine pages (after the header/filters/search) instead of below it. Verified the bar precedes the first row; suite 50/50. |
| 2026-07-12 | **Appointment filter-bar alignment.** Applied the same fix as the log bar to `AppointmentFilters` (From/To/Status/Search/Today): identical `flex flex-col gap-1.5` field wrappers, uniform `text-xs` labels, **w-44** for From/To/Status (were w-40 dates + w-40 select, ragged), Search flex-1, and the Today button wrapped with an invisible `Label` so it bottom-aligns with the inputs. Renders 200; typecheck green. Added a "N / page" selector to the pagination bar — a themed Base UI Select (`core/ui/page-size-select.tsx`), URL param `size` (`PAGE_SIZE_OPTIONS=[20,50,100]`, `parsePageSize`), changing it resets to page 1 and preserves filters. The pagination bar now always renders when there's data (so the dropdown shows even on a single page of <20 rows); Prev/Next still appear only when there's more than one page. Threaded `pageSize` through all nine list pages (limit/offset + count). Verified: dropdown present with 5 rows, `?size=50` shows all 30 on one page ("Showing 1–30 of 30 · 50 / page"); suite 50/50. |
| 2026-07-12 | **Pagination on every list screen.** Added a shared `core/lib/pagination.ts` (`parsePage`/`pageOffset`/`DEFAULT_PAGE_SIZE=20`) + server component `core/ui/pagination.tsx` — "Showing X–Y of Z" + Prev/Next that preserve current filters (all query params except `page` and flash flags). Wired into all nine lists: admin clinics, clinic staff, clinic patients, clinic + reception appointments, clinic + admin activity logs, clinic recalls, reception WhatsApp. Each page now runs a `count()` alongside the `limit/offset` slice (count re-joins only the tables its filter needs) and shows the true total in the header. Verified: 25 patients → page 1 = 20, page 2 = 5, "Showing 1–20 of 23 · Page 1 of 2", Next link + filter (`q`) preserved; typecheck green; suite 50/50. |
| 2026-07-12 | **Clinic manage page merged to one Save.** The super-admin clinic detail page had four separate settings saves — Clinic name, Specialties, Features, Activity-log access. Merged into a single **Settings** card + one **Save changes** (`ClinicSettingsForm` with name input + specialty/feature/log-access checkbox groups, each emitting its own hidden inputs). One new action `updateClinic` saves name + `modulesEnabled` + `featuresEnabled` + `logAccess` together and redirects to `/admin?updated=1` ("Clinic updated." toast); removed `updateClinicName`/`updateClinicModules`/`updateClinicFeatures`/`updateClinicLogAccess` and the four form files. Staff list + Danger-zone delete stay separate. Verified: one Save, all groups present, no old buttons, Staff/Danger intact; e2e still sees the Features toggle + ON/OFF behavior; suite 50/50. |
| 2026-07-12 | **Staff edit merged to one Save.** The staff detail page had two record-edit buttons — "Save changes" (profile) and "Save schedule" (doctor hours/cap/fee). Merged into a single **Details** card + one **Save changes** that updates name, username AND (for doctors) the schedule together — mirroring the create form. `updateStaffProfile` now also parses/saves the doctor schedule in the same call (a bad schedule blocks the whole save, like create); removed `updateDoctorSchedule` + `edit-schedule-form.tsx`; `EditProfileForm` → `EditStaffForm` (renders `DoctorScheduleFields` for doctors, submit gated on schedule validity). The separate, distinct actions stay separate (Leave, Password reset, Suspend/Reactivate, Delete). Verified: doctor page shows one Save + inline schedule and no "Save schedule"; receptionist shows just name/username; other action cards intact; suite 50/50. |
| 2026-07-12 | **View logs de-duplicated (30-min window).** Opening/refreshing the same record repeatedly no longer spams the log: `core/audit/log.ts#logView` now skips the insert when the SAME user already has a `view` of the SAME record within `VIEW_DEDUPE_MINUTES` (30). The `logView` server action (ViewLogger) routes through it. Best-effort, non-transactional (a rare concurrent double is harmless). Verified the dedupe query: same user+record recent → skip; 35-min-old / different record / different user → log; suite 50/50. |
| 2026-07-12 | **Log filter bar alignment.** Reworked `core/ui/log-filters.tsx` so every control (From, To, Clinic, Employee, Today) is an identical `flex flex-col gap-1.5` field with a uniform `text-xs` label and the **same width (w-44)** — previously dates were w-40 and selects w-48 (ragged), and the Today button used a plain `<span>` spacer (16px) vs the `Label` component's leading-none (~12px), so heights didn't match. Today now uses a real invisible `Label` so its column height matches and the button bottom-aligns with the inputs. Pages render 200; suite 50/50. |
| 2026-07-12 | **Log employee filter lists the clinic's staff + Today-button alignment.** The employee dropdown was built from *distinct actor names already in the logs* (further constrained by granted categories), so it was empty when a clinic had no matching activity. It now lists the clinic's **actual staff from the `users` table** (roles in `CLINIC_LOG_ROLES`), so everyone appears even before they've logged anything; the filter matches on `actor_user_id` (robust to renames, shows full names). Applies to both `/clinic/logs` and the super-admin `/admin/logs` (once a clinic is picked). Also fixed the **Today** button alignment in the log filter bar (wrapped it with a spacer label so it bottom-aligns with the labelled date/clinic/employee controls). Verified: dropdown lists Dr One/Rec Two/Admin A, filtering by an employee narrows to their rows on both pages; suite 50/50. |
| 2026-07-12 | **Log leak fix: clinic admin no longer sees super-admin actions.** Super-admin actions on a clinic (settings/module/feature/rename/log-access changes) are tagged with that clinic's `clinic_id`, so they were showing in the clinic admin's log. The clinic log page now filters to `actor_role IN {clinic_admin,doctor,receptionist}` (`CLINIC_LOG_ROLES` in `core/audit/access.ts`) — the admin sees only their own + their staff's actions. The super-admin employee dropdown (once a clinic is picked) likewise lists that clinic's staff, not the super admin. Super-admin `/admin/logs` still shows everything (incl. super-admin actions). Verified: clinic admin sees admin+doctor rows but NOT the super-admin action / username; super admin sees all + staff-only employee list; suite 50/50. |
| 2026-07-12 | **Super-admin log filter: clinic → then employee.** On `/admin/logs` the Employee filter now depends on the Clinic filter: with no clinic selected it's disabled and shows "Select a clinic first" (and a stray `actor` param is ignored); once a clinic is chosen, the employee dropdown lists ONLY that clinic's staff and filters within it. Clinic page unchanged (employee always active for its own staff). Verified: no-clinic disables employee + lists none; clinic A lists only A's staff and rows; suite 50/50. |
| 2026-07-12 | **Activity log: permission-based access (replaces the 5-day auto-hide) + today-default + clinic/employee filters.** Dropped the time-based `visible` model — removed `activity_logs.visible`, `hideOldLogs`, `LOG_VISIBLE_DAYS`, the `/api/cron/log-visibility` cron and its `vercel.json` entry (migration 0020). Instead the **super admin grants each clinic a set of visible ACTION categories** (`clinics.log_access` text[] — logins/views/creates/updates/status/deletes; `core/audit/access.ts`), configured via a new **Activity-log access** card on the clinic detail page (`updateClinicLogAccess`). The clinic admin's `/clinic/logs` shows only the granted categories for their clinic, and **no log page/nav at all when `log_access` is empty** (PanelShell `logsEnabled` gate from the clinic layout). Both log pages now **default to today** and gained filters: date range + **employee**; the super-admin page also has a **clinic** filter (and its employee list scopes to the chosen clinic). Verified end-to-end: no-access message, granted-categories + today filtering, employee/clinic/date filters, super admin sees all incl. non-granted categories; typecheck green; suite 50/50. |
| 2026-07-11 | **Activity-log filters: date range + actor.** Both `/clinic/logs` and `/admin/logs` got a filter bar — From/To themed date pickers + an **Actor** dropdown (themed Base UI Select, populated from the distinct actors in scope) + Clear. URL params `from`/`to`/`actor`, parsed by `core/audit/log-filters.ts` (dates DON'T default — empty = no constraint, unlike the appointment list). Verified: actor filter and today-range each narrow correctly on both pages; suite 50/50. |
| 2026-07-11 | **Activity / audit log with 5-day clinic-admin visibility.** New `activity_logs` table (migration 0019) with a `visible` bool (default true). Logs **all clinic-staff actions** (create/update/delete of staff, patients, appointments, settings, leave, status), **logins**, and **record views** (patient/appointment/staff detail, via a client `ViewLogger` so link prefetch doesn't create phantom views). CORE `core/audit/log.ts` — best-effort `logActivity`(current user)/`logActivityAs`(explicit, for login), never throws/blocks; actor identity is snapshotted so rows survive user deletion. A daily cron `/api/cron/log-visibility` (`hideOldLogs`, CRON_SECRET, in `vercel.json` at 00:30) flips `visible=false` once a row is older than **5 days**. **Clinic admin** `/clinic/logs` sees only its own clinic's `visible=true` rows; **super admin** `/admin/logs` sees ALL rows across clinics incl. hidden ones (with Clinic + Visible columns). Nav items added to both panels; shared `core/ui/activity-log.tsx`. Super-admin actions log against the affected clinic (except clinic-delete → clinic_id NULL, since the FK cascades away). Verified end-to-end: cron 401-without-token / hides the 6-day-old row / keeps the recent one; clinic view hides the hidden row, super-admin view shows it; typecheck green; suite 50/50. |
| 2026-07-11 | **Search icon on the appointment patient picker.** The "Search patients by name or phone…" input in the create/edit appointment form now shows a leading `Search` (magnifier) icon, matching the list search inputs (patients/staff/clinics) — `relative` wrapper + absolutely-positioned icon + `pl-8`. Verified it renders on the new-appointment page; typecheck green. |
| 2026-07-11 | **Patients entered by age + whole-row click on every list.** (1) The patient DOB date-picker is replaced with an **Age** number field: `core/lib/age.ts` (`ageFromDob`/`dobFromAge`/`dobFromAgeField`) stores the derived birth date (still `patients.date_of_birth`, no schema change) so age never goes stale — recomputed from the stored date each view. Add/edit forms use the Age input; the patient list shows an **Age** column (and "N yrs" on mobile). (2) New `core/ui/row-link.tsx` (`RowLink`) makes a whole table row / mobile card navigate to its detail on click (hover highlight, keyboard Enter/Space, `role="link"`), ignoring clicks on genuinely interactive controls (links, buttons, the status `Select`, inputs) so those keep working. Applied to all entity lists: patients, staff, admin clinics, and both appointment lists (the "Open" button stays as an affordance). Verified: age 30 round-trips through create/edit/list, every list renders clickable rows; suite 50/50. |
| 2026-07-11 | **Same toast+redirect for patient & clinic create/edit/delete.** Extended the redirect-to-list + `FlashToast` + form error-toast pattern beyond staff/appointments: **patients** — `createPatient`→`/clinic/patients?created=1`, `updatePatient`→`?updated=1`, `deletePatient`→`?deleted=1` (list shows "Patient added/updated/deleted."), add + edit forms drop inline saved/error text for a red error toast; **super-admin clinics** — `createClinicWithAdmin`→`/admin?created=1`, `deleteClinic`→`/admin?deleted=1` ("Clinic created/deleted."), create-clinic form gets the error toast. Left in-place (correct UX — no list to bounce to): the clinic-detail inline settings (modules/features/name/avg-visit-value), admin staff-profile/reset-password dialogs, doctor daily-limit, and doctor leave — these keep their on-page feedback. Verified all pages render incl. flash params; suite 50/50. |
| 2026-07-11 | **Create/edit staff → success/error toast + back to the staff list.** `createStaff` now redirects to `/clinic/staff?created=1`; `updateStaffProfile` and `updateDoctorSchedule` now redirect to `/clinic/staff?updated=1` (were returning `{saved:true}` and staying on the detail page). The staff list renders a `FlashToast` ("Staff member added." / "Staff member updated."). Each form (add-staff, edit-profile, edit-schedule) drops its inline saved/error text and pops a red **error toast** (via the shared `Toast` + per-attempt nonce) when the action fails — matching the appointment create/edit UX. Reset-password / suspend / delete are unchanged (they stay on the detail page). Verified staff pages render incl. flash params; suite 50/50. |
| 2026-07-11 | **Click a queue card → filter the list to that queue.** The "Today's queue" cards on `/clinic/appointments` and `/reception` are now clickable: clicking one sets `?session=<queueSession>` and the list shows ONLY that queue (a doctor's single visiting window), ordered by token number; the selected card is highlighted and a banner ("Showing queue: Dr X · 9:00 AM–12:00 PM" + "Show all appointments") clears it. In session mode the date/status/search filter bar steps aside (the session already pins doctor+window+day). `QueueSummary` gained `pathname`/`activeSession` props (cards render as `<Link>` only when a pathname is passed, so the doctor panel's cards stay non-clickable). Verified: w0 card shows only its patients, w1 only its patient, banner + clear present; suite 50/50. |
| 2026-07-11 | **Themed DatePicker everywhere (DOB, doctor leave) + month/year quick-nav.** Replaced the remaining native `<input type="date">` fields — patient **date of birth** (add + edit) and **doctor leave** start/end — with the shared themed `core/ui/date-picker.tsx` (controlled + hidden input carrying the value for the server action; leave "Add" disabled until both dates set). Enhanced the DatePicker header with **Month + Year dropdowns** (years span today−100 … today+10, descending) so far-back dates like DOB are reachable in one click instead of hundreds of month-chevron presses — benefits appointment/filter pickers too. No native date inputs remain in `src/`. Verified all three pages render the themed trigger with no native date field; suite 50/50. |
| 2026-07-11 | **Appointment list: status filter.** Added a **status** dropdown to the filter bar (All / Scheduled / Confirmed / Completed / Cancelled / No-show) — a themed Base UI Select (theme accent on hover, matching the row status control), URL param `status`. Server-side `parseListFilters` validates it against the enum (invalid → ignored) and both lists add `eq(status, …)` to the query; the header label reflects the active status. Verified: each status narrows correctly, bogus values ignored; suite 50/50. |
| 2026-07-11 | **Appointment list: date-range filter (defaults to today) + search.** Both `/clinic/appointments` and `/reception` now default to showing **today's** appointments, with a filter bar: **From**/**To** date pickers (themed `DatePicker`) + a debounced **search** box (patient name/phone) + a **Today** reset. Filters are URL query params (`from`/`to`/`q`) read server-side — shared parser `core/appointments/list-filters.ts` (defaults both bounds to today, tolerates a reversed range, half-open `[start, endExclusive)`), shared client bar `reception/appointment-filters.tsx`. List now orders soonest-first within the range (limit 200); the header shows the active range + query; empty state is range-aware. The live "Today's queue" summary is unchanged (always today). Verified: default shows only today, ranges include the right days, `q` narrows within the range; e2e updated (its seed sits 3 days out, so that assertion now queries a wide range) — suite 50/50. |
| 2026-07-11 | **Patient queue tokens (per doctor visiting-window, FCFS).** Every appointment with a doctor now gets a **token number** on create / staff-edit / WhatsApp self-book / reschedule, scoped to a **(doctor, day, visiting-window) session** — so a doctor's 9–12 window numbers 1,2,3… and their 4–7 window restarts at 1,2… the same day; resets daily. New `appointments.queue_session` + `queue_number` (migration 0018) with a UNIQUE (clinic, session, number) index; assignment is FCFS max+1 with retry-on-race, **stable** across cancellations (per owner's choice). Core: `core/appointments/queue.ts` (`queueSessionKey`, `withQueueNumber`, `getDayQueue`); `checkDoctorSlot` now also returns availability+flexible so callers build the session without a re-query. Patients learn their number: it's in the WhatsApp booking ack, the reschedule reply, and the `notifyAppointmentBooked` confirmation ({{7}}). UI: a **# token column** on both appointment lists + a shared **`QueueSummary`** ("Now serving #X · N waiting · M done", token chips coloured by status) shown on `/clinic/appointments`, `/reception`, and the **doctor panel** ("Your queue today"). No-doctor ("Any") appointments carry no token. Verified end-to-end: WhatsApp bookings assign w0→#1,#2,#3 and w1→#1, reply carries the token, list shows "Now serving #2" (skipping the completed #1); typecheck green; suite 50/50. |
| 2026-07-11 | **Themed appointment status dropdown (no more system-blue hover).** The status control was a native `<select>` whose open-option highlight is OS-drawn (blue) and unstylable. Rebuilt `appointment-actions.tsx` on **Base UI Select** (same lib as the DatePicker): the highlighted item now uses the theme `bg-accent`/`accent-foreground` via `data-[highlighted]`, the popup uses `bg-popover`/`border-border`, and a ✓ marks the current status. Same behaviour (set any status → supports undo; applied via `useTransition`; server-side transition guards unchanged). E2E updated — options now mount in a portal on open, so the detail assertion checks the trigger + current-value label instead of every option label. Suite 50/50. |
| 2026-07-11 | **Toast no longer cut short + Fee column left-aligned.** `FlashToast` was calling `router.replace` to strip the flash param, which triggered a Next navigation that remounted the toast subtree and dismissed it after ~2s; now it strips the param via `window.history.replaceState` (no navigation), so the toast runs its full duration (bumped to success 4s / error 6s). The appointment-list **Fee** column (both `/clinic/appointments` and `/reception`) is now left-aligned to match When/Patient/Doctor (was `text-right`). Typecheck green. |
| 2026-07-11 | **Error toast on failed create/edit appointment.** `core/ui/toast.tsx` refactored into a reactive presentational `Toast` (`variant` success/error, `token` to re-trigger the same message) + a `FlashToast` wrapper (the redirect/query-param success case used by the lists). The appointment form now pops a red **error toast** whenever `createAppointment`/`updateAppointment` returns an error (e.g. out-of-hours / on-leave / over-cap slot), re-triggered per attempt via a nonce bumped on each settled action; replaced the inline error `<p>`. Typecheck green; suite 50/50. |
| 2026-07-11 | **Create/edit appointment → redirect to the list + success toast; fixes edit blanking the doctor.** The discount value is now a string-backed input so it can be cleared (a numeric state snapped a blank field back to 0). On **edit**, `updateAppointment` now **redirects to the appointments list** (`?updated=1`) instead of returning `saved:true` and staying — which is what caused React 19's post-action `<form>` reset to blank the controlled doctor `<select>` ("selected doctor removed"); navigating away means the reset artifact is never shown. **Create** already redirected, now with `?created=1`. Both `/clinic/appointments` and `/reception` read the flash param and render a new reusable `core/ui/toast.tsx` success toast ("Appointment scheduled." / "Appointment updated.") that strips the param on mount and auto-dismisses. Removed the dead inline "Saved." indicator. Typecheck green; suite 50/50. |
| 2026-07-11 | **Per-appointment discount off the doctor's fee + fee shown on the lists.** The create/edit appointment form now has a **Discount** field: a type toggle (**Amount (Rs)** — the default — or **Percent (%)**) plus a value (e.g. 500, or 20). New columns `appointments.discount_type`/`discount_value` (migration 0017, additive, free-text/int not enums). The **net fee** (doctor's `consultation_fee` − discount, clamped to `[0, fee]`) is derived live via pure CORE `core/appointments/fee.ts#computeFee` (shared by the client form + server) — never stored, so a fee change flows through; percent is capped at 100 server-side. The form shows a live "Fee Rs X − Rs Y discount = **Rs Z**" preview once a doctor is picked. Both appointment lists (**/clinic/appointments** + **/reception**) gained a **Fee** column showing the net (with the full fee struck through when discounted) on desktop and a "Fee:" line on mobile. The WhatsApp booking confirmation now quotes the **net** fee. Typecheck green; migration 0017 applied. |
